OPGAVE — Fase 4 pkt. 1 under HURTIGT SPOR (Mathias M-46/M-47: »hele workflowet er overkill … det skal du løse«; fabrik-beslutning plan DEL VIII pkt. 42): PRODUKTION (workspace-write, netværk FRA, web FORBUDT, INTET gate_input). Du skriver pakkens angrebs-TESTS SOM KODE + INDEKSET, blindt fra den låste plan — der findes ingen build. Workdir = kodebasen @ 5739bfc4dc2a8b5b48e9b13bb324a5256ac81994 uden .git (verificér blobs m. `git hash-object <fil>`). Pakke: lokations-skabelon. Plan-lås: plan v3.5 plan-build/lokations-skabelon/plan.md @ 289b87616750a1a1180a8ec5f7a639cd43f07a29 · manifest plan-build/lokations-skabelon/forventnings-manifest.json @ ab4c8501253cbc9310f9875c0ab5c672f1ad23dd (kanonisk: 60 forpligtelser · 127 negativer · 150 assertions · 12 eneste-værn) · krav docs/sandhed/krav/lokations-skabelon-krav.md @ 9402164d87a35fb939661058bea77c1a052493d0 · kill-list-udkast @ 13b783927f4fed107caa0e849e65499fd3291b61. ARBEJDSLISTE = din egen kontraktbehovs-dom plan-build/lokations-skabelon/provenance/angrebs-kontraktbehov.leverance.md @ 5ea32424b8ddd5ccc8f7244a00a8e448d59d3087 (K-1..K-14, inkonsistenser, over-test) — under den nye kontrakt er K-1/K-2/K-5/K-6/K-12/K-13 »bare kode i testen«; K-4/K-7/K-8/K-9/K-10/K-11 løses gennem lib/indeks/mutant-loop; K-3 (FA-3) håndteres pr. pkt. G nedenfor; K-14 = Fase 5 (ikke nu). Inkonsistens #6/#7 (kræver ny plan-SHA) er fabrik-armens/Mathias' bord: skriv testene mod manifestet SOM DET ER, og nævn præcis hvilke tests/mutanter der berøres i leverancen (ingen omskrivning af manifest/plan). MÅLE-LAGETS KODE @ denne commit er autoritet ved uoverensstemmelse m. kontrakt-teksten: scripts/v5/angrebs-indeks.mjs @ e523af9053524b22d97281d066751df075872a88 (skema 2 + komplethed) · scripts/v5/test-runner.mjs @ 76a9b4a7840532c42fbbd71c6c186b0c9684739d (lib-fladen makeLib + loadTests + rapport) · scripts/v5/pg-runner.mjs @ 42531d3a62e89d5e648cc551683d67136f5e5ffa (sql/http/race) · scripts/v5/build-proof.mjs @ 07356e6d8309198d99b86ed5993002ccb94c15a2 (dommen) · scripts/v5/angrebs-indeks-validate.mjs @ 04bc6b1b28715221d464b0d83080bb85acb46746 (CLI). Læs dem FØR du skriver.

=== TEST-KONTRAKT (fabrik-armen 2026-09-21, ordret) ===
A) TESTFILER: scripts/v5/lokations-skabelon/tests/<navn>.test.mjs — kun dér (måle-laget; byggeren kan ikke skrive i scripts/v5/**). Hver fil: `export const tests = [{ id, covers, run: async (lib) => { … } }]`. En test FEJLER ved at kaste (lib.forvent.* kaster `Afvist`; enhver anden exception tæller også som fejl). Ingen imports af produktkode; alt går gennem lib. Timeout pr. test 60 s.
B) LIB (det eneste testene får):
  lib.ejer.sql(text) → {ok, code, detail:{message, routine}, rows?}   (som ejer/superuser: opsætning, observation)
  lib.som({role, settings?}).sql(text) → samme udfald som aktør (set role + request.jwt.claims-settings)   · .http({method, path, body?, headers?}) → udfald m. http_status (PostgREST, JWT mintet fra aktøren) — R+/R− via API
  lib.race(scenario) → pg-runner race (overlap-vidne via tredje session; {protocolOk, a, b, overlap, invariantRows})
  lib.kontrakt(negativ_id) → manifestets reject_contract
  lib.forvent.ok(udfald, hvad?) · lib.forvent.afvist(udfald, negativ_id | kontrakt, {subst?}) (håndhæver sqlstate + grund + afvisningssted når observerbart; via API kræves http_status 4xx/5xx; {id}-grund kræver subst:{id:<uuid>}) · lib.forvent.lig(rows, {kind: rows|count|scalar|empty|null, value?}, hvad?) · lib.forvent.sandt(bool, detail)
  lib.manifest · lib.forventning (expectedSet) · lib.pakke
C) INDEKS = plan-build/lokations-skabelon/angrebs-spec.json SKEMA 2:
  { schema_version: 2, pakke, bindings: {manifest:{path,oid}, plan:{path,oid}},
    bids: [ {bid_id, kind: forudsaetning|effekt, depends_on:[…], covers:[<forpligtelses-id>…]} ]   (D12 — samme graf som før, covers = manifestets effekt_bid),
    tests: [ {id, file: "scripts/v5/lokations-skabelon/tests/<navn>.test.mjs", oid: <git hash-object af filen>, covers:[…]} ],
    mutants: [ {mutant_id, guard_ref, apply: <SQL som ejer>, restore: <SQL>, target_test_ids:[≥1], control_test_ids:[≥1, disjunkte]} ] }
  covers-former: "<forpligtelse>:<UT|FS|MH|SA>" · "<negativ-id>" · "<forpligtelse>|<delbevis-id>". id/covers i filen SKAL være identiske med indeksets. Komplethed: hver forpligtelse×form · hvert negativ · hvert navngivet delbevis dækket ≥1 test; D10: hvert eneste-værn har en mutant hvis targets dækker netop det negativ (SA-test tæller); K-gulv: hvert K ≥1 mutant.
D) MUTANT-KILL (CI kører det, Codex designer det): apply ok → alle target-tests SKAL FEJLE → alle control-tests SKAL være grønne → restore ok → alle targets+controls grønne igen. Ellers IKKE dræbt = gate rød. apply/restore = SQL som ejer mod den friske CI-store (fx `create or replace function … uden værnet`, `alter table … disable trigger …`, `drop policy …`).
E) VALIDÉR FØR COMMIT: `node scripts/v5/angrebs-indeks-validate.mjs plan-build/lokations-skabelon/angrebs-spec.json plan-build/lokations-skabelon/forventnings-manifest.json` → ✓ = komplet mod manifestet OG konsistent m. filerne på disk (oids, ids, covers). Exit ≠ 0 = ret før commit. Testfilerne FRYSES af oids: ændres en fil efter indekset, afviser CI hele kørslen.
F) CI (v5-build-dom.yml): måle-job uden token kører migrationer @ commit → testene → mutant-loopet → rapport skema 3; dom-jobbet (build-proof v3) tjekker: præcis indeksets tests alle ok · præcis indeksets mutanter dræbt (genudledt fra protokollen) · index_oid == gate-bindingen · bids/<bid>.json + reviews/<bid>.json PASS @ samme base_oid · prover.json grøn → v5/gate/build. Falsk-grøn-værnet er processen (der måler ≠ der bygger · CI kører · mutant-kill), ikke en DSL-genudledning.
G) HVAD CODEX IKKE SKAL: ingen DSL-cases, ingen footprint/claim_graph, ingen FA-3-driver medmindre datoflows ikke kan udtrykkes via SQL-opsætning (skriv da HALT m. præcis hvilke flows). Ét pas (max to).
=== SLUT ===

DRIVERENS TILLÆG: (1) Alle navne (tabeller · RPC'er m. fulde signaturer og navngivne parametre · roller · AK-*/SQLSTATE/grund/afvisningssted · audit-labels · faser) er PRÆCIS planens (§0.2 · §0.3 · §2 register · §3) og manifestets — du opfinder intet. Aktører pr. plan:56: R+ (ctx-A) = {role:"authenticated", settings:{"request.jwt.claim.sub":"<uuid for en bruger du opretter i setup via ejer-SQL>"}}; R− = authenticated uden den prøvede tab-grant; A+/SA/V m. planens rettigheder seedet via ejer-SQL i testens setup; ctx-B ejer-prober = lib.ejer.sql (kun S-bevis). API-tests (K-9/ac-1, K-9/ac-2, K-1/ac-5, K-5/ac-4 + negativer m. »(via API)« i fase) bruger lib.som(actor).http; alt andet SQL. Observationer af audit/spor og orakler = lib.ejer.sql (ejer-læsning, aldrig som erstatning for et aktør-negativ). (2) Én test må dække flere covers (fx et helt pending-forløb dækker K-6/ac-10:MH + dets delbeviser + negativer der falder i forløbet) — men hvert negativ SKAL være forsøgt som den aktør og i den fase kontrakten siger, og afvist-forventningen SKAL bruge lib.forvent.afvist m. negativ-id (kontrakten er kilden). (3) {id}-grund (K-7/ac-2/neg-2): lib.forvent.afvist(udfald, "K-7/ac-2/neg-2", {subst:{id:<den uuid du sendte>}}). (4) FA-3 (pkt. G): et datoflow må KUN sættes op via SQL hvis det ikke kræver skrivning af pending-status/deadline/domænerækker uden om produktets indgange (plan:33 forbyder det — din egen K-3-analyse). Kan et flow ikke udtrykkes lovligt: skriv IKKE en falsk test; list det under HALT-FA3 m. forpligtelse/assertion-id'er og skriv alt andet. Hvis det efterlader huller i komplethed, rapporterer validatoren dem — det er den ærlige tilstand; leverancen er stadig LEVERET-DELVIS. (5) Prover-filerne er allerede skrevet af dig (r2) og lagt i workdir uændret: plan-build/lokations-skabelon/prover.json (sha256 cb3b11adef9c45ca28e6abb990fd0fb927d22a679704a7547dffa3f362ac2b7a) og scripts/v5/lokations-skabelon/prover-run.mjs (sha256 394784f5499bcc05fc7fc107d8eac33ea977d4a7d94a689cd4fdf0903159612e) — behold dem byte-identiske. (6) Skriv INGEN andre filer end tests/*.test.mjs og angrebs-spec.json (+ de to prover-filer uændret). Ingen bids/ eller reviews/. Rør ikke plan, manifest, scripts/v5/*.mjs. (7) Vejnings-reglen: præcis hvad komplethed + D10 + K-gulv kræver; ingen over-test (din egen liste med 8 afgrænsninger gælder). (8) Rækkefølge hvis tiden er knap: bids i planens orden (Bid 1 → 5); stop hellere m. en konsistent, validator-kørt delleverance (alle skrevne filer i indekset m. korrekte oids) end m. inkonsistente filer — pas 2 fortsætter i samme workdir.

OUTPUT (din afsluttende besked, markdown, ≤ 200 linjer): 1) leverance-tabel: antal testfiler · tests · covers-dækning (forpligtelse×form n/60-former, negativer n/127, delbeviser n/N) · mutanter n (pr. K og pr. eneste-værn) · sha256 for angrebs-spec.json og de to prover-filer · 2) validatorens output ordret (sidste 10 linjer) + exit-kode · 3) HALT-FA3-flows (id'er) og #6/#7-berørte tests/mutanter · 4) rest til pas 2 (hvis nogen) · 5) fund i lib/kontrakt (ting du manglede i lib — fabrik-armen retter samme dag) · 6) ALLERSIDSTE ikke-tomme linje, præcis én gang i hele beskeden: LEVERANCE-JSON: {"status":"LEVERET"|"LEVERET-DELVIS"|"HALT","validator_exit":n,"testfiler":n,"tests":n,"mutants":n,"daekning":{"former":n,"negativer":n,"delbeviser":n},"halt_fa3":[…],"rest":[…],"lib_fund":[…]}. Skriv aldrig strengen VERDIKT-DRAFT.
