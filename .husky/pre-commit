#!/usr/bin/env sh
# pre-commit: rollens zoner, vagten for Mathias' dokumenter og lint-staged.

# Rollens zoner (scripts/v5/hooks.mjs)
node scripts/v5/pre-commit-zone.mjs || exit 2

# Mathias' dokumenter og workflowets tekster kun med hans ord i ledgeren (disciplin.md §8.1)
node scripts/v5/sandhed-vagt.mjs --staged || exit 2

# lint-staged
pnpm exec lint-staged
LINT_EXIT=$?

exit $LINT_EXIT
