Du er Codex i P2-rollen (codex-angreb) — C1-RUNDE 4: DELTA-VERIFIKATION af sandheds-motoren v2.3 efter din C1-runde-3-dom (C1-R3-FUND.md: M/G/E/P PASS; A/H/V FAIL m. F-C1-20..27). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for SYV moduler: forventnings-manifest.mjs · angrebs-spec.mjs · build-harness.mjs · build-proof.mjs · gates.mjs · gate-eval.mjs · prover.mjs (M/G/E/P uændrede blobs siden dit PASS — bekræft OID'erne; A/H/V er ændret).

## Input
`DIFF-c1-r3-til-r4.diff` (HELE deltaet siden runde 3, inkl. selvtests + integrationsrunner) · alle filer under scripts/v5/ · `INTEGRATION-LOG-v2.3.txt` (41 grønne mod rigtig Postgres 17.6 + rigtig git: motoren → manifest+spec committet → validateAngrebsSpec + verifyBuildProof grøn over det ikke-redigerede bevis) · C1-R3-FUND.md · B2-leverancen · forventningsliste (LÅST) · P-8-spec · docs/workflow-implementeringsplan.md (2.C »v2.3 efter Codex C1-runde 3« + C4-leveranceliste; DEL VIII pkt. 36 = fabrik-beslutning).

## Hvad der er rettet pr. fund (koden er sandheden)
F-20 → specObs/obsProj (V) bærer `has_setup` (case.setup ELLER race.setup) og `has_action` (FS): udelades setup/handling af observationerne matcher spec-projektionen ikke — også i baseline/under/clean og kontroller (bindCase bruges for alle indlejrede). judgeObservations (H) kræver `setup {ok:true}` når feltet findes, og komplette kald-udfald (kaldGyldig: ok boolean · code streng|null · detail objekt|null) for positive/negative/action/a/b. race.setup udføres af runCase som ejer FØR runner.race og observeres; runneren modtager scenariet UDEN setup (integrationsrunneren afviser et scenario m. setup).
F-21 → judgeKill kræver TRE vellykkede rækkesæt-målinger (fpRows: tæt array af kanoniserbare rækker) før noget sammenlignes; null/fejl/objekt er en manglende måling → hverken kill eller restore. killCaseMutant gemmer fejlede målinger som null + diagnose (`footprint-fejl: under: …`).
F-22 → observationen `overlap {observed, witness_pid, a_pid, b_pid}` erstatter `barrier`: et UAFHÆNGIGT tredje backend (witness_pid ∉ {A,B}) så begge sessions inde i deres skrivende transaktion samtidigt FØR nogen commit (integration: pg_stat_activity m. xact_start ∧ (backend_xid ∨ wait_event_type=Lock) — forudsætter IKKE produktlåsen; pg_blocking_pids er nu kun en produkt-observation, ikke dom). Manglende `overlap.observed` normaliseres IKKE (null → protokol). Nye assertions: to-sessions · overlap-observeret · aktoer-bundet · fase-bundet · afvisninger-bundne (alle afvisninger = kontraktens) · praecis-en-afvisning · afslutning-konsistent · invariant-efter-commit. brudtPaaFormensMaade(SA) = invariant brudt ∧ to-sessions ∧ overlap ∧ aktør ∧ fase ∧ afslutning-konsistent ∧ afvisninger-bundne ∧ BEGGE forløb ok+commit. Dermed er ingen af dine fire rækker (observed:false · samme pid · A ok m. rollback · forkert grund + brud) et kill længere.
F-23 → exit: rc ∉ {0,kontrakt} → protokol · rc==kontrakt UDEN klasse-linje → protokol (crash/manglende diagnose) · rc==kontrakt m. ANDEN klasse → protokol (en anden klasse tæller kun som brud med en låst udfaldskontrakt — den findes ikke i manifestet, og vi har IKKE tilføjet én; vej om det er rigtigt) · rc 0 m. klasse-linje → protokol (inkonsistent) · rc 0 → brudt (afvisningen bortfaldt). Kill på et exit-værn kræver altså rc 0.
F-24 → exit-kontrakt i observationerne bærer fase/aktoer; obs bærer fase/aktoer; judge pusher fase-/aktoer-bundet i exit OG SA; A (spec-validator) kræver fase == kontrakt.fase og actor.role == kontrakt.aktoer i exit-grenen og SA; V's projektion sammenligner fase/aktør/kontrakt.fase i alle tre varianter.
F-25 → claim_graph-entry kræver `guard_ref` (manifest-deklareret); ALLE mutant_ids skal have det værn og ramme cases ⊆ claim.case_ids; for hver mutant skal target-assertion være udført og opfyldt i topniveau-delbeviset for target-casen (kill genudledes separat); source_anchor git-verificeres (verifyEvidence) OG uddraget (readBlobLines+excerptAt) skal nævne symbolet casen handler om (caseSymbol: afvisningssted-routinen for UT/SA sqlstate ≠ "-", ellers entrypoint.ref — unqualified). Ikke krævet pr. K (proportionalitet); case_ids/mutant_ids distinkte.
F-26 → A afviser mutant_id == case_id; engine dead ved kollision/dublet; V: executed_ids skal være PRÆCIS mængden (længde == antal, ingen dubletter, alle ∈ forventet).
F-27 → session(): tilfældig nonce (12 bytes) pr. session i markøren, match KUN som hel linje (^…$ multiline), PRÆCIS én forekomst i segmentet (ellers reject → race protocolOk=false); ERROR-detektion kun på hel linje; VERBOSITY verbose fra sessionsstart. Kontrolleret-strøm-prøver i integrationen (markør-lignende data før den rigtige markør · rigtig markør to gange).
Uændret: manifest v2 (ingen skemaændring) · gates/gate-eval/prover (bekræft blobs).

## Angrib — vejnings-reglen
A. F-20-lukningen: er `has_setup/has_action` + `setup.ok` + kaldGyldig tilstrækkeligt, eller findes der stadig et spec-foreskrevet forløbselement der kan udelades/erstattes uden at projektionen eller dommen ser det (fx checkpoints/vidner m. samme id men anden observe-sql — er observe-sql'en overhovedet spec-projiceret, og BEHØVER den være det når runneren er residualen?). Vej det ærligt.
B. SA-kill-definitionen (begge ok+commit ∧ overlap ∧ …): er den for STRAM (findes et legitimt SA-brud den ikke kan udtrykke — fx en mutant hvor invarianten brydes selvom én session afvises?) eller for LØS (kan overlap-vidnet snydes: witness_pid ≠ A,B men ikke faktisk uafhængigt; a_pid/b_pid kopieret fra sessionerne — hvad beviser det reelt? Navngiv det som runner-residual hvis det er det).
C. Exit-varianten efter F-23: er »anden klasse → protokol« den rigtige klassifikation uden en låst udfaldskontrakt, eller skjuler den et reelt brud (et værn der er muteret til at afvise med en anden grund)? Skal manifestet have `klasser` (låst mængde) — vej pris mod behov.
D. F-25: er guard→mutant→case→target-assertion→anker-symbol den rigtige mekaniske forbindelse, eller kan et claim stadig »låne« et andet værns kill (fx to mutanter på samme værn m. forskellige cases; et anker der nævner routinen i en kommentar)? Er symbol-inklusion i uddraget for svag/for stærk?
E. F-26/F-27: rest-huller? (fx executed_ids som ikke-streng-elementer · nonce-markør der lækker via pg_stat_activity.query til produktkoden — betyder det noget?)
F. Kontrakt-dækning mod B2 (tabel 1/2 · resultatkontrakt · reject-felter · pkt. 1-6 · frit-pas-manifest): MØDT/DELVIST/MANGLER — hvad er MATERIELT før første bid, og hvad er C4/plan-gate/runner-residual (navngiv præcist).
Giv: dom pr. modul (PASS/FAIL m. blob-OID) for de syv + integrationsrunneren, fund F-C1-n, mutant-tabel (fjern has_setup/has_action-projektion · fjern fpRows-kravet · fjern SA-kill-betingelserne (kun invariant) · fjern exit »ingen klasse-linje → protokol« · fjern fase-binding i exit/SA · fjern claim guard/target-case-binding · fjern executed_ids-entydighed · fjern hel-linje-match på markøren), og navngivne residualer.
