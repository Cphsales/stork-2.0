Du er Codex i P2-rollen (codex-angreb) — RUNDE 4 = DELTA-VERIFIKATION efter din runde-3-dom (RUNDE3-FUND.md: FAIL(8) — T-F3, T-F7, T-F8, G-F1, F-14, F-15, F-16, F-17). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for de 9 moduler (codex-run.sh · verdikt-byg.mjs · verdikt.mjs · git.mjs · gates.mjs · gate-eval.mjs · kvittering.mjs · plan-gate-run.mjs · krav-gate-run.mjs).

## Input (workdir)
`DIFF-runde3-til-runde4.diff` (alt ændret siden din runde-3-dom) · alle filer under scripts/v5/ (nu-version) · historiske artefakter under plan-build/ · husky pre-commit.

## Hvad runde 4 gør
T-F3: efter codex/node er resolved fra kalderens RENSEDE PATH, sættes PATH=system-PATH (/usr/bin:/bin:/usr/local/bin) for resten af scriptet — cat/cut/mv/rm/ls/dirname/head/basename kommer aldrig fra kalderen; selvtest m. falsk `cat` bekræfter at rolleteksten forbliver den ægte. T-F7: OUT-basename med reserveret suffix/prefix (.lock · .receipt.json · .provenance · .pid · .stderr.log · .log · .attempt* · .tmp · .run-*) blokeres FØR låsen; selvtest for 7 navne. T-F8: hook: en staged SLETTET rolletekst som låsen stadig refererer → deny (tidligere faldt den tilbage til HEAD). F-14: gate-kernens verifyTransport kræver nu en COMMITTET leverance (provenance/<navn>.leverance.md, opslag på sha256 == kvitteringens output_sha256) og GENUDLEDER dommen: udtraekVerdiktDraft + draftMatcherVerdikt (aktor · conclusion · negative_cases · claim_graph_refs · evidence path/line_span) mod verdiktet — ændret conclusion alene → rød (selvtest). F-15: GATE_REGISTRY.codexRolle (krav/plan = "codex-angreb"); verdikt-byg + verifyTransport kræver receipt.rolle == gatens codexRolle, run_id/run_attempt/effort == kvitteringen, og lås @ receipt.regel_commit == lås @ evidens-commit (HEAD i hjælperen) + rolle-felter matcher låsen. F-16: fence-parser (udtraekVerdiktDraft) i kvittering.mjs: kun en åbner på linjestart m. PRÆCIS tre backticks + "json verdikt-draft" på top-niveau tæller; en blok inde i en ````-fence eller m. tekst før backticks ignoreres; præcis én aktiv blok; uafsluttet fence → rød; selvtests for citeret/inline/4-fence/to blokke. F-17: tree-casen kræver "ikke en fil (git-type tree)"; F-7-casen: verifier pinned @ H1 forbliver grøn efter approval slettes i H2, ny verifier @ H2 rød; git replace af devil-blob gennem git.mjs → verifier ser original; binaries-pin nås under selftest via STORK_V5_SELFTEST_FORCE_BINCHECK (mismatch → BLOKER, match → kører). Desuden: approval-objektet bindes til den committede approval @ E (eksistens + kanonisk digest).
G-F1: uændret — afventer Mathias' udtrykkelige ord (forelagt; DEL VIII pkt. 36).

## Angrib
1. Pr. fund T-F3/T-F7/T-F8/F-14/F-15/F-16/F-17: LUKKET / DELVIST / IKKE m. fil:linje. G-F1: bekræft at det ALENE er mandat-spørgsmålet der står.
2. Nye vektorer skabt af runde 4: system-PATH-skiftet (mister vi noget nødvendigt værktøj? `basename`/`cut`/`tee` i /usr/bin — ok på Ubuntu/WSL2, men er der en vej hvor SYSPATH-værktøjer selv er skrivbare for aktøren?) · reserveret-suffix-listen (mangler et suffix wrapperen faktisk skriver? `.attempt<N>.started/.done` dækkes af `*.attempt*`; `.receipt.json.tmp` af `*.tmp`) · leverance-opslag på sha (to leverancer med samme bytes = samme sha — er det et hul?) · draftMatcherVerdikt's canon (nøgle-sortering kun på objekter i top-niveau af værdier? evidence-sammenligning tager kun path+line_span — kan blob_oid/excerpt_sha i verdiktet afvige fra leverancen uden konsekvens, og betyder det noget når kernen selv læse-beviser dem?) · codexRolle hardkodet "codex-angreb" for krav — er buildability-Codex (r4b) den rolle? (r4b er digest-undtaget, så kun fremtidige krav-verdikter rammes) · lås @ regel_commit == lås @ E: kan en aktør køre under den gældende lås men ældre regel_commit m. SAMME lås-blob (legitimt) — tillades det? (ja, blob-lighed) — er det rigtigt?
3. Mutant-tabel: (a) drop system-PATH-skift · (b) drop reserveret-suffix-check · (c) drop slettet-rolle-check i hook · (d) drop leverance-genudledning i verifyTransport · (e) drop codexRolle-check · (f) drop lås@regel_commit==lås@E · (g) parser accepterer 4-backtick åbner · (h) drop approval-indholds-binding — fanges de af de leverede selvtests?
4. Dom: PASS / FAIL(n). Ved PASS: blob-OID pr. fil.

## Leverance (markdown, kort)
Dom på én linje · tabel | fund | dom | fil:linje | · nye fund F-18… · mutant-tabel (a-h) · hvad der er rigtigt.


## Tilføjelse runde 4b
Efter runde-4-koden blev codex-kaldet rettet: codex-shim'en er `#!/usr/bin/env node`, så wrapperen giver den PRÆCIS den verificerede node-mappe + system-PATH (CODEX_PATH) — ikke kalderens PATH. Selvtestens falske codex er nu et node-script. Vurder om CODEX_PATH-konstruktionen genåbner T-F3 (node-mappen er den prefix-verificerede NODE's dirname).
