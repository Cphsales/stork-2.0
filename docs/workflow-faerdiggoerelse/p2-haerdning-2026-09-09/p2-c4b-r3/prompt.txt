Du er Codex i P2-rollen (codex-angreb) — C4b RUNDE 3: DELTA-VERIFIKATION af ci-build-dom.mjs alene efter din runde-2-dom (C4B-R2-FUND.md: pg-runner PASS · workflow-form PASS · ci-build-dom FAIL m. F-C4b-3). Skriv på dansk. Kun læse-adgang; skriv intet. Dommen afgør om ci-build-dom.mjs går fra KANDIDAT til register-pas som hærdet transport. Alle andre moduler er byte-identiske (bekræft kort: pg-runner c43c57bc · gates 1a4369b7 · gate-eval 7110053a · build-proof 756627cb · ci-gate-dom dc59d796).

## Kontekst
Mathias: »der går alt for langsomt«; kandidat-drift (pkt. 36); én runde pr. fund-klasse; sidste planlagte runde på C4b. Navngiv kun MATERIELLE huller; et fortjent PASS er det rigtige svar.

## Input
`DIFF-c4b-r2-til-r3.diff` · scripts/v5/ci-build-dom.mjs (+ selftest, 38) · øvrige scripts/v5 · v5-build-dom.yml · C4B-R2-FUND.md · docs/workflow-implementeringsplan.md (pkt. 39).

## Hvad der er rettet (koden er sandheden)
F-C4b-3 → dom-jobbet læser bevis-filen som RÅ Buffer (`readFileSync(inn)` uden encoding); `doemBevis` tager Buffer (eller producentens UTF-8-streng, som konverteres tabsfrit til Buffer), hasher de RÅ bytes (`hashObjectBytes` m. Buffer-input til `git hash-object --stdin`), og afkoder KUN m. `TextDecoder("utf-8", {fatal: true})` — ugyldig UTF-8 → rød (»bevis-bytes er ikke gyldig UTF-8«), aldrig erstatning; JSON.parse på den tabsfrit afkodede tekst. Selvtest: din 0xff-modprøve → rød; Buffer-roundtrip → oid == hash(rå) == hash(producentens streng); lossy ≠ raw. Din afgrænsning af miljøisolationen: runProver merger process.env — producerBevis scrubber forælderens credentials (CREDENTIAL_ENV_RE) fra process.env for kaldets varighed og genopretter dem; job A har intet emissions-token (scriptet afviser --produce m. GITHUB_TOKEN), så det er hygiejne, ikke et hul. /proc/<ppid>/environ og service-container-isolation står som R-RUNNER-UDFØRELSE.

## Angrib — sidste runde
A. Byte-bindingen: er der stadig en vej hvor check-run'ets oid ≠ det downloadede artefakts rå bytes (BOM · CRLF · trailing bytes efter JSON · JSON der parser ens men er byte-forskellig — oid er over bytes, så det er ok; vej om body-felter der IKKE bruges af verifieren kan ændres uden at oid-bindingen betyder noget — oid binder bytes, verifieren dømmer indhold: begge dele må holde)? Kan `readFileSync(inn)` få en anden fil end den uploadede (dom-jobbet downloader artefaktet fra samme run — R-CI-AUTENTICITET)?
B. Meta-kontrakten: `--meta-in` er også en fil fra job A (produktkodens runner-VM) — meta.fejl/naaet/store læses; kan meta forfalske noget til GRØNT? (naaet:false → tavshed, ikke grønt; fejl → rød; store → kun summary-tekst — og `skipped_migrations` står i body, ikke meta. Bekræft.)
C. Bekræft at F-C4b-1/2-forsvarene og verifierens genudledning er uændrede i deltaet.
Giv: dom for ci-build-dom.mjs (PASS/FAIL m. blob-OID), fund F-C4b-n KUN for materielle huller, mutant-tabel (genindfør utf8-afkodning før hash · fjern fatal-decoderen · hash den afkodede streng i stedet for rå bytes), navngivne residualer m. placering (kort — henvis til runde 2 hvor uændret).
