Du er Codex i P2-rollen (codex-angreb) — RUNDE 10b = DELTA-VERIFIKATION efter din runde-10-dom (RUNDE10-FUND-ukvitteret.md: FAIL codex-run.sh @ 1caee1ac — fund 1: `--version` startede før hash-kontrollen; fund 2: shim'en kan vælge en anden native end den hashede; G-F1 = mandat, HALT). Din runde-10-leverance blev publiceret men IKKE kvitteret: fabrik-armen redigerede wrapper-filen mens bash stadig kørte den, så kørslen døde før kvitteringen (dokumenteret drift-fejl, ikke dit). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (+ genbekræftelse af de ni .mjs).

## Input
`DIFF-runde9-til-runde10b.diff` (codex-run.sh · binaries.lock.json · codex-run.selftest.mjs · haerdet-register.json — sidstnævnte er registerets runde-9-opdatering, ingen gate-modul-ændring) · alle filer under scripts/v5/ · RUNDE9-FUND.md · RUNDE10-FUND-ukvitteret.md · historiske artefakter under plan-build/. De ni .mjs-blobs skal være identiske med dine runde-9-PASS-entries — bekræft.

## Hvad der er rettet siden din runde-10-dom (samme wrapper-blok, tre fund i ét delta)
F-3b fund 1 (rækkefølge): lås valideres → shim-sha → native-sha (fil, eksekverbar, kendt prefix) → FØRST DEREFTER første start `--version` (på den verificerede fil; kræver rc 0, præcis én linje uden CR, == låsens `codex.version`) → exec. Selvtestens falske codex logger nu ALLE starter inkl. `--version` (`starts`) adskilt fra exec-kald (`calls`); BLOKER-casene kræver `starts.length === 0`; versions-casen kræver præcis én start = `--version` og ingen exec.
F-3b fund 2 (filbinding): wrapperen kører den VERIFICEREDE native binær DIREKTE (`CODEX_EXEC=$CODEX_NATIVE`) med det env shim'en selv sætter (CODEX_MANAGED_PACKAGE_ROOT=<pakkerod>, CODEX_MANAGED_BY_NPM=1) — shim'en og dens require.resolve/package-exports er ude af eksekveringsstien. Kvitteringen bærer `binaries.codex_exec{path,sha256}` (== codex_native). Selvtest: den falske native er en eksekverbar kopi af den falske codex; casen beviser at det var native der startede (env CODEX_MANAGED_PACKAGE_ROOT == realpath(pakkerod)), og at byttet native → BLOKER uden nogen start. Under STORK_V5_SELFTEST=1 uden FORCE_BINCHECK spiller den falske shim selv native (kvittering selftest=true → afvises som gate-evidens).
F-3c (nyt værn, driver-/fabrik-fund): `CODEX_HOME` pinnes til `$HOME/.codex` (override `STORK_V5_CODEX_HOME` kun under selftest), `OPENAI_BASE_URL` fjernes fra kaldets env (`env -u`), og `$CODEX_HOME/config.toml` må ikke indeholde `model_provider`, `model_providers`, `base_url`, `chatgpt_base_url`, `openai_base_url`, `profile` eller tabellerne `[model_providers…]`/`[profiles…]` → BLOKER (»rollen bestemmer kaldet« gælder også serveren bag modellen). Kvitteringen bærer `codex_home` + `config_sha256`. config.toml som symlink → BLOKER.
A5 (driver-fund fra B1's banner): sandbox-POLITIKKEN sættes eksplicit pr. kald: `-c sandbox_workspace_write.network_access=false`, `exclude_slash_tmp=true`, `exclude_tmpdir_env_var=true` — lokal config havde network_access=true, så produktions-kørsler havde netværk trods »Web forbudt«. Røgtestet mekanisk med den rigtige CLI: NETVAERK NEJ · TMP_SKRIV NEJ · WORKDIR_SKRIV JA; banner »sandbox: workspace-write [workdir]«. Kvitteringen bærer `sandbox_policy`. Læse-adgang uden for workdir kan sandboxen ikke begrænse → deklareret residual (blind-workdir-recept + prompt).

## Angrib
1. Fund 1 og 2 genbesøgt: er der NOGEN start af et u-verificeret program før alle byte-kontroller (også via `$NODE`-kald til JSON-parsing af låsen — node er prefix-kontrolleret men ikke hash-pinnet: er det et hul i samme klasse, eller dækket af »kendt prefix«-residualen?) · kan `CODEX_EXEC` afvige fra den hashede fil (race mellem hash og exec — TOCTOU på samme sti; symlink-skift efter realpath; bind-mounts) · kører den native binær korrekt uden shim'en (env-forskelle) — vurder ud fra shim'ens kode.
2. F-3c: kan config stadig omdirigere kaldet med nøgler regex'et ikke fanger (fx `model_providers` skrevet som inline-tabel `model_providers = { … }`, nøgler med anførselstegn `"base_url"`, dotted keys `model_providers.evil.base_url = …`, unicode-whitespace før nøglen, `[model_providers . evil]`, includes/`experimental_*`, `wire_api`, env-var-reference `env_key`)? Kan andre env-variabler end OPENAI_BASE_URL pege kaldet om (OPENAI_API_KEY → anden konto men samme model? CODEX_* ? HTTPS_PROXY/HTTP_PROXY/ALL_PROXY → MITM af TLS er ikke muligt uden CA, men SSL_CERT_FILE/NODE_EXTRA_CA_CERTS?) — vej efter vejnings-reglen: hvad ÅBNER faktisk en falsk-grøn (fremmed server svarer PASS som »gpt-6-astra«), og hvad er støj?
3. A5: kan `-c`-overrides tabe mod config/profil/env (præcedens i Codex' config-lag)? Er read-only (dom) uden netværk uafhængigt af config? Er `-c`-nøglerne stavet som Codex 0.154.0 faktisk kender dem (binæren indeholder strengene sandbox_workspace_write · network_access · exclude_slash_tmp · exclude_tmpdir_env_var)?
4. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — inkl. `codex-run.sh` og de ni .mjs; binaries.lock.json + selvtest som inputbinding. Ved FAIL: kun fund der lader et u-pinnet program køre, lader en fremmed server svare som den pinnede model, giver netværk i produktion, eller giver grøn kvittering uden alle pins (vejnings-reglen).
5. Mutant-tabel — fanges hver af disse af de LEVEREDE selvtest-cases: (a) flyt `--version` foran byte-kontrollerne · (b) exec via `$CODEX` (shim) i stedet for `$CODEX_EXEC` · (c) fjern `env -u OPENAI_BASE_URL` · (d) fjern config-værnet · (e) fjern én af de tre sandbox-politik-flag · (f) accepter flere linjer fra `--version` · (g) hash native EFTER `--version`.
