Du er Codex i P2-rollen (codex-angreb) — RUNDE 10 = DELTA-VERIFIKATION efter din runde-9-dom (RUNDE9-FUND.md: PASS for de ni .mjs-moduler; G-F1 = mandat, HALT). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (og genbekræftelse af de ni .mjs).

## Input
`DIFF-runde9-til-runde10.diff` (codex-run.sh · binaries.lock.json · codex-run.selftest.mjs — intet andet er ændret siden runde 9; bekræft at de ni .mjs-blobs er identiske med dine runde-9-PASS-entries) · alle filer under scripts/v5/ · historiske artefakter under plan-build/.

## Hvad runde 10 gør — FUND F-3b (2026-09-10, fundet ved en rigtig CLI-opdatering)
Codex-CLI blev opdateret 0.153.0 → 0.154.0 via npm. Wrapperens F-3-pin (sha256 af shim-filen `bin/codex.js`) fangede det IKKE: shim-filen er byte-identisk på tværs af versioner (sha 61b0194f…); den native binær ligger i `<pakkerod>/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/bin/codex` (262 MB) og udskiftes uden at shim-hashen ændres. Et binær-skift under samme kendte prefix var altså usynligt for pinnen — præcis det F-3 skulle fange. Rettelse i `codex-run.sh` (pin-blokken efter `resolve_bin`): låsen @ regel_commit SKAL indeholde `codex.version` (streng) og `codex_native{sha256, path_from_pkg_root}`; ellers BLOKER. Native binær = `realpath -e "$(dirname "$CODEX")/.."` + `path_from_pkg_root` ('..'-segmenter afvises i lås-valideringen), skal være en almindelig fil (ikke symlink), i kendt prefix (uden for selftest), med sha == lås. `codex --version` (kørt med CODEX_PATH) skal være == `codex.version`. Alt sker FØR første codex-kald. Kvitteringen får `binaries.codex_native{path, sha256}`. binaries.lock.json er opdateret (0.154.0: shim 61b0194f…, native 3188814c…). Selvtest: +8 cases (native mangler · native sha forkert · version forkert · lås uden codex_native · '..'-traversal · alt pinnet → kører + kvittering · binær byttet EFTER låsen → BLOKER).

## Angrib
1. Kan en udskiftet/opdateret/injiceret codex-binær stadig køre gennem wrapperen med grøn kvittering? Prøv: native udskiftet men shim identisk (selve F-3b) · native som symlink til anden fil · pakkerod-afledningen (shim i `bin/`, `..` = pakkerod — kan en shim et andet sted i et kendt prefix pege på en fremmed »pakkerod«?) · `path_from_pkg_root` med URL-encodede/unicode-segmenter, dobbelte skråstreger, trailing slash · versionsstrengen kommer fra binæren selv (kan lyve — men er sha-tjekket så ikke det bærende værn, og er rækkefølgen rigtig?) · lås med `codex_native.sha256` i STORE bogstaver (hex64-regex er lowercase — konservativt = rød?) · lås-JSON med dubletnøgler/`__proto__` · CR/LF i versionsstreng · `codex --version` der udskriver flere linjer eller intet (`ukendt` ≠ lås → rød?) · BIN_JSON bygges med printf — kan en sti med `"` eller `\` give ugyldig kvitterings-JSON, og hvad sker der så (fail-closed eller grøn kvittering med ødelagt JSON)?
2. Rækkefølge og fail-closed: er der NOGEN vej hvor codex kaldes før alle tre pins (shim · native · version) er verificeret, eller hvor en BLOKER efterlader `.done`/kvittering? Rører ændringen flock/privat rundir/OUT-reglerne?
3. Selvtest-lempelser: under STORK_V5_SELFTEST=1 uden FORCE_BINCHECK springes hele pin-blokken over — er kvitteringen så mærket selftest=true, så den afvises som gate-evidens? Bekræft at FORCE_BINCHECK-casene faktisk NÅR native- og versionstjekket (ikke bare shim-tjekket).
4. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — DENNE gang inkl. `codex-run.sh` (og gerne binaries.lock.json som inputbinding) foruden de ni .mjs. Ved FAIL: kun fund der lader en ikke-pinnet binær køre eller giver grøn kvittering uden alle tre pins (vejnings-reglen).
5. Mutant-tabel: (a) fjern native-sha-tjekket · (b) fjern versionstjekket · (c) accepter lås uden codex_native · (d) tillad '..' i path_from_pkg_root · (e) tjek native EFTER codex-kaldet — fanges hver af dem af de leverede selvtest-cases?
