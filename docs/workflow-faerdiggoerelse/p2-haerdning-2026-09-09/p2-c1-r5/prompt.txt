Du er Codex i P2-rollen (codex-angreb) — C1-RUNDE 5: DELTA-VERIFIKATION af sandheds-motoren v2.4 efter din C1-runde-4-dom (C1-R4-FUND.md: M/A/G/E/P PASS; H/V/I FAIL m. F-C1-28..33). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for SYV moduler: forventnings-manifest.mjs (ÆNDRET: guards[].locus) · angrebs-spec.mjs (uændret e768060d) · build-harness.mjs · build-proof.mjs · gates.mjs · gate-eval.mjs · prover.mjs (G/E/P uændrede — bekræft OID'erne) + integrationsrunneren.

## Input
`DIFF-c1-r4-til-r5.diff` (HELE deltaet siden runde 4, inkl. selvtests + integrationsrunner) · alle filer under scripts/v5/ · `INTEGRATION-LOG-v2.4.txt` (49 grønne mod rigtig Postgres 17.6 + rigtig git) · C1-R4-FUND.md · B2-leverancen · forventningsliste (LÅST) · P-8-spec · docs/workflow-implementeringsplan.md (2.C »v2.4 efter Codex C1-runde 4«).

## Hvad der er rettet pr. fund (koden er sandheden)
F-28 → kaldGyldig (H) kræver KONSISTENS: ok:true ⇒ code og detail null/udeladt; ok:false ⇒ code er ikke-tom SQLSTATE-streng. safeSql afviser et selvmodsigende runner-udfald (ok:true m. kode) og en afvisning UDEN kode som protokol (dead) før nogen dom. Gælder positive/negative/action/a/b og alle mutantfaser (samme runCase).
F-29 → brudtPaaFormensMaade(SA) = invariant brudt ∧ to-sessions ∧ overlap-observeret ∧ aktoer-bundet ∧ fase-bundet ∧ afslutning-konsistent ∧ afvisninger-bundne ∧ PRÆCIS to sessions ∧ MINDST én committet (ok+commit) ∧ HØJST én afvist. Din modmodel (A committer nul aktive, B korrekt afvist) er nu et kill; dine fire runde-3-afvisninger (observed:false · samme pid · A ok m. rollback · forkert grund) er stadig ikke-kill; begge afvist (ingen committet) er ikke-kill; uvedkommende kode er protokol.
F-30 → runCase indsamler ALLE `klasse=`-linjer: obs.exit = {exit_code, klasse_observeret (kun når præcis én), klasse_linjer (rå antal)}. Dommen: klasse_linjer mangler → protokol; >1 → protokol (flertydig/modstridende); (klasse_linjer==1) ≠ (klasse_observeret≠null) → protokol.
F-31 → manifestet (M) får `guards[].locus {path, pattern}` (valgfrit pr. værn; valideret: relativ repo-sti uden traversal, gyldigt regex/m); expectedSet.guards er nu Map(id → {beskrivelse, locus|null}). Verifieren (V): et claim KRÆVER at værnet har locus (ellers rød); source_anchor.path == locus.path; ankerets uddrag (readBlobLines+excerptAt over det git-verificerede anker) skal matche locus.pattern. Symbol-inklusionen er fjernet (den var for svag — kommentar — og for stærk — ACL-ankre). Locus-mønstrets INDHOLD (fx `^\s*create (or replace )?function f\.lokation_opret\(`) er plan-gatens dom over manifestet (R-PLAN-SEMANTIK); mekanikken sikrer kun at producenten ikke kan vælge et andet uddrag. Guard→mutant→target-case→target-assertion-bindingen fra v2.3 er bevaret.
F-32 → session() (I) holder stdout (data) og stderr (diagnostik) ADSKILT — ikke længere `2>&1`. Pr. sætning skrives `\warn MARK` (stderr) og `\echo MARK :ERROR :SQLSTATE` (stdout). Dommen ok/fejl + SQLSTATE kommer fra psql's egne statusvariable på markørlinjen; message/routine parses KUN fra stderr-segmentet. Konsistenskrav: status=fejl ⇒ diagnostikkens SQLSTATE == status-SQLSTATE; status=ok ⇒ ingen ERROR-linje i diagnostikken; ellers reject (→ race protocolOk=false). Nonce, hel-linje-match og præcis-én-forekomst gælder BEGGE strømme. Prøver: komplet falsk ERROR/CONTEXT-blok som data → ok:true (data); falsk fejl-data + virkelig 22012 → 22012; kontrollerede strømme (markør-lignende statuslinje · dublet-markør · inkonsistent status/diagnostik begge veje · konsistent fejl).
F-33 → dockerPsql (I): query-output der er tomt / ikke JSON / ikke et array = FEJLET måling (ok:false, rows:null, aldrig []); fejlet query → rows null. race() afviser en fejlet invariant-måling som protokol (aldrig invariantRows). Prøver: `where false` → ok rows=[]; syntaksfejl → ok:false rows=null; invariant `select 1/0` → SA protokol.
Uændret: A (angrebs-spec) · G/E/P. Manifestets øvrige skema uændret (locus er additiv).

## Angrib — vejnings-reglen
A. F-28: er konsistenskravet komplet (detail m. message uden code? code '' ? ok:false m. code men detail-objekt uden felter)? Kan et runner-udfald stadig få en uvedkommende fejl til at ligne en tilladelse eller en bundet afvisning?
B. F-29: er »≥1 committet ∧ ≤1 afvist ∧ alle afvisninger bundne« den rigtige generelle SA-kill-definition, eller åbner den et ugyldigt kill (fx A afvist korrekt, B committer noget der IKKE er racets brud men bryder invarianten af en anden grund — er det stadig SA-formens brud?) eller udelukker den et gyldigt? Vej mod B2 pkt. 3 og K-2/ac-6.
C. F-30: rest-huller (klasse-linje m. whitespace/CR · linje i stderr i stedet for stdout · klasse i exit 0-tilfældet)?
D. F-31: er manifest-locus den rigtige binding (plan-gate-dømt, gate-bundet, producenten kan ikke vælge)? Kan producenten stadig substituere (samme sti, andet linjespan der matcher mønstret — fx en ANDEN version af routinen i samme fil; et mønster der er for bredt)? Er »værn uden locus kan ikke bære claim« proportional, eller tvinger det locus på alle værn (nej — kun værn m. claims)? Hvad skal plan-gaten konkret dømme om et locus (navngiv det som R-PLAN-SEMANTIK-krav).
E. F-32/F-33: kan produktdata stadig påvirke dommen (fx en \echo-lignende linje kræver kendskab til nonce; data på stderr er umuligt fra SQL — men hvad med NOTICE/WARNING fra RAISE NOTICE i produktkoden — de går til stderr: kan de forfalske en ERROR-linje/CONTEXT? Vej det: RAISE NOTICE 'ERROR:  P0001: x' — hvordan ser psql-output ud, og fanger konsistenskravet (status ok ⇒ ingen ^ERROR:) det?). Tom-mængde: er der en vej hvor json_agg giver noget andet end et array ved succes?
F. Kontrakt-dækning mod B2 (tabel 1/2 · resultatkontrakt · reject-felter · pkt. 1-6 · frit-pas-manifest): MØDT/DELVIST/MANGLER — hvad er MATERIELT før første bid, og hvad er C4/plan-gate/runner-residual (navngiv præcist).
Giv: dom pr. modul (PASS/FAIL m. blob-OID) for de syv + integrationsrunneren, fund F-C1-n, mutant-tabel (fjern konsistenskravet i kaldGyldig · genindfør »begge committer« · tillad første klasse-linje · fjern locus-sti-kravet · fjern locus-mønster-kravet · slå strømmene sammen igen (2>&1) · rows=[] ved tomt output · race uden invariant-tjek), og navngivne residualer. Hvis alt holder: sig PASS pr. modul og navngiv residualerne ærligt — et PASS der ikke er fortjent, er værre end et FAIL.
