Du er Codex i P2-rollen (codex-angreb) — RUNDE 2 = DELTA-VERIFIKATION af gate-kernen efter fold-ind + angreb på to NYE moduler. Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for gates.mjs, gate-eval.mjs, kvittering.mjs, plan-gate-run.mjs, krav-gate-run.mjs.

## Input (workdir)
- `P2-RUNDE1-FUND.md` — dine fund fra runde 1 på gate-registry-ændringen: F-1 (BLOKER: approval-data beviser ikke godkendelseshændelsen) · F-2 (layout-alias) · F-3 (egen iterator på prerequisite_digests) · F-4 (mutant e: sæt vs multiset ikke testet).
- `AENDRINGER-siden-runde1.diff` — diffen fra den angrebne commit til nu for gates/gate-eval/krav-gate-run + deres selvtests.
- NYE moduler: `scripts/v5/kvittering.mjs` (godkendelses-kvittering + verifyApproval mod git-historik), `scripts/v5/plan-gate-run.mjs` (udfør-siden af plan-gaten), `scripts/v5/kvittering.selftest.mjs` (42 cases mod temp-git-repo).
- Kontekst: verdikt, proofs, git, recon-gate-run, register-selftest.

## Hvad der er gjort
F-1: `gates.mjs` GATE_REGISTRY har `approvalReceipt` (plan+slut true; krav false = HISTORISK UNDTAGELSE: M-38 faldt før kæden fandtes, dokumenteret, ikke retro-fikset; recon/build false). Approval må/skal bære `kvittering_digest` (sha256) på receipt-gater og må ikke på andre. Kernen kræver `deps.verifyApproval` (funktion, ok===true) og sender {gateId, expectedScope, verdictDigests, artifactOid, expectedOids}. `kvittering.mjs`: `validateKvittering` (plain, egne felter, exact keys, gate/pakke/artefakt/bindinger/scope/prerequisite multiset via egne indeks-felter, fremlaeggelse{path,blob_oid}, devil{path,blob_oid,dom:PASS}, frosset ISO) + `makeApprovalVerifier({git,pakke,evidenceRef})`: læser kvitteringen @ evidenceRef, sha256 == approval.kvittering_digest, validerer indhold, kræver fremlæggelses-/devil-blobs == `evidenceRef:path`, og RÆKKEFØLGE: `git log -1 -- kvittering` er ÆGTE forfader (`merge-base --is-ancestor`) til `git log -1 -- approval`, aldrig samme commit. Signeret runner-kvittering = deklareret residual. F-2: gate-eval kaster på sammenfaldende normaliserede stier (artefakt+bindinger). F-3: IKKE rettet i kernen — Mathias' M-40 D14 fryser JS-runtime-klassen (JSON kan ikke bære en iterator; data-nåelig? nej); kvittering.mjs læser dog egne indeks-felter. F-4: dublet-case tilføjet. `plan-gate-run.mjs`: spejl af krav-gate-run m. verifyApproval; `vaelgVerdikter` fik `gate`-param.

## Angrib
1. Pr. F-1..F-4: LUKKET / DELVIST / IKKE (fil:linje). For F-3: er D14-afgørelsen ærligt anvendt (dvs. kan vektoren nås fra committet JSON via plan-gate-run)?
2. kvittering.mjs: kan rækkefølge-beviset omgås (rebase/amend/squash der flytter kvittering og approval i samme commit — hvad ser `git log -1 -- path`? merge-commits? approval-fil oprettet i én commit og kvittering i en SENERE men på en ANDEN branch der merges? evidenceRef manipuleret til en ref hvor approval mangler? `git log -1 -- path` når filen er flyttet/omdøbt?) · kan fremlæggelses-blob-kravet omgås (path peger på en anden fil med samme indhold — er det et hul?) · devil.dom er en STRENG i kvitteringen — hvad beviser "PASS" (kun at driveren skrev PASS; devil-filens indhold læses ikke) — er det ærligt nok eller skal devil-filen selv indeholde dommen og hashes? · isPlain m. null-prototype accepteres — problem? · pakke-regex, path-checks (`..`, absolut) — traversal via symlink i repoet?
3. plan-gate-run.mjs: predecessor = runKravGate(commitSha,{root,evidenceRef}) — krav-gaten re-dømmes med evidenceRef=HEAD: kan et SENERE ændret krav-approval/verdikt påvirke plan-dommen? evidenceRef-default HEAD vs pinned commit — hvad bør det være? Fejl-håndtering, CLI-detektion.
4. Selvtestene (kan-den-fejle): plant (a) drop `merge-base --is-ancestor`-kaldet (accepter alt) · (b) sammenlign cK===cA som OK · (c) glem fremlæggelses-blob-check · (d) validateKvittering bruger `[...pd]` · (e) kernen kalder verifyApproval men ignorerer resultatet · (f) approvalReceipt: false på plan · (g) verdictDigests ikke sendt i ctx — fanges de af kvittering.selftest/gate-kerne.selftest?
5. Dom: PASS / FAIL(n). Er residual-deklarationen ærlig?

## Leverance (markdown, kort)
- Dom på én linje. Tabel | fund | lukket? | fil:linje |. Nye fund F-5… (fil:linje · klasse · repro · konsekvens · rettelse · alvor). Mutant-tabel (a-g). Kort: hvad der er rigtigt.
