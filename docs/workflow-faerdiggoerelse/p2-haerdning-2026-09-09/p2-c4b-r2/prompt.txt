Du er Codex i P2-rollen (codex-angreb) — C4b RUNDE 2: DELTA-VERIFIKATION efter din runde-1-dom (C4B-R1-FUND.md: gates/gate-eval/build-proof/ci-gate-dom PASS; ci-build-dom + pg-runner FAIL m. F-C4b-1 og F-C4b-2). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom gælder to KANDIDAT-moduler: ci-build-dom.mjs · pg-runner.mjs — plus formen af v5-build-dom.yml (to jobs). De fire PASS-moduler er byte-identiske (bekræft kort).

## Kontekst (ejerens beslutninger)
Mathias: »der går alt for langsomt«; mekanik er ikke hans bord. Kandidat-drift (pkt. 36); autoritet kræver admin + C4c. Én runde pr. fund-klasse; navngiv kun MATERIELLE huller (falsk-grønt check-run/bevis mulig), skeln fra skærpelser/residualer. Et fortjent PASS er det rigtige svar.

## Input
`DIFF-c4b-r1-til-r2.diff` · scripts/v5/ci-build-dom.mjs (+ selftest, 33) · scripts/v5/pg-runner.mjs · build-harness.integration.mjs · .github/workflows/v5-build-dom.yml (+ v5.yml, v5-gate-dom.yml) · `INTEGRATION-LOG-v2.9.txt` (79 grønne mod rigtig Postgres: tenant-kontrast · injektionsnøgle · exec uden token · credential-env afvist) · C4B-R1-FUND.md · docs/workflow-implementeringsplan.md (pkt. 39).

## Hvad der er rettet (koden er sandheden)
F-C4b-1 → TO JOBS, TO TILLIDSZONER. Job `maaling` (contents:read, INGEN checks:write, `persist-credentials: false`, Postgres-service): `ci-build-dom --produce` → producerBevis: nåethed · manifest/spec valideres · migrationer @ commit som ejer (STOP) · motoren via pg-runner · prover via runProver — ALT produktkode-nært m. RENSET miljø (`producentMiljoe(process.env)`: fjerner GITHUB_TOKEN · GH_TOKEN · GITHUB_PAT · ACTIONS_* · INPUT_* · RUNNER_TOKEN · NODE_AUTH_TOKEN · NPM_TOKEN · AWS_*/AZURE_* · GOOGLE_APPLICATION_CREDENTIALS · SUPABASE_ACCESS_TOKEN/SERVICE_ROLE_KEY · OPENAI/ANTHROPIC_API_KEY; makePgRunner og producerBevis KASTER hvis env indeholder en credential; pg-runner giver env til psql/session/exec). Output: bevis-bytes + meta (nåethed · fejl · store · run_id) som workflow-artefakt. Scriptet afviser `--emit` sammen med `--produce`, og afviser at køre `--produce` når GITHUB_TOKEN er sat i miljøet. Job `dom` (checks:write, ingen Postgres, ingen produktkode): download → `ci-build-dom --judge --meta-in --proof-in --emit` → doemBevis: bytes → JSON.parse · commit_sha/pakke i beviset == den dømte · oid = git hash-object(bytes) · forgænger plan frisk (ci-gate-dom, identitet) · snapshot m. ci-produceret artefakt · envelope (ok = body.engine.allOk) · evaluateGate(build) m. verifieren · emission. Meta m. fejl (migration STOP osv.) → rød m. målingens grund; intet bevis → rød. Check-run-summary bærer FULD artefakt-oid.
F-C4b-2 → race(): aktørens settings valideres (kun identifier[.identifier]-nøgler; værdier quoted), anvendes i BEGGE sessions i preluden, og VERIFICERES bagefter m. `select current_setting(k, true)` i begge (≠ foreskrevet → protocolOk:false); sql() samme validering. Integration: settings → begge rækker bærer tenant-b; uden → 42704; injektionsnøgle → protokol før nogen handling.

## Angrib — sidste runde på C4b
A. Isolationen: kan produktkode i `maaling` stadig påvirke `dom`? Veje: bevis-bytes og meta er produktkodens output → dom-jobbet dømmer dem m. verifieren (genudledning) — kan bytes være grønne uden ægte måling? (Det er R-CI-AUTENTICITET/målingens ophav: navngiv hvad der binder bytes til den faktiske kørsel — workflow-run-identitet, ikke indhold.) Kan job A's produktkode nå job B's token (adskilte runners/VM'er; artefakt-upload m. job A's token har kun contents:read — kan den overskrive/erstatte artefaktet job B henter? Samme navn `build-dom-<sha>`, samme run → job A ER kilden; en anden workflow-kørsel på samme sha kunne ikke skrive ind i denne runs artefakt. Vej det.) `persist-credentials: false` + intet token i env — kan produktkode alligevel få et token (GITHUB_TOKEN i runner-processens miljø? actions/checkout's midlertidige credentials? OIDC `ACTIONS_ID_TOKEN_REQUEST_*` — fjernet af producentMiljoe, men det oprindelige process.env i node-processen har dem: produktkode er UNDERPROCESSER (psql/exec/prover-cmd) — er der en vej fra underproces til forældreprocessens miljø? nej, men vej det ærligt). Migrationer kører som postgres/superuser i servicen — kan de nå runnerens filsystem (COPY TO PROGRAM · lo_export · pg_read_server_files)? Servicen er en separat container; hvad kan den nå? (Kun containeren — men notér det som R-RUNNER-UDFØRELSE/service-isolation.)
B. Settings: kan aktørkonteksten stadig forfalskes (settings der sættes men ændres af produktkode i a.sql via set_config — er det snyd eller produktets egen adfærd? Målingen er af produktet — vej)? Nøglevalidering: `set app.tenant = '…'` m. escaped værdi — er værdien sikker (quote-dobling)? Verifikationen læser `current_setting(k, true)` — kan en RAISE i produktkoden ændre hvad vidnet ser? (Verifikationen sker FØR handlingerne.)
C. Meta/bytes-kontrakten mellem jobs: kan et manglende/korrupt artefakt give grønt? (nej: rød) — bekræft. Kan `naaet:false` i meta forfalskes så et nået build ikke får check (tavshed)? Hvem skriver meta — job A's betroede måle-lag (ci-build-dom) FØR produktkode kører? (nåethed afgøres før migrationer — men skrives til disk EFTER hele kørslen; kan produktkode i samme job ændre filen inden upload? Ja — produktkode kører i samme runner-VM som filsystemet! Vej: betyder det falsk-GRØN (nej — produktkode kan gøre beviset rødt/tavst, ikke grønt uden verifierens genudledning) eller tavshed (»ikke nået« → intet check → hvis check er required = rødt).) Navngiv præcist.
Giv: dom for ci-build-dom.mjs og pg-runner.mjs (PASS/FAIL m. blob-OID) som HÆRDET TRANSPORT, dom over v5-build-dom.yml's form, fund F-C4b-n KUN for materielle huller (falsk-grøn mulig), mutant-tabel (slå jobbene sammen igen / giv måle-jobbet checks:write · fjern producentMiljoe · fjern settings-verifikationen · fjern commit/pakke-bindingen i doemBevis · ignorér meta.fejl), og navngivne residualer m. placering (R-CI-AUTENTICITET · R-RUNNER-UDFØRELSE/service-isolation · C3 · C4c · admin).
