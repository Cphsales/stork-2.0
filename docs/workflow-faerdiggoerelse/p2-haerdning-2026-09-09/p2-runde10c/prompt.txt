Du er Codex i P2-rollen (codex-angreb) — RUNDE 10c = DELTA-VERIFIKATION efter din runde-10b-dom (RUNDE10b-FUND.md: FAIL codex-run.sh @ c6a3cbd6 — fund 1: config-regex omgås af gyldig TOML; fund 2: bindings-testen skelner ikke shim fra native; G-F1 = mandat, HALT). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (+ genbekræftelse af de ni .mjs).

## Input
`DIFF-runde10b-til-runde10c.diff` (det NYE delta: codex-run.sh · binaries.lock.json · codex-run.selftest.mjs) · `DIFF-runde9-til-runde10c.diff` (hele vejen fra runde 9, inkl. haerdet-register.json = registerets runde-9-opdatering) · alle filer under scripts/v5/ · RUNDE9/10/10b-FUND · historiske artefakter under plan-build/. De ni .mjs-blobs skal være identiske med dine runde-9-PASS-entries — bekræft.

## Hvad der er rettet siden runde 10b
Fund 1 (F-3c) — værnet er ikke forbedret, det er AFSKAFFET og erstattet af isolation (vejnings-reglen: det simpleste der fuldt dækker): CLI'en læser ALDRIG brugerens ~/.codex/config.toml. Wrapperen opretter en wrapper-ejet `CODEX_HOME` i den private kørselsmappe (`$RUNDIR/codex-home`, 0700) med KUN en kopi af `auth.json` (0600) og INGEN config.toml — alle indstillinger kommer fra kaldet (`-m`, `-c model_reasoning_effort`, `-c sandbox_workspace_write.*`). Token-refresh i kopien skrives tilbage til kilden ved afslutning (`auth_tilbage` i EXIT-trap, atomisk mv). Kilden `$HOME/.codex/auth.json` skal findes og må ikke være symlink (override `STORK_V5_AUTH_SRC` kun under selftest). Miljøet til CLI'en er en ALLOWLIST via `env -i`: HOME, PATH (=verificeret node-mappe + system-PATH), LANG, CODEX_HOME, CODEX_MANAGED_PACKAGE_ROOT, CODEX_MANAGED_BY_NPM — OPENAI_BASE_URL, *_PROXY, SSL_CERT_FILE, CODEX_CA_CERTIFICATE, NODE_EXTRA_CA_CERTS, NODE_OPTIONS, OPENAI_API_KEY når aldrig CLI'en. Selvtest: den falske codex logger `envKeys` (præcis allowlisten), `cfgExists=false`, `authExists=true`, CODEX_HOME under `.run-*/codex-home`; gift-env-case; auth-refresh skrives tilbage; auth mangler/symlink → BLOKER uden start. Kvitteringen bærer `codex_home`, `codex_home_mode`, `auth_source`, `env_mode`.
Fund 2 (F-3b måling) — den falske SHIM er nu en FÆLDE: findes native-fixturen og er den startede fil ikke native (`self`), logger den starten og exiter 3 uden output. Kun native-fixturen leverer. Casen »alt pinnet → kører« kræver: rc 0, `codex_exec == native`, præcis to starter og `envs.every(self === native)`, leverancen »RESULTAT 1« findes. Mutant b (`exec` via `$CODEX`) → shim-fælden fejler → intet output → genforsøg fejler → status 1 → casen rød.
Nyt F-3d — node pinnes: `binaries.lock.json` får `node.sha256`; låsen læses som TEKST (git + grep fra system-PATH) og node's sha SKAL stå i den FØR første node-start; derefter parser node låsen og `node.sha256` sammenlignes præcist. En BLOKER før node-pin skriver en minimal kvittering med printf (status blokeret, `node_pinned=false`) — ingen node. Selvtest: lås uden node → BLOKER uden start; node-sha som løs streng andetsteds i låsen men `node.sha256` forkert → BLOKER efter parse; kvitteringens `binaries.node.sha256` == låsens.
Nyt F-3e — wrapperens EGNE node-kald: alle `NODE_*`-variabler fjernes fra wrapperens miljø ved start (`compgen -e | grep ^NODE_` → unset), så NODE_OPTIONS/NODE_PATH ikke kan ændre lås-parsing eller kvittering. Gift-env-casen sætter NODE_OPTIONS=--require /tmp/evil.js og kræver grøn kørsel.
Rækkefølge nu: lås som tekst → node-sha (tekst) → [node parser lås] → node-sha (præcis) → shim-sha → native-sha (fil, eksekverbar, kendt prefix) → `--version` på native (rc 0, én linje, == lås) → exec på native. Uændret fra 10b: sandbox-politik A5, privat rundir, flock, OUT-regler, VERDIKT-DRAFT.
Røgtestet mekanisk med den rigtige CLI 0.154.0 i det private hjem med allowlist-env: dom → »OK«; produktion → NETVAERK NEJ · TMP_SKRIV NEJ · WORKDIR_SKRIV JA; kvittering codex_exec = native, node pinnet.

## Angrib
1. Isolationen: kan CLI'en stadig nå brugerens config (andre stier CLI'en læser — `~/.codex/` via HOME? projekt-lokale `.codex/`-mapper i workdir eller dennes forældre? `CODEX_HOME`-fallbacks? `.env`-filer i cwd?), eller kan workdir'en selv (som aktøren ejer i produktion) plante en config CLI'en læser ved næste kørsel? Er `auth_tilbage` et skrivehul (kopien er aktør-skrivbar under produktion? — CODEX_HOME ligger i RUNDIR uden for workdir; sandboxen har kun workdir skrivbar — men CLI'en selv skriver den ved refresh: kan en kompromitteret CLI-session dermed skrive vilkårlige bytes til `$HOME/.codex/auth.json`? Vej det: samme trust-klasse som CLI'en selv?).
2. Allowlist: mangler CLI'en noget den bruger til at vælge server/model (fx `OPENAI_*`, `CODEX_*` udover de to)? Kan HOME (som vi giver) alene bruges til at nå `~/.codex/config.toml`? (Codex bruger CODEX_HOME hvis sat — bekræft ud fra din viden om CLI'en/binæren.)
3. Node-pin: er tekst-tjekket (grep -F på `"<sha>"`) + præcis parse tilstrækkeligt, eller findes en vej hvor en anden node (samme prefix) parser låsen og lyver? Hvad med `git` og `grep`/`sed`/`sha256sum` selv (system-PATH /usr/bin, root-ejet — deklareret residual: betroet runner)?
4. Fælden: beviser den leverede case nu at mutant b (exec via shim) dør, og at ingen shim-start sker? Er der en vej hvor native-fixturen findes men shim'en ikke fanges (fx `--version` via shim)?
5. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — inkl. `codex-run.sh` og de ni .mjs; binaries.lock.json + selvtest som inputbinding. Ved FAIL: kun fund der lader et u-pinnet program køre, lader en fremmed server svare som den pinnede model, giver netværk i produktion, lader lokal config/env styre kaldet, eller giver grøn kvittering uden alle pins (vejnings-reglen).
6. Mutant-tabel — fanges hver af disse af de LEVEREDE cases: (a) `env -i` → `env` (arv) · (b) exec via `$CODEX` · (c) CODEX_HOME=$HOME/.codex i stedet for privat · (d) skriv en config.toml i det private hjem kopieret fra brugerens · (e) fjern node-tekst-tjekket · (f) fjern NODE_*-rensningen · (g) `auth_tilbage` skriver aldrig tilbage · (h) `--version` før native-sha.
