Du er Codex i P2-rollen (codex-angreb) — C4b RED-TEAM: CI's PRODUKTIONSRUNNER + BUILD-DOMMER og det CI-PRODUCEREDE ARTEFAKT (plan 2.A/2.C/2.E · DEL V · DEL VIII pkt. 36/38/39 · GRUNDPLAN-v2 C4). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for SEKS moduler (alle KANDIDATER nu): ci-build-dom.mjs (NY) · pg-runner.mjs (NY, udtrukket) · gates.mjs · gate-eval.mjs · build-proof.mjs · ci-gate-dom.mjs — plus en dom over v5-build-dom.yml.

## Kontekst (ejerens beslutninger)
Mathias: »der går alt for langsomt«; mekanik er ikke hans bord. Kandidat-drift (pkt. 36); autoritet kræver admin + C4c (godkendelses-kanal — Mathias' valg). Dine tidligere domme: C1 r1-8 (motoren PASS, runneren F-40 lukket i v2.8), P-1 r1-2 (ci-gate-dom PASS som hærdet transport; F-C4-2 = R-CI-APPROVER-FLOW). Vejnings-reglen: én runde pr. fund-klasse; navngiv kun MATERIELLE huller (falsk-grøn check-run/bevis mulig), skeln fra skærpelser og residualer.

## Input
`DIFF-c4b.diff` (hele deltaet siden bd1f2c7: nye + ændrede moduler, selvtests, workflows) · alle filer under scripts/v5/ · .github/workflows/{v5.yml, v5-gate-dom.yml, v5-build-dom.yml} · `INTEGRATION-LOG-v2.9.txt` (72 grønne mod rigtig Postgres via pg-runner) · C1-R8-FUND.md · C4-P1-R2-FUND.md · docs/workflow-implementeringsplan.md (pkt. 39).

## Kontrakten (koden er sandheden)
CI-PRODUCERET ARTEFAKT: build-beviset committes aldrig (jobs har contents:read). gates.mjs: `artifactCiProduced: true` på build/slut; `isArtifactRef(r, gate)` accepterer {path, oid, type:"ci-produced"} KUN for de gates; bindinger er altid blobs (isRef). gate-eval.buildSnapshot: `opts.artifact` injiceres KUN for artifactCiProduced-gates, SKAL være {path == layout-stien, oid, type ci-produced}; ellers kast. build-proof.verifyBuildProof: snapshot.artifact m. type ci-produced springer git-sti-bindingen over (kernen binder proof.artifact_oid === artifact.oid; resten af verifieren er uændret v2.5 — manifest/angrebsspec fra gate-bindinger, genudledning, footprint, locus).
ci-build-dom.doemBuild: (1) nåethed = plan-approval.json + angrebs-spec.json @ commit; (2) manifest + spec @ commit valideres; (3) supabase/migrations/*.sql @ commit anvendes som ejer i sorteret rækkefølge via runner.sql — første fejl STOP → failure; (4) forgænger = ci-gate-dom.doemGates(gates:["plan"]) (pinned + identitet) → predecessor {plan, conclusion, artifact_oid = plan-blob @ commit, bindings_oids.manifest}; lukket → failure; (5) runBuildProofEngine({manifest, run_id, angrebsSpec}, pg-runner); (6) body = {schema_version, pakke, commit_sha, run_id, store{kind, migrationer_anvendt, skipped_migrations}, engine{run_id, store:"real", summary}, cases, mutants, bid_bindings (base_oid fra bids/<bid>.json, ellers commit), async_reviews (reviews/<bid>.json), prover_result (runProver m. prover.json {cmd, resultRelPath}; mangler → ok:false m. grund)}; bytes = JSON.stringify(body,null,1)+"\n"; artifactOid = git hash-object --stdin; envelope = body + {ok: engine.allOk, gate_id build, proof_kind build-proof, artifact_oid, bindings_oids @ commit}; snapshot = buildSnapshot("build", {artifact ci-produced}) + predecessor; (7) evaluateGate("build", snap, {verifyProof: makeProofVerifier}) → checkRunFromGateResult → emitCheckRuns (fra ci-gate-dom) v5/gate/build; summary bærer artefakt-oid + run_id + store. CLI: --commit --pg env|<json-argv> --emit --proof-out --skip-migrations (mærkes i beviset). ci-gate-dom: build er DELEGERET (intet check-run derfra).
pg-runner.makePgRunner({argv}): sql (json_agg for queries; frame m. psql-variable; ON_ERROR_STOP=0) · race (to sessions + tredje vidne; setup afvises; invariant-måling fail-closed) · exec (spawnSync; manglende binær → 127; stdout altid streng) · session · q1.
v5-build-dom.yml: push + dispatch · contents:read · checks:write · Postgres-service (public.ecr.aws/supabase/postgres:17.6.1.121, health-check) · PG*-env · psql-klient · selvtest før dom · `ci-build-dom --commit $GITHUB_SHA --pg env --emit --proof-out build-proof.json` · upload-artifact build-proof-<sha> (90 d).

## Angrib — vejnings-reglen
A. FALSK-GRØNT v5/gate/build: kan en producent (contents:write på branchen) få success uden at beviset er ægte? Veje: angrebs-spec.json der er svag men komplet (det er plan-gatens dom — R-PLAN-SEMANTIK; men er nåetheden (spec findes) + forgænger (plan åben) nok til at binde spec'en til plan-gaten? plan-gaten binder manifestet, IKKE angrebs-spec — R-SPEC-LEGITIMITET: navngiv præcist hvad der mangler: hooks/C4c så spec'en er Codex' og frosset før byg) · migrationer der selv ændrer værn under kørslen (migration = produktkode; motoren måler efter — kan en migration snyde footprint/kill? det er produktets eget felt — vej) · reviews/<bid>.json + bids/<bid>.json + prover.json er producent-skrevne filer (C3-residual — men kan de ALENE åbne gaten uden ægte motor-måling? nej, motoren kører altid — bekræft) · prover.json's cmd kører i CI som producentkode (runProver i detached worktree @ commit) — kan cmd skrive et falsk resultRelPath? (ja — det er R-PROVER-REGISTRERING/C4c; skeln fra hul i C4b) · --skip-migrations i CI (workflowet sætter det ikke; mærkes i beviset — skal verifieren AFVISE skipped_migrations i CI? vej).
B. Artefakt-typen: kan en ci-produceret ref misbruges på andre gates eller som binding? Kan proof.artifact_oid stemme uden at bevis-bytes er dem der blev dømt (oid beregnes AF samme proces der dømmer — er det cirkulært, og betyder det noget når CI er den betroede part og uploader bytes?)? Hvad SKAL en tredjepart genverificere fra artefaktet + check-run'et (navngiv proceduren)?
C. pg-runner som produktionsrunner mod supabase-image: rolle/ejerskab (jobbet kører psql som postgres = superuser/ejer — aktør-settings/role via `set role` pr. kald; er »ejer«-kald korrekt afgrænset fra aktør-kald?), sessions-/timeout-adfærd, psql-klientversion (apt postgresql-client vs server 17 — variabler :LAST_ERROR_* kræver psql ≥ 11), samtidige jobs på samme service (concurrency pr. ref — samme store for to pushes?).
D. Bekræft at intet i deltaet svækker tidligere PASS'er (C1 motor/verifier, P-1 ci-gate-dom): ændringerne i gates/gate-eval/build-proof er additive for type ci-produced.
Giv: dom pr. modul (PASS/FAIL m. blob-OID) for de seks + dom over v5-build-dom.yml (kan ikke køres — vurder formen), fund F-C4b-n KUN for materielle huller, mutant-tabel (fjern artifactCiProduced-afgrænsningen (tillad ci-produced overalt) · tillad ci-produced som binding · lad ci-build-dom fortsætte efter fejlet migration · fjern forgænger-kravet · sæt ok:true uanset engine.allOk · tag base_oid = commit uanset bids-fil), og navngivne residualer m. præcis placering (C3 · C4c · R-PLAN-SEMANTIK · admin). Et fortjent PASS er det rigtige svar.
