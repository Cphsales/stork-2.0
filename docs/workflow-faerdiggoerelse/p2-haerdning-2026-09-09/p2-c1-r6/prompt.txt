Du er Codex i P2-rollen (codex-angreb) — C1-RUNDE 6: DELTA-VERIFIKATION af sandheds-motoren v2.5 efter din C1-runde-5-dom (C1-R5-FUND.md: M/A/G/E/P PASS; H/V/I FAIL m. F-C1-34..36). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom gælder TO register-moduler der nu kører som KANDIDATER (blob-bundne, uden pas): build-harness.mjs · build-proof.mjs — plus integrationsrunneren (I). M/A/G/E/P er byte-identiske med runde 5 (bekræft OID'erne kort; ingen ny dom nødvendig).

## Kontekst du SKAL kende (ejerens beslutninger, ikke til genforhandling)
Mathias 2026-09-15: »der går alt for langsomt. Tjek grundigt for at vi over tester og dermed forsinker vores proces« og om mekanik: »det er ikke mit bord — så længe det kun er kode logikker«. Fabrik-armen har derfor taget VEJNINGS-BESLUTNINGER (plan 2.C v2.5 + 2.A): (1) KANDIDAT-REGISTER — registret accepterer `status: kandidat` (blob-bundet, pas afventer); lokal drift + CI's selftest-job kører m. STORK_V5_KANDIDAT_OK=1 (⚠), CI's separate register-autoritetsjob er rødt indtil dit pas; motoren er PUSHET i kandidat-drift mens du dømmer (lokal = candidate, CI = autoritet — plan DEL VIII pkt. 36). (2) prover_result's id-bijektion (executed_ids ⇔ cases+mutanter, F-19/F-26) er FJERNET som over-test: cases og mutanter er beviset for sig selv; bijektionen krævede tre ekstra C4-leverancer uden at lukke et hul case-/mutantbeviset ikke lukker; prover er et uafhængigt grønt resumé (leverede id'er valideres stadig for dubletter/fremmede). (3) claim_graph er VALGFRI i pakke 1: mutant-kill + footprint beviser at værnet findes og testes; reviewer-ankre hører til C3-verdiktet; leveres den, valideres den fuldt (locus). Du må VEJE (2) og (3) og sige ærligt om skæringen åbner et FALSK-GRØNT hul som case-/mutantbeviset ikke dækker — men du dømmer dem som beslutninger, ikke som fund: PASS/FAIL for H/V afgøres af F-34..36 og eventuelle NYE materielle huller i det der er tilbage, ikke af de fjernede krav.

## Input
`DIFF-c1-r5-til-r6.diff` (hele deltaet: H · V · I · register-selftest · v5.yml · selvtests) · alle filer under scripts/v5/ · `INTEGRATION-LOG-v2.5.txt` (56 grønne mod rigtig Postgres 17.6 + rigtig git) · C1-R5-FUND.md · B2 · forventningsliste (LÅST) · P-8 · docs/workflow-implementeringsplan.md (2.C v2.5).

## Hvad der er rettet (koden er sandheden)
F-34 → safeSql (H) validerer RÅ typer FØR normalisering: code skal være streng|null, detail objekt|null, error streng|null, rows tæt rækkesæt — ellers dead (protokol); ok:true m. fejl-detail (message/routine ≠ null) → dead; ok:true m. kode → dead (F-28); ok:false uden kode → dead. Den rene dom får `obsKaldGyldig` for observe/checkpoint/vidne ({ok, code, rows}): ok:true ⇒ code null/udeladt, ok:false ⇒ SQLSTATE-streng — en bevaret modstrid i proofens observationer er protokol, og verifieren genbruger dommen. Din tredje vej (FS observe.ok:true + code:'42601' i proofen) er lukket.
F-35 → runner.exec's stdout SKAL være en streng (manglende/null → protokol, ikke »ingen diagnose«); ALLE linjer i det reserverede format `^klasse=` tælles råt, og en linje der ikke matcher `^klasse=([A-Za-z0-9._:-]+)$` (fx efterstillet mellemrum) er protokol; komplet stdout "" m. rc 0 er stadig det legitime bortfald (brudt). Stderr-kanalen er R-RUNNER-EXIT som du navngav.
F-36 → parseErr (I, eksporteret) tager den SIDSTE `^ERROR:  XXXXX:`-linje (den sætning der fejlede — psql afbryder sætningen ved fejl), primærmeddelelsen = alle linjer til næste psql-feltpræfiks (flerlinjet bevares), routine KUN fra en CONTEXT-linje i SAMME blok. Bruges både i session() (status-SQLSTATE skal == blokkens) og dockerPsql. Prøver mod rigtig Postgres: RAISE NOTICE der forfalsker ERROR/CONTEXT-blok før virkelig 22012 (session + dockerPsql) · NOTICE-forfalskning uden fejl → status ok; kontrolleret stderr: din modprøve (to P0001-blokke) → felter fra den sidste · CONTEXT før fejlen → routine null · flerlinjet besked bevares.

## Angrib — vejnings-reglen (én runde pr. fund-KLASSE; sig PASS hvis fortjent)
A. F-34-klassen: er der en observations-/kaldform der stadig kan bære en modstrid ind i dommen (invariantRows · footprint-rækker · SA a/b detail · setup)? Kan en runner-normalisering stadig »reparere« noget til succes?
B. F-35/F-30-klassen: rest i exit-varianten (klasse-linje i stderr er R-RUNNER-EXIT; andet?).
C. F-36-klassen: kan grund/routine stadig substitueres via stderr (NOTICE/WARNING m. indlejrede linjer EFTER den virkelige ERROR-linje? — psql udskriver CONTEXT/DETAIL/HINT for fejlen, men kan produktkode få noget ud på stderr efter ERROR i samme sætning? Vej det) eller via status/diagnostik-uoverensstemmelse?
D. Skæringerne (2) og (3): åbner de et falsk-grønt hul som cases+mutanter+footprint ikke dækker? Svar ærligt, og skeln »hul« fra »mindre redundans«.
E. Kandidat-registret: kan kandidat-tilstanden misbruges til at få et gate-modul i AUTORITATIV drift uden pas (fx STORK_V5_KANDIDAT_OK sat i CI's autoritetsjob · kandidat-entry m. forkert blob · entry uden status men uden pas_ref)? Læs haerdet-register.selftest.mjs + v5.yml.
Giv: dom pr. modul (PASS/FAIL m. blob-OID) for H · V · I (+ bekræft M/A/G/E/P-blobs), fund F-C1-n KUN for materielle huller, mutant-tabel (fjern rå-typetjek i safeSql · fjern obsKaldGyldig · tillad stdout-fravær · tag første ERROR-blok · fjern samme-blok-kravet for CONTEXT · tillad kandidat uden blob-match · fjern STORK_V5_KANDIDAT_OK-kravet), og navngivne residualer. Et fortjent PASS er det rigtige svar hvis det er fortjent; et ufortjent PASS er værre end FAIL.
