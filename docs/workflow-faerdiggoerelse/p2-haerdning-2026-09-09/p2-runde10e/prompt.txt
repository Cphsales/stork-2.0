Du er Codex i P2-rollen (codex-angreb) — RUNDE 10e = DELTA-VERIFIKATION efter din runde-10d-dom (RUNDE10d-FUND.md: FAIL codex-run.sh @ f2990e07 — F-3f: tom lås-blob åbnede selvtest-grenen i produktion; F-3d målelag: mutant d (pin fra arbejdstræ) og f (»pin ELLER lås«) overlevede; G-F1 = mandat, HALT). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (+ genbekræftelse af de ni .mjs).

## Input
`DIFF-runde10d-til-runde10e.diff` (det NYE delta: codex-run.sh · codex-run.selftest.mjs) · `DIFF-runde9-til-runde10e.diff` (hele vejen fra runde 9, inkl. haerdet-register.json = registerets runde-9-opdatering) · alle filer under scripts/v5/ · RUNDE9/10c/10d-FUND · historiske artefakter under plan-build/. De ni .mjs-blobs skal være identiske med dine runde-9-PASS-entries — bekræft.

## Hvad der er rettet siden runde 10d (din angrebs-spec fulgt)
F-3f: Om binær-kontrollen KØRES afgøres nu af den eksplicitte selvtest-tilstand: `BINCHECK=1` når `SELFTEST≠1` eller `FORCE_BINCHECK=1`; kun `BINCHECK=1` læser pin/lås og binder shim/native/version; grenen uden binær-kontrol kræver `SELFTEST=1` (ellers BLOKER »intern fejl« — dobbeltsikring, kan ikke nås). En tom/whitespace-only lås-blob → BLOKER »binaries.lock.json er tom« FØR node-parse — ingen codex-start, heller ikke `--version`. Cases: tom blob · kun LF'er · kun whitespace → BLOKER, `starts.length === 0`, kvittering status blokeret.
F-3d målelag: to nye cases med den falske node (nul node-starter krævet): (d) committet pin = ægte node, ARBEJDSTRÆETS pin = den falske (ikke committet), valgt node = den falske → BLOKER (pin læses fra regel_commit); (f) committet pin = ægte, låsen får `node.sha256` = den falske → BLOKER (låsen er ingen alternativ kilde). Mutant d og f dør nu på `nodeStarts === 0`.
Grammatik-kommentaren i wrapperen præciseret efter din måling: kontrollen gælder indholdet efter bash' `$(…)`-normalisering (afsluttende LF'er/NUL fjernes); hash-bindingen er uændret præcis. Ingen kodeændring i grammatikken (b/c var redundante værn — ikke rørt).
Uændret fra 10d: node.pin som eneste node-kilde · privat CODEX_HOME + auth-kopi · allowlist-env · NODE_*-rens · native-exec + shim-fælde · A5-politik · flock/rundir/OUT/VERDIKT-DRAFT. Røgtestet igen med den rigtige CLI 0.154.0: dom »OK«; produktion NETVAERK NEJ · TMP_SKRIV NEJ · WORKDIR_SKRIV JA.

## Angrib
1. F-3f: er der NOGEN anden vej til `CODEX_EXEC="$CODEX"` eller til et codex-kald uden alle tre pins i produktion (SELFTEST=0)? Fx lås der parser men mangler felter (→ BLOKER?), lås som JSON-`null`/array/streng, native-sti der peger på shim'en selv (samme fil hashet to gange — er det et hul? shim'en er ikke native, men den ER pinnet; vurder), `--version` på en fil der ikke er native.
2. Kan `SELFTEST` sættes til 1 udefra i en produktions-kørsel (env `STORK_V5_SELFTEST=1` fra kalderen)? Ja — det er selvtest-lempelsen; men da mærkes kvitteringen `selftest=true` og afvises af verdikt-byg/transport-verifikatoren som gate-evidens. Bekræft at den kæde holder (kvittering.mjs validateTransportReceipt + verdikt-byg), så »selftest=1 udefra« aldrig kan give en grøn gate.
3. Mutant-tabel — fanges hver af disse af de LEVEREDE cases: (a) `[ "$BINCHECK" -eq 1 ]` → `[ -n "$BINLOCK" ]` (10d-regressionen genindført) · (b) fjern tom-lås-tjekket · (c) læs pin fra arbejdstræet · (d) »pin ELLER låsens node.sha256« · (e) dobbeltsikringen fjernet alene (bør IKKE kræves dræbt — redundant; sig det hvis du er enig).
4. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — inkl. `codex-run.sh` og de ni .mjs; binaries.lock.json, node.pin og selvtest som inputbinding. Ved FAIL: kun fund der lader et u-pinnet program starte, lader lokal config/env/server styre kaldet, eller giver grøn kvittering uden alle pins (vejnings-reglen).
