Du er Codex i P2-rollen (codex-angreb) — P-1 TRANSPORT-RED-TEAM af C4 »CI som dommer« FØR autoritativ ibrugtagning (plan 2.A »Autoritet = CI's friske evaluateGate emitteret som check-run v5/gate/<id>« · 2.E · DEL V · DEL VIII pkt. 36/38 · M-35 P-1). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for TO moduler: scripts/v5/ci-gate-dom.mjs (KANDIDAT) · scripts/v5/checkrun.mjs (KANDIDAT) — plus en dom over CI-adskillelsen i .github/workflows/v5.yml + v5-gate-dom.yml.

## Kontekst (ejerens beslutninger)
Mathias 2026-09-15: »der går alt for langsomt — tjek for over-test«; mekanik er ikke hans bord. Fabrik-beslutning (pkt. 36): lokal = candidate, CI = autoritet. KANDIDAT-REGISTER: haerdet-register.json kan have `status: kandidat` (blob-bundet, pas afventer); STORK_V5_KANDIDAT_OK=1 (lokal drift · CI's selftest-job) → ⚠; uden (CI's register-autoritetsjob) → rød. C4 er PUSHET og har kørt LIVE: `LIVE-CHECK-RUNS-cdddf64.json` er GitHubs faktiske check-runs på commit cdddf64 (v5/gate/recon + v5/gate/krav = success, app github-actions). Autoritet kræver repo-admin (branch protection: required v5/gate/<id> fra kilden »GitHub Actions«; ruleset på scripts/v5/** + .github/workflows/**) — det er Mathias' infrastruktur og IKKE en del af din dom, men navngiv præcist hvad det skal dække.

## Input
scripts/v5/ci-gate-dom.mjs (+ .selftest, 29 cases) · scripts/v5/checkrun.mjs (+ .selftest) · scripts/v5/gates.mjs (GATE_IDS · checkRunName · evaluateGate) · gate-eval.mjs · recon-/krav-/plan-gate-run.mjs (udfør-siderne CI kalder) · kvittering.mjs · git.mjs · haerdet-register.selftest.mjs + haerdet-register.json · .github/workflows/v5.yml + v5-gate-dom.yml · plan-build/lokations-skabelon/krav-approval.json + krav-gate-resultat.json (det rigtige kandidat-resultat CI læste pinned commit fra) · launch.json · LIVE-CHECK-RUNS-cdddf64.json · docs/workflow-implementeringsplan.md (2.A · 2.E · DEL V · DEL VIII pkt. 36/38).

## Kontrakten (koden er sandheden)
ci-gate-dom.mjs (`node scripts/v5/ci-gate-dom.mjs --commit $GITHUB_SHA --emit`): for hver gate i GATE_IDS: (1) NÅETHED — gate-specifik fil findes i den PUSHEDE commit (recon-coverage-proof · krav-approval · plan-approval · build-proof · chain-proof); ikke nået → INTET check-run (aldrig skipped=green). (2) PINNED commit: recon = pushed; krav/plan = `<gate>-gate-resultat.json`.commit_sha @ pushed, SKAL være fuld OID og forfader til/lig pushed (merge-base --is-ancestor) — mangler/ugyldig/fremmed → conclusion failure m. grund; build/slut: udfør-side ikke bygget → failure m. grund (fail-closed, ikke tavs). (3) FRISK DOM: runReconGate(pinned) · runKravGate(pinned, {evidenceRef: pushed}) · runPlanGate(pinned, {evidenceRef: pushed}); runner der kaster → failure. (4) checkRunFromGateResult(gate, result) (checkrun.mjs: kun eksplicit eget-data open===true m. tomme reasons → success; gate_id-mismatch/malformet → failure; ukendt gate → `v5/gate/invalid` failure). (5) emitCheckRuns: POST /repos/{owner}/{repo}/check-runs {name, head_sha=pushed, status completed, conclusion, output{title, summary (pinned·pushed foranstillet)}, external_id `v5:<gate>:<sha12>`} m. Bearer GITHUB_TOKEN; navn ≠ checkRunName(gate) → kast; HTTP ≠ 2xx → kast; uden token → kast før emission. Jobbet er grønt når dommeren kørte og emitterede; gaternes udfald står i check-runs. v5-gate-dom.yml: on push + workflow_dispatch · permissions contents:read checks:write · fetch-depth 0 · selvtest før dom · GITHUB_TOKEN. v5.yml: job 1 selftests m. STORK_V5_KANDIDAT_OK=1 (grønt på cdddf64) · job 2 register strengt (rødt så længe kandidater).

## Angrib — vejnings-reglen
A. FALSK-GRØN check-run: kan en producent (builder/driver m. contents:write på branchen) få `v5/gate/<id>=success` uden at gaten faktisk er åben ved den commit? Veje: commit en `<gate>-gate-resultat.json` m. commit_sha der peger på en GAMMEL commit hvor gaten VAR åben (forfader-kravet holder den i historikken — men er det nok? En gammel åben plan-gate-commit + ny plan-tekst i pushed: dømmer CI den gamle plan eller den nye? Hvad binder pinned-commit'ens artefakt til det der faktisk bygges?) · commit en approval/kvittering der får runKravGate/runPlanGate til at åbne (kvitterings-kæden dømmer digest + commit-orden — vej om CI's evidenceRef=pushed lukker »approval i samme commit som kvittering«) · manipuler launch.json (pakke) i pushed så nåetheds-stierne peger et andet sted · to workflows/jobs der emitterer samme check-navn (job-navnet i v5-gate-dom.yml deler ikke præfiks — men kan en anden workflow-fil i samme push emittere `v5/gate/plan=success`? Ja hvis contents:write på .github/workflows — det er ruleset-residualen: navngiv den præcist) · GITHUB_TOKEN-emission fra pull_request-events fra forks (permissions read-only — fejler emissionen så jobbet bliver rødt, eller bliver det stille?).
B. checkrun.mjs: mapper-fail-closed — malformet result, accessor/arvet open, reasons ikke-tomt, gate-mismatch, Proxy (deklareret residual) — bekræft eller find.
C. Nåethed/pinned-logikken: kan »ikke nået« misbruges (slet plan-approval.json i pushed → intet plan-check → hvis branch protection kræver v5/gate/plan er det rødt (godt); hvis IKKE krævet er fraværet usynligt — er det et hul i C4 eller i admin-konfigurationen?) · isAncestor m. anc===desc-kortslutning · readJson-fejl.
D. Kandidat-registret i CI: kan kandidat-tilstanden lække til autoritet (env i autoritetsjobbet · en `status: kandidat` på et modul der ALDRIG får pas · entry uden status og uden pas_ref)? v5.yml's to jobs — er adskillelsen ærlig?
E. Selvtesten (ci-gate-dom.selftest.mjs) kører i CI FØR dommen — kan en fejlende selvtest skjule en emission, eller omvendt: kan selvtesten selv emittere? (Den injicerer fetch — bekræft ingen rigtig emission.)
Giv: dom pr. modul (PASS/FAIL m. blob-OID) for ci-gate-dom.mjs og checkrun.mjs + dom over CI-adskillelsen (v5.yml/v5-gate-dom.yml), fund F-C4-n KUN for materielle huller (falsk-grøn check-run mulig), mutant-tabel (fjern forfader-kravet · fjern gate_id-match i mapperen · tillad open uden tomme reasons · emit ved ikke-nået som neutral/success · ignorer HTTP-fejl · byt evidenceRef til HEAD), og navngivne residualer m. præcis admin-handling (branch protection: hvilke checks, hvilken kilde; ruleset: hvilke stier, hvilke regler; gate-App vs GITHUB_TOKEN: hvad vindes). Et fortjent PASS er det rigtige svar.
