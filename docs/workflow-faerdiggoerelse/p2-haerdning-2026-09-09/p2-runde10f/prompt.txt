Du er Codex i P2-rollen (codex-angreb) — RUNDE 10f = DELTA-VERIFIKATION efter din runde-10e-dom (RUNDE10e-FUND.md: FAIL codex-run.sh @ a7e1ebb6 — ét fund F-3g: låsen kunne pege codex_native på shim'en selv (eller alias), som starter den rigtige native uden hash-binding; alt andet rettet med bevis; G-F1 = mandat, HALT). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (+ genbekræftelse af de ni .mjs).

## Input
`DIFF-runde10e-til-runde10f.diff` (det NYE delta: codex-run.sh · codex-run.selftest.mjs) · `DIFF-runde9-til-runde10f.diff` (hele vejen fra runde 9, inkl. haerdet-register.json = registerets runde-9-opdatering) · alle filer under scripts/v5/ · RUNDE9/10d/10e-FUND · historiske artefakter under plan-build/. De ni .mjs-blobs skal være identiske med dine runde-9-PASS-entries — bekræft.

## Hvad der er rettet siden runde 10e (din kill-liste fulgt)
F-3g — to krav til native-kandidaten, begge FØR første start: (1) lås-grammatik: `codex_native.path_from_pkg_root` SKAL matche `^/node_modules/@openai/codex-[a-z0-9-]+/vendor/[A-Za-z0-9_.-]+/bin/codex$` (platform-pakkens vendor-binær; `/bin/codex.js`, `/bin/./codex.js`, `/vendor/codex` afvises i lås-valideringen → BLOKER uden start); (2) filen SKAL være en ELF-binær (`printf '\177ELF' | cmp -s -n 4 - <fil>`, cmp fra system-PATH) → en shim-KOPI (JS m. shebang) lagt på den gyldige vendor-sti og pinnet med sin egen sha afvises: »ikke en ELF-binær«, nul starter. Shim'en (bin/codex.js) kan derfor hverken direkte, via alias eller som kopi blive `CODEX_EXEC`.
Målelag: selvtestens native-fixture er nu en ÆGTE ELF — en kopi af node-binæren på platform-stien under den falske pakkerod — kaldt `<fixture> exec …` med cwd = EXECDIR, hvor `exec.js` er den falske codex-logik (logger start m. `self = process.execPath`, env, leverer »RESULTAT k«). Låsens `codex.version` for positive cases = node's `--version` (én linje). Cases: positiv kontrol (codex_exec == native-ELF, self == native, leverance findes) · native byttet (ELF + én byte) → BLOKER, nul starter · flerlinjet `--version` (bash-kopi som ELF) → BLOKER · shim-sti / alias / fil uden for vendor → BLOKER på grammatik · shim-kopi på vendor-stien → BLOKER på ELF. `--version rc≠0`-casen er droppet: der findes ingen kontrollerbar ELF med rc≠0 uden compiler på maskinen; koden er uændret siden du eksekverede den i 10b/10c — sig til hvis du kræver den som case alligevel (så finder vi en vej).
Uændret fra 10e: BINCHECK-flag/tom-lås-BLOKER · node.pin · privat CODEX_HOME + auth-kopi · allowlist-env · NODE_*-rens · shim-fælde · A5-politik · flock/rundir/OUT/VERDIKT-DRAFT. Røgtestet igen med den rigtige CLI 0.154.0: dom »OK«; produktion NETVAERK NEJ · TMP_SKRIV NEJ · WORKDIR_SKRIV JA; kvitteringens codex_exec = den ægte native (3188814c…).

## Angrib
1. F-3g: kan en fil der passerer grammatik + ELF stadig være »et andet program« end den native codex — fx en anden ELF lagt under vendor-stien med navnet codex og pinnet i låsen (node-binæren selv?) — og i så fald: er det et hul i wrapperen eller i låsen (som er committet/reviewet — fabrik-frys)? Vej det ærligt efter vejnings-reglen: hvad er wrapperens ansvar, når låsen er en betroet, versionsbundet kilde, og hvad er låsens? Navngiv residualen præcist, hvis du lander der.
2. Rækkefølge: sker ELF- og grammatik-tjekket før `--version`? Findes der en vej hvor `cmp` fejler »stille« (fx fil kortere end 4 bytes; læserettigheder) og alligevel giver accept?
3. Mutant-tabel — fanges hver af disse af de LEVEREDE cases: (a) fjern ELF-tjekket · (b) lemp grammatikken til »ender på /bin/codex« · (c) lemp til »starter med /node_modules/@openai/« · (d) fjern begge (10e-tilstand).
4. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — inkl. `codex-run.sh` og de ni .mjs; binaries.lock.json, node.pin og selvtest som inputbinding. Ved FAIL: kun fund der lader et u-pinnet program starte, lader lokal config/env/server styre kaldet, eller giver grøn kvittering uden alle pins (vejnings-reglen).
