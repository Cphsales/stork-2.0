Du er Codex i P2-rollen (codex-angreb) — RUNDE 10h = VERIFIKATION af det bevis din runde-10g-dom krævede (RUNDE10g-FUND.md: HALT — intet nyt wrapperfund; F-3h kan først lukkes med »en læsbar kørsel bundet til OID'erne fra et miljø med fungerende ptrace: grøn original/positiv kontrol og rød targeted mutant ›native-hash efter --version‹, hvor selve tracerloggen viser den ekstra native-start; tilsvarende for de øvrige mutanter«). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (+ genbekræftelse af de ni .mjs).

## Input
`docs/workflow-faerdiggoerelse/p2-haerdning-2026-09-09/f3h-mutant-bevis/` — det krævede bevis (README m. tabel + SHA-256-manifest; `baseline/` selftest.log + 6 ptrace-logs; `mutant-{a,b,c,d,e}/` hver med `mutant.diff`, `selftest.log` og ptrace-logs for nøgle-cases; `mut-*.py` = de brugte transformationer; `rigtig-koersel/` = rigtig dom-kørsel m. CLI 0.154.0 under traceren: trace.log · OUT.md · receipt · provenance). `DIFF-runde10g-til-runde10h.diff` (delta: codex-run.selftest.mjs · exec-tracer.py — codex-run.sh UÆNDRET, blob 5102d31b siden runde 10f) · `DIFF-runde9-til-runde10h.diff` · alle filer under scripts/v5/ · RUNDE9/10f/10g-FUND · plan-build/. De ni .mjs-blobs skal være identiske med runde-9-PASS — bekræft.

## Hvad der er leveret siden runde 10g
1. Beviset (din angrebs-spec): baseline = `codex-run: 138 ok`; mutant a (native-sha efter `--version`, 3 linjer): `2 FEJLEDE` = præcis `wb` og `wg`; `mutant-a/wb-native-sha-forkert.trace.log` indeholder én start af native-fixturen med argv[1] `--version` (baseline: 0). Mutant b (ELF efter `--version`): `wg2` rød; trace viser shim-kopien startet via `env node …/vendor/…/bin/codex --version`. Mutant c (exec via shim): `w2` + positiv-kontrol rød; trace viser shim-start `bin/codex exec`. Mutant d (`"$NODE" -e ""` før node.pin): 11 røde inkl. n1's kernel-assertion; trace viser `…/bin/node -e` før BLOKER. Mutant e (tracer fjernet i selvtest-kopi): 18 røde — alle trace-afhængige positive kontroller og nul-start-cases m. `execs.length > 0`. Rigtig kørsel: 378 execve; native startet præcis 2 gange (`--version`, `exec`), begge efter `cmp -s -n 4` og `sha256sum` af samme fil.
2. Dine tre påpegninger: (i) traceren fejler nu HØJLYDT: `PTRACE_TRACEME ≠ 0` → exit 125 m. besked; ingen exec-events observeret → exit 125 (aldrig tavs tom log). (ii) Alle nul-start-assertions (`w`, `wa`, `wb`, `wd`, `we`, `wg`, `wg1`, `wg2`, tom-lås ×3) kræver nu `execs.length > 0`; `wh` kræver `argv[1] === "--version"`. (iii) Den vedlagte rigtige trace er gen-produceret med den committede tracer (escapede linjeskift; 378 EXEC-linjer, 0 ikke-EXEC-linjer) — bundet via SHA-256-manifestet i README og kvitteringen i samme mappe.
3. Selvtest: nøgle-cases (`n1`, `w`, `wb`, `w2`, `wg2`, `wg`) gemmer deres ptrace-log når `SELFTEST_TRACE_DIR` er sat (bevis-arkiv); ingen anden semantik ændret.

## Efterprøv
1. Læs beviset som du ville læse en CI-log: stemmer `mutant.diff` med den påståede mutant (og KUN den)? Stemmer selftest.log's røde assertions med det forventede kill? Viser trace-loggen den ekstra start med korrekt pid/argv — og viser baseline-loggen for samme case 0? Er manifestets SHA-256 konsistente med filerne (du kan hashe dem)? Er wrapper-blobben i `rigtig-koersel/…provenance` den samme 5102d31b?
2. Er beviset tilstrækkeligt til at lukke F-3h efter din egen spec, eller mangler en mutant/kørsel? Sig præcist hvad.
3. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — inkl. `codex-run.sh` (5102d31b) og de ni .mjs; binaries.lock.json, node.pin, exec-tracer.py, selvtest og bevis-README som inputbinding. Ved FAIL/HALT: kun fund der lader et u-pinnet program starte, lader lokal config/env/server styre kaldet, giver grøn kvittering uden alle pins, eller lader en relevant mutant overleve målelaget (vejnings-reglen).
