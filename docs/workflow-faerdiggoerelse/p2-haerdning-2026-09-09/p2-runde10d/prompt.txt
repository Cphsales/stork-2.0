Du er Codex i P2-rollen (codex-angreb) — RUNDE 10d = DELTA-VERIFIKATION efter din runde-10c-dom (RUNDE10c-FUND.md: FAIL codex-run.sh @ 31a779c9 — ét fund: F-3d tekst-medlemskab i låsen lod en fremmed node starte før den præcise sammenligning; alt andet efterprøvet OK; G-F1 = mandat, HALT). Skriv på dansk. Kun læse-adgang; skriv intet. Din dom bliver hærdet-register-entry for codex-run.sh (+ genbekræftelse af de ni .mjs).

## Input
`DIFF-runde10c-til-runde10d.diff` (det NYE delta: codex-run.sh · binaries.lock.json · codex-run.selftest.mjs · NY fil scripts/v5/node.pin) · `DIFF-runde9-til-runde10d.diff` (hele vejen fra runde 9, inkl. haerdet-register.json = registerets runde-9-opdatering) · alle filer under scripts/v5/ · RUNDE9/10b/10c-FUND · historiske artefakter under plan-build/. De ni .mjs-blobs skal være identiske med dine runde-9-PASS-entries — bekræft.

## Hvad der er rettet siden runde 10c (din angrebs-spec fulgt)
F-3d — ÉN allerede betroet mekanisme med streng tekstgrammatik: den separate fil `scripts/v5/node.pin` @ regel_commit læses via `git show` (system-PATH); HELE filens indhold skal være præcis én linje `sha256=<64 lowercase hex>` (længde 71 efter bash' strip af afsluttende newline, og `grep -cE '^sha256=[0-9a-f]{64}$'` == 1); node's sha256 (sha256sum fra system-PATH) skal være lig pin'en — ellers BLOKER FØR nogen node-start (kvittering skrives med printf, `node_pinned=false`). Tekst-medlemskab i binaries.lock.json er AFSKAFFET, og låsen har ikke længere et node-felt (én kilde, ingen dobbelt sandhed). Rækkefølge nu: node.pin → node autoriseret → lås (parses af den autoriserede node) → shim-sha → native-sha → `--version` på native → exec på native.
Målelag: selvtesten har nu en FALSK NODE i BIN (bash-script der logger hver node-start i `node.N` og exec'er den rigtige node); wrapperen finder den via PATH, og fixturen `#!/usr/bin/env node` også. `nodeStarts` måles særskilt fra Codex-starter. Din kill-liste: (1) feltbinding — pin = ægte nodes sha, valgt node = den falske, OG den falskes sha plantet i et uvedkommende låsefelt (`andet.sha256`) → BLOKER med NUL node-starter (to cases) · (2) rækkefølge — pin mangler → BLOKER, nul node-starter; fem grammatik-brud (to linjer · STORE bogstaver · hale-tekst · forkert nøgle · ekstra tegn uden newline) → BLOKER på grammatik, nul node-starter · (3) positiv kontrol — pin = selvtestens (falske) node → kører, kvitteringens `binaries.node.sha256` == pin, node-starter ≥ 1, plus alle hidtidige positive krav (native-exec, allowlist, sandbox-politik).
Uændret fra 10c (dine efterprøvninger): privat CODEX_HOME uden config + auth-kopi m. tilbageskrivning · allowlist-env (`env -i`, seks nøgler) · NODE_*-rens for wrapperens egne kald · shim-fælde · A5-politik · flock/rundir/OUT/VERDIKT-DRAFT. Røgtestet igen med den rigtige CLI 0.154.0: dom »OK«; produktion NETVAERK NEJ · TMP_SKRIV NEJ · WORKDIR_SKRIV JA; kvitteringens binaries.node == node.pin.

## Angrib
1. node.pin-grammatikken: kan en fil der passerer længde 71 + grep -cE == 1 alligevel indeholde noget andet end én sha-linje (fx CR før LF — tælles CR i længden? `$(...)` stripper kun LF; NUL-bytes; ledende BOM; to linjer hvor den ene er tom)? Er `${#NODE_PIN_TXT}` byte- eller tegnlængde under LANG/locale (multibyte-tegn i filen)? Kan `git show` levere noget andet end blob-bytes (filters/smudge/`.gitattributes`, `git replace` er slået fra via --no-replace-objects — bekræft at det gælder dette kald)?
2. Er der NOGEN node-start før autorisationen — også i `blok`-stien (printf-kvittering), i `cleanup`/`auth_tilbage` (EXIT-trap) eller i `sha_of`? Måles den af selvtesten (falsk node logger)?
3. Mutant-tabel — fanges hver af disse af de LEVEREDE cases (falsk node + `nodeStarts`): (a) node startes før node.pin-tjekket · (b) grammatik lempes til `grep -q` (ikke -c == 1) · (c) længdetjek fjernes · (d) pin læses fra arbejdstræet i stedet for regel_commit · (e) sammenligning fjernes (pin læses men bruges ikke) · (f) node-felt i låsen genindføres som alternativ kilde (»pin ELLER lås«).
4. Bekræft ved PASS at G-F1 er det ENESTE udestående og giv blob-OID pr. fil som PASS-entries — inkl. `codex-run.sh` og de ni .mjs; binaries.lock.json, node.pin og selvtest som inputbinding. Ved FAIL: kun fund der lader et u-pinnet program (node eller codex) starte, lader lokal config/env/server styre kaldet, eller giver grøn kvittering uden alle pins (vejnings-reglen).
