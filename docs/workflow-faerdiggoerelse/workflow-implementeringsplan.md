# Workflow — implementeringsplan (v5) · SAMLET

> **AFLØST 2026-09-24** af `docs/strategi/disciplin.md` og workflow-planen (`docs/workflow-plan/`, Mathias' ok M-67). Filen fjernes i oprydningen.

**Status:** ÉN samlet plan (2026-08-11). Design godkendt af Mathias 2026-06-25; herefter foldet ind: Codex-endcheck (9 fund, 2026-06-26) · gate-kerne v4 · dybde-resolution (effect-harness + config-mutant-kill). **Erstatter v1/v2/v3 — denne fil er eneste sandhed.** **Design-grøn ≠ skudsikker:** en designet-men-utestet mekanisme er selv doc-grøn (KERNEN gælder også planen); skudsikker = DEL VI bestået.

> **OVERLEVERING til frisk session (2026-08-12) — hvor vi er:**
> **Rolleteksterne (`scripts/v5/roller/*.md`) er AAA-godkendt af Mathias.** Hele det **pakke-agnostiske fabrik-fundament er BYGGET + Codex-cross-vendor-hærdet til AAA + pushet + v5-CI grøn** (branch `claude/workflow-implementeringsplan`; `npm run v5:selftest` = 446 grønne). Bygget i `scripts/v5/`: gate-kerne · verdikt · git · prover · coverage · proofs · **build-proof-verifier** · **build-harness/mutation-framework** (bevist mod rigtig Postgres; integration i `build-harness.integration.mjs`, IKKE i CI) · hooks (+`buildWriteDecision`) · driver · roller · **actors-lock** · gate-eval · **checkrun** + selftests.
> **Ærlig grænse (dokumenteret i modulerne):** Proxy + global built-in-prototype-mutation = runtime-integritets-antagelser (ikke nåelige af fabrik-DATA); de er bevidst IKKE lukket.
> **NÆSTE = FØRSTE PAKKE gennem fabrikken** — låser de sidste pakke-afhængige residualer op: angrebs-spec-legitimitet end-to-end (plan-deklareret forventet-OID) · chain-proof held-out reel data · fase-mikro-orkestrering + actor-runner + CI-som-dommer der emitterer check-runs pr. gate. **Valg af masterplan-trin = Mathias' bord** (mindst muligt trin først, for at bevise flowet ende-til-ende). **Byg intet i pakke-fasen før Mathias' ord.**

---

# DEL I — FUNDAMENT (globalt · gælder alle faser)

## Context
Byg-workflowet er **fabrikken, ikke varen** — det sikrer at hvert Stork-masterplan-trin bygges **korrekt og efter hensigten**, fri af falsk-grøn (ser færdig ud uden at virke). **Vi bygger SELVE WORKFLOWET — den pakke-agnostiske fabrik for HVER fremtidig pakke — ikke en Stork 2.0-pakke.** Bygges friskt fra designet; sund plumbing genbruges, falsk-grøn-mekanikken bygges på ny (*Genbrug — vejet*, DEL IV).

> **IKKE i scope:** Stork-produktfeatures (løn · salg · vagter · provision · RLS-domæne) hører til de pakker workflowet KØRER på — bages aldrig ind i fabrikken.

## Rygraden — én ubrudt kæde af troskab
```
vision/forretning  ⊨  krav  ⊨  plan  ⊨(1:1)  build  ⊨  sandhed (slut)
```
**⊨** = *"tro mod efter hensigten"*: hvert led skal være en sand følge af det forrige. Hvert **⊨** har **ét bevis + én dommer**. En falsk-grøn er et **⊨** der påstås uden at bevises.
- **Menneske-led** (vision→krav→plan): semantisk troskab → **Mathias + Claude.ai dømmer** ved gates.
- **plan→build:** mekanisk **1:1** → bijektion + reel slut-effekt-kør dømmer. **1:1 = al kode-skrivning står i planen; build = ren mekanisk udførelse (intet nyt besluttes) → planen er workflowets tungeste led.**
- **build→sandhed:** reel-data fuld-kæde dømmer (P5).

## Principper
P1 u-forfalskelig · P2 forskellige blinde vinkler · P3 non-LLM/menneske = dommer, LLM = input · P4 fanget falsk-grøn → regressions-test · P5 kode = sandhed ved fuld dybde.

## KERNEN — forståelse af funktionen > ord
En test/validering fanger kun sandheden hvis den bygger på **forståelse af hvad funktionen skal kunne, hvad den skal afvise, og hvordan logikken/opsætningen virker**. *"Rettigheder: ja, de er der"* er grønt på ord og misser sandheden (er `WITH CHECK` korrekt, afvises cross-org reelt). **Mekanismen er vognen; forståelsen er brændstoffet** — en test uden genuin funktions-forståelse er ord-teater (doc-grøn ≠ dybde). Forståelsen fanges i **krav-acceptkriteriet (inkl. negativer)** · **Codex' angrebs-spec** · **dybde-dommen ved plan-gaten**, og bæres ind i slut-effekt-testen. Derfor: **CI's deterministiske checks = struktur/komplethed** (dækket · mappet · til stede · ikke-fabrikeret), **IKKE sandhed**; sandheden — virker logikken/opsætningen — afgøres af **proverens reelle kør ved fuld dybde + effect-harness + mutant-kill (2.C)**, aldrig ord-tilstedeværelse.

**"Sat korrekt op" vs. "findes":** man kan aldrig mekanisk påstå *"konfig er korrekt"* — men man kan bevise **"testen går RØD hvis konfig brydes"**: effect-harness (reel handling gennem rigtig indgang mod rigtig store/ikke-bypass-rolle, observér hård slut-effekt) **+ config-mutant-kill** (mutér `WITH CHECK`/rolle/predikat → testen SKAL gå rød; en "findes"-test overlever mutanten → falsk-grøn afsløret).

## Vejnings-reglen
Hver mekanisme skal svare ja til *"tjener den et led, og ville en falsk-grøn slippe uden den?"* — ellers ud (over-test). Vi jagter et grundigt krav + et 1:1 build, ikke nul-residual via test-fæstning.

## Gennemgående regler (gælder alle led)
- **Spørg ved uklarhed — antag ALDRIG:** uklar krav/plan → aktøren **HALTER og spørger** (forretning→Mathias ved hans rytme · teknisk→ejer); bygget på antaget tolkning → FAIL.
- **HALT vs. terminal STOP:** modsigelse/uadresseret fund/divergens → **HALT** (fail-closed, bygger ikke videre, durabelt flag). Uløselig modsigelse mod krav/låste docs → **terminal STOP**. Broer mellem gates = ren transport; **dømmekraft kører aldrig af sig selv**. Mathias afbrydes ikke mid-stream — ser flag ved næste gate eller når han pull'er.
- **Anti-tavshed:** hvert gate har et forventet sæt positive, OID-bundne verdikter; manglende/timeout/forkert binding = fail-closed BLOKER. Tavshed ≠ ja.
- **Læsebevis:** et verdikt tæller kun hvis det er indholds-afledt (citat findbart i artefaktet ved bundet OID, eller aktøren hentede selv via egen kanal).

## Forbygning — forbyg > fang
Detektion (gates · prover · troskab) er **sidste** forsvar; forbygning er **første** — reducerer fejl-raten ved kilden. Hver rolle bærer to pligter: **(a) verificér + forstå INPUT** (forrige lags artefakt ved dets OID) · **(b) forbyg i eget OUTPUT** — indlejret i rolle-skillsene (2.G), håndhævet af hooks (2.E). Konkret pr. fase i DEL III. **Ingen nye gates** — forbygning er praksis, ikke mere detektion.

## Docs — sprog/form (bord-deling)
Interne AI-flade-docs (recon · plan · kontrakter · skills · attack-specs · verdikter) skrives i **aktørernes eget sprog/form**. Kun **Mathias-flade** (krav-doc · recon-præsentation · gates · masterplan-diff) er i hans sprog. Mathias læser ikke det interne.

## Mathias' flader (hård constraint)
**Code-terminalen er hans ENESTE flade** (Mathias 2026-09-02; tidligere app+terminal — ændret for nemmere kommunikation på tværs af sessioner). Forretning + med-forfatterskab af krav sker i en **NY, FRISK Code-terminal-session pr. gate i claude-ai-rollen** (Mathias starter den selv og giver rolle-ord + binding). Gate-ord (`qwers` · `krav ok` · `plan ok` · `slut ok`) fra hans mgrubak-session i terminalen. (`krav upload` som Mathias-ord BORTFALDT 2026-09-08, M-34: »ingen grund til først at godkende upload for derefter at skrive ok det man lige har godkendt« — upload er driver-automatik, Fase 2 pkt. 3; hans ene ord er `krav ok` efter ÉN fuld fremlæggelse.) GitHub-Actions kører automatisk/usynligt — **han rører ALDRIG GitHub-UI.** *Ærligt tab (navngivet):* app-projektets chat-historik indgår ikke længere i krav-konteksten; kontekst = repo-docs + recon @ OID + Mathias live i sessionen.
**ÉT Mathias-vindue pr. fase + driver som POSTKONTOR (Mathias 2026-09-03):** alle andre sessioner/aktører taler KUN til driveren — ALDRIG direkte til Mathias' vindue. Hver fremlæggelse skrives OGSÅ som durabel fil (`plan-build/<pakke>/fremlaeggelse-N.md`, committet) så vindue-tab aldrig koster den — **fil-reference ≠ fremlæggelse gælder stadig** (chatten bærer fremlæggelsen; filen bærer durabiliteten).
**Rolle-sessioner implementerer ALDRIG workflow-rettelser (Mathias 2026-09-03):** en Mathias-vendt rolle-session (claude-ai i krav/plan/slut) laver KUN sit rolle-arbejde; workflow-mekanik/plan-rettelser er driver/fabrik-byggerens bord. Rolle-sessioner modtager ændringer udelukkende via deres opdaterede rolletekst (2.G).

## Konventioner
Repo-rod `/home/mathias/stork-implplan`. Refs = git-OIDs (`git rev-parse <sha>:<path>` / tree-oid — intet custom hash). U-committet indhold kan aldrig åbne en gate.

---

# DEL II — SUBSTRAT (mekanismerne · pakke-agnostisk)

## 2.A Gate-kernen — autoritet + evaluering (v4)
**Autoritet = CI's friske `evaluateGate` emitteret som required GitHub check-run `v5/gate/<id>`**, sat af gate-App'en (`stork-code-bot` har ikke `checks:write`). **Ingen committet fil er autoritet** — committede filer er evidens/arbejds-artefakter; CI re-evaluerer altid.

**Gate-registry** (eneste sandhed · PURE · schema/constants genereres herfra):
```
recon: pred=null   evidens = machineProof(recon-coverage)                          bindings = {anker(launch), bundle}
krav:  pred=recon  evidens = actors(code, codex) + approver(mgrubak, SIDST)        bindings = {recon, anker}
plan:  pred=krav   evidens = actors(code-reviewer, codex, claude-ai) + approver    bindings = {krav, recon2}
build: pred=plan   evidens = machineProof(build-proof)                             bindings = {plan}
slut:  pred=build  evidens = machineProof(chain-proof) + approver(mgrubak)         bindings = {plan, krav, build-proof}
```
Evidens-typen er **eksplicit pr. gate** — krav/plan kræver ingen machine-proof (deres bevis ER aktør-verdikter + approval); build er ren maskine.

**`evaluateGate(gateId, snapshot)` — PURE · re-deriverer ALT · fail-closed-by-default. Åben ⟺ ALLE:**
1. Artefakt + hver binding findes ved pinned commit (oid + type fra rå git — `GateSnapshot`; ingen "live vs HEAD"-matematik).
2. **Typed bundet bevis:** proof-resultat skal matche `{gate_id · proof_kind · artifact_oid · bindings_oids}` — et generisk `{ok:true}` kan IKKE åbne.
3. **Re-verifikation DETTE run:** `verifyProof`/`verifyVerdict` køres mod rå input — committede `ok`/booleans trustes aldrig.
4. **approver-gates:** server-side-verificeret `mgrubak`, bundet til `scopeDigest(artefakt-OID + bindings-OIDs)` (anti-replay). Krav-gaten kræver **orderedApproval**: approval SKAL referere de forudgående aktør-verdikt-digests → beviser Mathias kom SIDST (krav 5).
5. **actors-gates:** præcis ét verdikt pr. forventet aktør, hvert bundet som #2, `conclusion === 'PASS'` eksplicit (FAIL/HALT blokerer); uventet/ekstra → fail.
6. **Kæde:** forgængerens check er `success` OG indholds-bundet (`predecessor.artifact_oid === bindings[pred]` — et `{open:true}` uden indholds-match åbner intet).

- **Fail-closed-by-default schema:** manglende/tomt felt = RØD, aldrig sprunget over. Digest kanonisk (sorterede nøgler).
- **oid-binding, ikke commit_sha:** bindinger = artefakt-OIDs → forgænger-gyldighed overlever nye commits når opstrøms-OID er uændret; `commit_sha` = provenance. Stale er trivielt: nyt indhold = ny OID = nedstrøms gates lukkede til gen-evaluering.
- **Irreducibel tillid (ærligt navngivet):** at gate-App'en faktisk kørte evalueringen server-side = GitHub-App/Actions-tillidsroden; org-admin (= mgrubak) er uden for workflowet.

> **Bindende lektie (2026-06-26):** gate-kernens falsk-grøn-frihed kan IKKE bevises på papir — tre uafhængige Codex-passes fandt hver gang nye huller i "færdige" versioner. Derfor bygges kernen FØRST og red-teames STRAKS (DEL VI). Flere papir-runder på kernen er stoppet.

## 2.B Verdikt-kontrakten (aktør-evidens)
`verdikt/<gate>/<aktor>.json` — schema-valid (draft-2020-12 · `required` · `additionalProperties:false` · fail-closed; CI ignorerer fri prosa uden kontrakt):
`{schema_version, gate_id, aktor, artifact_oid, bindings_oids, input_oids_read, conclusion (PASS|FAIL|HALT), negative_cases, claim_graph_refs, evidence:[{commit_sha, path, blob_oid, line_span, excerpt_sha}], run:{run_id, run_attempt, raw_output_sha256, actor_server_id}}`
- **Læsebevis REELT (path-bundet):** pr. evidens-item verificeres `git rev-parse <commit_sha>:<path> === blob_oid` FØR span-hash — blobben ligger reelt på den citerede sti i den gatede commit (stale/orphan-blob afvist); citatet findbart ved OID (`git cat-file`, ikke regex).
- **Cardinality + anti-tavshed:** præcis ét pr. forventet aktør; fuldt sæt; timeout/manglende = BLOKER.
- **Server-provenance:** CI producerer verdikterne (`actor_server_id`) — bot kan ikke fabrikere aktør-identitet.

## 2.C De tre proofs
**recon-coverage (recon-gaten):** hvert punkt i **pakke-fladen** (den `flade_filter`-filtrerede derivation, 2.D — **filter PÅKRÆVET i bundlet; fravær = rød**) gjort rede for · 3 bøtter · kode-punkter ≠ `intet_data` · **3-blind-uafhængighed bundet** (samme bundle-OID · separat workdir · læseforbuds- + web-forbuds-attest · konflikt-bevaring hård: kasseret uenighed → fail) · **omission-devil grøn på BEGGE akser** (filteret HAR en dommer): (a) `filter_angreb` — angrib `flade_filter` mod den FULDE deriverede flade: er pakke-relevante punkter udeladt? (driver-forfattet scope-krympning må aldrig stå udømt) · (b) `pakke_flade_angreb` — find misset INDEN FOR pakke-fladen; begge attesteres eksplicit i proofen (manglende akse = rød).

**build-proof (build-gaten) — MEKANISK:**
- **Bijektion:** hvert K-n → bid → test, intet rogue-trin; bundet til angrebs-spec-OID pr. bid. **Forudsætnings-bids (M-40 D12):** et bid uden egen slut-effekt (ren DDL/forudsætning) BINDER til det senere effekt-bid + dets K — validatoren tjekker afhængigheden + samlet K-dækning; "kolonnen findes"-tests er aldrig beviset (validator-implementering følger med sandheds-motoren, C-sporet).
- **effect-harness pr. K:** testen går gennem **public entrypoint** (API/RPC/UI-flow) mod **real backing store / ikke-bypass DB-rolle** og observerer **hård slut-effekt** (state/event/DB-row) — aldrig en intern helper-return. RLS/`WITH CHECK` er effekt-adfærd, ikke tekst.
- **Negativer:** hvert K-negativ udøvet som **negativ afvist m. PINNET reject-klasse, målt i samme harness-kørsel** (M-40 D11: branch/path-coverage er komplethed/sporbarhed — IKKE selvstændigt bevis; sandheden er den observerede afvisning).
- **config-mutant-kill:** for **hvert opsætnings-/konfig-/logik-K** (fra krav-acceptkriteriet): ≥1 targeted mutant DRÆBT (kill-list-eksempler: drop `WITH CHECK` · fjern tenant-predicate · flip role-check · skip state-guard · vend operator). Overlever mutanten → gate rød — en "findes"-test kan ikke overleve dette. Kill-listen skrives af **Codex** (angrebs-spec, før byg); tilstrækkeligheden **dømmes ved plan-gaten**. **MUTANT-REGLEN (M-40 D10):** én MENINGSFULD dræbt mutant pr. AFVISNINGS-acceptkriterie hvis værn alene bærer negativet — ALDRIG pr. "konfig-knap"; redundante værn skal ikke isoleret gøres nødvendige; targeted-gulvet (≥1 pr. opsætnings-K) består; bred mutation/PBT kun m. risiko-flag (DEL IV).
- **Sandheds-motoren v2 (M-41 Trin C1/C2, 2026-09-10, Codex' adapter-krav B2 som kontrakt):** forventningen kommer fra et **manifest** (`plan-build/<pakke>/forventnings-manifest.json`, `forventnings-manifest.mjs`: stabile ID'er `K-n/ac-m` · `K-n/S` · `K-n/ac-m/neg-k`, bevisformer pr. forpligtelse, alias skrevet ud, scope nu/overdragelse, `reject_contract` pr. negativ med SQLSTATE-klasse KUN 22023/P0001/P0002/42501 (eller CI-exit-klasse), `sole_guard_ref` mod deklarerede værn; bundet til forventningslistens låste blob + krav + plan) — build-proof.mjs path-binder manifestet @ gated commit og udleder den forventede mængde derfra, ALDRIG fra `proof.ks` (B2's frit pas). **Case-model** (`build-harness.mjs`): hvert atomart delbevis har præcis én bevisform hentet fra manifestet, status opfyldt/brudt/protokol-fejl (manglende observation er aldrig et udfald), typede observationer (rows/count/scalar/empty/null; `rows=[]` ≠ manglende svar) — **UT** lovligt søsterkald + forbudt forsøg afvist med NETOP den bundne SQLSTATE + tilstand uændret omkring forsøget · **FS** observation == orakel + historiske checkpoints · **MH** legitim non-admin kan handle via offentlig indgang OG sideeffekt-vidner findes · **SA** to sessions m. observeret barriere, præcis én bundet afvisning, invariant efter commit (`runner.race`). **Mutant-kill er formbestemt** (`break_form` == målets form; kontrol-cases skal forblive opfyldt; protokol-fejl/uvedkommende regression/apply-fejl/manglende restore er aldrig kill) — »positiv regression er aldrig kill« er præciseret til »målrettet MH-kill tæller«. **D10:** hvert negativ med eneste-værn kræver ≥1 dræbt mutant på netop det værn der rammer netop det negativ + K-gulv. **D11:** `negative_path_exercised` er afskaffet — negativet er en opfyldt UT-case i SAMME kørsel (`run_id`). **D12:** bids skelner forudsætning/effekt, `depends_on` acyklisk, hver forudsætning bæres af et effekt-bid, effekt-bids' `covers` = præcis den forventede mængde, hver case hører til et effekt-bid der dækker dens forpligtelse. **Bevist mod rigtig Postgres** (`build-harness.integration.mjs`: 23 cases — 22023/42501/P0001-UT, FS m. checkpoint + tomt svar, MH m. audit-vidne, ægte to-sessions-race m. række-lås observeret i `pg_stat_activity`, 7 mutanter dræbt formbestemt, »findes«-mutant overlever). Lærdom til pakken: Postgres giver EXECUTE til PUBLIC som default — en revoke fra én rolle fjerner intet uden `revoke all on all functions in schema … from public`. **v2.1 efter Codex C1-runde 1 (FAIL, 10 fund — alle lukket):** F-1 manifestet er GATE-BINDING (plan-gaten binder det, build-gaten kræver samme blob via `predecessorBindings` i gate-kernen — beviset kan ikke vælge sit manifest) · F-2 manifestet bærer NAVNGIVNE delbeviser (checkpoints · vidner · race_id) som verifieren kræver observeret, alias er eksplicit ekspansion (`{id, former}`, UT ⇒ egne negativer), deloverdragelse pr. form (`overdraget_former` + ref) · F-3 hvert delbevis afleverer RÅ observationer og verifieren GENUDLEDER status/assertions/kill/summary med samme rene funktioner som motoren (`judgeObservations` · `judgeKill`) — selvrapporterede flag tæller intet · F-4 reject-kontrakten bindes til observationen: SQLSTATE + præcist message-token (`grund`) + routine (`afvisningssted`, PL/pgSQL-CONTEXT) + aktør (DB-rolle); exit-kanal kræver struktureret `klasse=`-linje · F-5 afviste observations-/state-kald og manglende/ikke-endelige orakel-værdier er protokol-fejl · F-6 afvisning med kode uden for de anerkendte klasser (fx 42601) er protokol-fejl, aldrig brudt/kill; mutanter er MÅLRETTEDE (`target_assertion_id`) og kræver ≥1 nødvendig kontrol-case · F-7 kontroller genkøres efter restore (`cleanAfter` kræver mål + kontroller opfyldt) · F-8 SA kræver to backend-pids, barriere som blokeret/blokerende pid (`pg_blocking_pids`), afslutning commit/rollback og invariant; deklareret `setup` pr. case ved fasegrænsen (ingen runner-wrapper i måleforløbet) · F-9 `negative_id` kun på UT, D10 kræver kill på UT-casen for netop det negativ · F-10 hvert effekt-bid skal have ≥1 opfyldt case pr. dækket forpligtelse, manifestets `effekt_bid` binder bid-relationen. **v2.2 efter Codex C1-runde 2 (manifest/gates/gate-eval PASS; motor+verifier FAIL m. F-11..19 — alle lukket):** den MASKINLÆSBARE angrebs-/måle-spec (`angrebs-spec.mjs`, `plan-build/<pakke>/angrebs-spec.json`: cases m. orakler og kontrakter · mutanter m. guard/target_assertion/controls/apply/restore/**footprint** · den låste bid-graf) er build-gate-binding `angrebsspec` og valideres for komplethed mod manifestet — beviset leverer KUN observationer; orakler, kontroller, mutationer og bid-graf kan ikke vælges af buildet (F-13/F-16/F-18) · kontraktens KANAL bestemmer observationsvarianten, blandede SQL-/exit-payloads afvises (F-11) · indlejrede mutant-delresultater bindes til case-identitet + run_id + kontrakt, break_form er judgeKill's (F-12) · fase og aktør håndhæves i UT og SA (F-14) · SA uden pids/afslutning/barriere-observation er protokol-fejl, SA-kill = invariantens faktiske brud, exit-proces der ikke fungerer (rc ∉ {0, kontrakt}) er protokol-fejl (F-15) · mutation attesteres ved **footprint** (ejer-observation af det muterede objekt: baseline ≠ under, restored == baseline FØR clean-setup) og kontrolmængden er spec'ens i alle faser (F-16) · integrationsrunner: ordnet strøm (2>&1), rolleopsætning verificeret, `pg_blocking_pids` (F-17) · prover-resumé afstemmes (judgeTestSummary + executed_ids == alle cases+mutanter), claim_graph binder case/mutant til samme K (F-19). Integration 26 grønne. **v2.3 efter Codex C1-runde 3 (M/G/E/P PASS; A/H/V FAIL m. F-20..27 — alle lukket):** det FORESKREVNE forløb er spec-afledt — deklareret setup (case/race) og FS-handling SKAL findes i observationerne og være gennemført, også i indlejrede mutant-faser; race.setup udføres af motoren som ejer, ikke runneren (F-20) · footprint attesterer kun med tre VELLYKKEDE rækkesæt-målinger: fejlet/null/malformet under-måling er en manglende måling, aldrig »et andet aftryk« (F-21) · SA: overlap vidnes af et UAFHÆNGIGT tredje backend (begge sessions inde i deres skrivende transaktion samtidigt FØR nogen commit — forudsætter ikke produktlåsen); SA-KILL = to vidnede, overlappende forløb gik BEGGE igennem og committede med aktør/fase = kontraktens, og invarianten brød — manglende overlap, samme pid, inkonsistent afslutning eller ubundet afvisning er aldrig et kill; en manglende måling normaliseres ikke (F-22) · exit-kanal: rc==kontrakt UDEN struktureret klasse-linje = crash/manglende diagnose (protokol), rc==kontrakt m. anden klasse = ikke kontraktens udfald (protokol, en anden klasse kræver en låst udfaldskontrakt), rc 0 = afvisningen bortfaldt (det legitime brud) (F-23) · fase + aktør bindes i ALLE varianter (UT-sqlstate · UT-exit · SA) gennem spec-validator, observationer og verifier (F-24) · claim_graph: hvert claim binder ét VÆRN → dets dræbte mutanter → deres target-cases (⊆ claimets cases) → den navngivne target-assertion udført og opfyldt i topniveau-delbeviset → source_anchor git-verificeret OG uddraget nævner den routine/indgang casene rammer — samme K er ikke en forbindelse (F-25) · case_id og mutant_id deler ét entydigt id-rum; prover.executed_ids skal være PRÆCIS mængden uden dubletter eller fremmede id'er (F-26) · integrationsrunner: nonce-markør pr. session matchet som hel linje og krævet præcis én gang (F-27). Integrationen kører nu HELE vejen: motoren mod rigtig Postgres → manifest + angrebs-spec committet i rigtig git → validateAngrebsSpec + verifyBuildProof grøn over det ikke-redigerede bevis, K-8-mutant til K-gulvet (41 grønne). **C4 skal levere (Codex C1-r3, prover-kravet er gennemførligt):** testregister afledt af låst manifest/spec m. entydige eksekverings-id'er · friske udførelsesregistreringer (executed_ids kopieres aldrig fra forventningslisten) · afstemning mod resumé/cases/mutanter/traces fra samme kørsel · adapter fra runProver {ok, summary} til proof-kontrakten · produktionskoblingen frisk plan-dom + autoriseret, frosset angrebs-spec → build-gate (R-SPEC-LEGITIMITET/R-PREDECESSOR-WIRING). **v2.4 efter Codex C1-runde 4 (M/A/G/E/P PASS; H/V/integrationsrunner FAIL m. F-28..33 — alle lukket):** kald-udfald skal være KONSISTENTE — ok:true uden fejlkode, ok:false med SQLSTATE; {ok:true, code:'42601'} er protokol både i safeSql og i dommen (F-28) · SA-kill = et gyldigt, vidnet forløb med MINDST én committet session og HØJST én (bundet) afvisning hvor invarianten brød — den committende session kan bryde invarianten mens den anden korrekt afvises (F-29; runde 3's fire afvisningsprøver bevares) · exit-diagnosen skal være ENTYDIG: præcis én klasse-linje, flere/modstridende linjer er protokol, antallet observeres råt (F-30) · claim-ankre bindes til værnets LÅSTE locus i manifestet (`guards[].locus {path, pattern}` — plan-gatens dom): ankerets sti == locus.path og uddraget matcher mønstret; en kommentar, et andet objekt eller en anden fil er ikke værnets definition, og et værn uden locus kan ikke bære et claim (F-31; erstatter symbol-inklusion, som var for svag (kommentar) og for stærk (ACL-ankre)) · integrationsrunneren adskiller DATA (stdout) og DIAGNOSTIK (stderr): dommen ok/fejl kommer fra psql's egne statusvariable på nonce-markørlinjen, diagnostikken rammes af `\warn`-markøren, og status og diagnostik skal stemme — produktdata der ligner »ERROR: …« er bare data (F-32) · query-transporten skelner komplet tomt rækkesæt fra manglende/ugyldigt output (tomt/null/objekt = fejlet måling, rows null), og en fejlet invariant-måling gør racet til protokol-fejl (F-33). Integration v2.4: 49 grønne. Manifest-skema: `guards[].locus` er nyt, valgfrit pr. værn — men mindst ét værn med locus kræves for at build-proofens claim_graph kan bæres (v3.1-manifestet bør give locus til de værn der får mutanter). **v2.5 efter Codex C1-runde 5 (M/A/G/E/P PASS; H/V/runner FAIL m. F-34..36 — lukket) + VEJNINGS-SKÆRING (Mathias 2026-09-15: »der går alt for langsomt — tjek grundigt for at vi over-tester«):** rå typer valideres FØR normalisering (numerisk code · detail af forkert type · fejl-detail på ok-udfald = protokol) og observationskald (observe · checkpoint · vidne) skal være konsistente også i den rene dom (F-34) · exit: stdout SKAL være indsamlet som streng, malformede `klasse=`-linjer er protokol, komplet tomt stdout m. rc 0 er stadig det legitime bortfald (F-35) · diagnostik tages fra den SIDSTE ERROR-blok (den sætning der fejlede) m. hele primærmeddelelsen og CONTEXT kun fra samme blok — en NOTICE kan ikke forfalske grund/routine (F-36). **Skåret som over-test (fabrik-beslutning, vejnings-reglen):** (1) prover-afstemning på id-niveau (executed_ids ⇔ cases+mutanter) — cases og mutanter ER beviset; bijektionen krævede tre ekstra C4-leverancer uden at lukke et hul de ikke allerede lukker; prover er et uafhængigt grønt resumé (leverede id'er valideres stadig) · (2) claim_graph er VALGFRI i pakke 1 — mutant-kill + footprint beviser at værnet findes og testes; reviewer-ankre hører til C3-verdiktet; leveres den, valideres den fuldt (locus) · (3) **KANDIDAT-REGISTER:** hærdet-registret får `status: kandidat` (blob-bundet, pas afventer) — lokal drift og CI's selftest-job kører m. `STORK_V5_KANDIDAT_OK=1` (⚠, ikke rød), CI's register-autoritetsjob er rødt indtil Codex-pas; registret må ikke blokere overdragelse af kandidat-mekanik i dagevis (lokal = candidate, CI = autoritet) · (4) Codex-loop: én runde pr. FUND-KLASSE, ikke pr. fund; motor-loopet fortsætter i baggrunden (runde 6 på H/V) mens kandidat-driften pushes · (5) en Codex-CLI-opdatering er en mekanisk lås-opdatering + selvtest, ikke en red-team-runde. Integration v2.5: 56 grønne. C4-leverancelisten reduceres til: autoriseret, før-build-frosset spec + frisk plan-dom → build-gate (R-SPEC-LEGITIMITET/R-PREDECESSOR-WIRING) · runner-adapter fra runProver til proof-kontrakten · check-run-emission. **v2.6 efter Codex C1-runde 6 (V PASS → register; H/I FAIL m. F-37/F-38 — lukket):** det RÅ kald-udfald fra runneren valideres i sin HELHED (raaKaldFejl: ok · code streng|null · detail objekt|null m. message/routine streng|null · ok:true ⇒ hverken code (heller ikke '') eller fejl-detail · ok:false ⇒ SQLSTATE) FØR nogen normalisering eller projektion — også for SA-sessionerne, hvor sess() før reparerede et ugyldigt detail til null (F-37) · FLERTYDIG diagnostik er protokol: mere end én ERROR-linje eller CONTEXT-feltlinje i én sætnings stderr-segment (fejlens egne MESSAGE/DETAIL/SCHEMA-tekster kan indlejre linjer der ligner psql-felter, også EFTER den rigtige CONTEXT) gør fejlen uklassificerbar — parseren vælger aldrig mellem kandidater; en builder kan gøre sin afvisning rød, men ikke få den til at ligne kontraktens (F-38; bevist mod rigtig Postgres m. RAISE EXCEPTION USING message/detail/schema). Skæringerne (prover-bijektion · valgfri claim_graph) bekræftet af Codex som »mindre redundans, intet nyt hul« hhv. »inden for det delegerede valg«. Kandidat-registret bestod Codex' misbrugsprøver (flag · forkert blob · entry uden status/pas_ref · manglende dato). Integration v2.6: 65 grønne. **v2.7 efter Codex C1-runde 7 (H PASS → register: ALLE syv C1-moduler har nu Codex-pas; I FAIL m. F-39 — lukket):** runnerens diagnostik tages STRUKTURELT: SQLSTATE og HELE primærmeddelelsen læses fra psql's egne fejlvariable (`:ERROR :SQLSTATE :LAST_ERROR_SQLSTATE :LAST_ERROR_MESSAGE`) udskrevet mellem nonce-markører på stdout efter sætningen — libpq's felter, ikke tekstparsing; en meddelelse med indlejret »HINT:«-linje kommer hel med og kan ikke matche kontraktens token (F-39: tekstparsing kan aldrig skelne en indlejret feltlinje fra et rigtigt felt). stderr bruges kun til at tælle ERROR-linjer, krydstjekke SQLSTATE og læse afvisningsstedet (CONTEXT) — og kun når blokken er entydig (én ERROR, højst én CONTEXT, ingen CONTEXT før ERROR). Samme ramme-dommer (`frame`) i begge transporter (session + enkeltkald), enkeltkald m. ON_ERROR_STOP=0 så rammen altid udskrives. Bevist mod rigtig Postgres m. Codex' modprøve (RAISE EXCEPTION USING message=E'min_en_stand\nHINT: …'). Integration v2.7: 69 grønne. **C1-status:** motor, verifier, manifest, spec-validator, gates, gate-eval og prover er Codex-passede og i registret; integrationsrunneren (ikke gate-afhængig) får sidste runde i baggrunden; produktionsrunneren i CI (C4b) bygger på samme ramme. **v2.8 efter Codex C1-runde 8 (I FAIL m. F-40 — lukket; runde 8 var sidste planlagte på runneren):** siger psql's :ERROR succes for den aktuelle sætning mens stderr har ERROR-linje(r), er det PROTOKOL — gamle :LAST_ERROR_* er en tidligere sætnings fejl og må aldrig blive denne sætnings afvisning (F-40: en NOTICE-forfalsket blok kunne ellers hente en gammel bundet afvisning frem i en genbrugt session); et flersætnings-ejerscript m. fejl i en tidligere sætning rapporteres stadig ok:false (protokol), aldrig ok. Bevist mod rigtig Postgres m. Codex' sekvens (reel 22023 → succes → succes m. NOTICE-blok → protokol). Integration v2.8: 72 grønne. Runneren er ikke gate-afhængig; dens loop stopper her — CI's produktionsrunner (C4b) bygger på samme `frame` og red-teames dér. Navngivne runner-residualer til C4b: R-RUNNER-UDFØRELSE · R-RUNNER-LIVSCYKLUS · R-RUNNER-ATOMARITET (ON_ERROR_STOP=0 fortsætter et script efter fejl — skærpelse om transaktion/oprydning) · R-RUNNER-OVERLAP. Residual (ærlig): at kørslen skete mod en real store som ikke-bypass rolle attesteres af runner-adapteren i CI's build-job (betroet runner); proportional undtagelse fra mutant-gulvet er plan-gatens dom via manifestet.
- **claim_graph (proportional):** for høj-risiko- + sikkerheds-/penge-/rettigheds-K: `K → test → runtime-trace → source-anchor (re-verificeret mod rå git) → dræbt mutant → reviewer-claim`. Reviewer-claimet produceres ved **plan**-gaten (dommen ligger dér); **build bekræfter mekanisk** at anchoret blev EKSEKVERET og mutanten DRÆBT — claim uden eksekvering+kill = ugyldigt.
- **async_reviews:** PASS-review for ALLE forudgående bids, bundet til `base_oid` — build-gaten åbner ikke mens et sent angreb på N-1 kører; sent fund → cascade-STOP invaliderer N.
- **prover grøn** (reel kør; skipped/0-tests = rød). Bred mutation/PBT/fuzz kun ved høj-risiko-bids (planens risiko-flag).

**chain-proof (slut-gaten):** CI fetcher **held-out reel data EFTER build** (seedet af run-id; raw-source-digest + anonymiseret snapshot-digest = anti-tailoring stærkere end pre-kendt datasæt) + genererede negative canaries + metamorphic-/property-checks på kritiske invarianter + full-chain grøn ved fuld dybde (P5, alle K-n).

**Dybde — ærlig ansvarsdeling:** dybden **DESIGNES + DØMMES ved plan-gaten** (planner specificerer de dybe tests · Codex' angrebs-spec definerer mutanter/negativer · code-reviewer+codex-verdikterne dømmer at testene udøver logikken). **Build UDFØRER + BEKRÆFTER mekanisk** (eksekveret + dræbt) — ingen ny dom. Effect-harness + mutant-kill **MINDSKER** falsk-grøn-rummet mekanisk; de **TVINGER ikke** fuld dybde — resten er dømmekraft (DEL VII). *(v3's "dybde TVINGES mekanisk" var overclaim — rettet.)*

## 2.D Deterministiske dommere: prover + coverage
- `prover.mjs` — model-frit; reel kør mod **committede artefakter** (ikke fixtures); skipped/0-tests = rød. ("Kan testen fejle" sikres af negative-case-kravet + red-teamet — ingen separat liveness-selvtjek.)
- `coverage.mjs` — pakke-flade udledt **uafhængigt** (anker + statisk analyse: entrypoints/routes/RLS/migrations/config); `--check`: hvert flade-punkt gjort rede for. (Derivations-komplethed vogtes af omission-devil.)
- **Pakke-flade-filter (struktur, ikke disciplin — Mathias 2026-08-13):** den deriverede repo-flade indsnævres til **KUN det pakken berører** via `flade_filter.punkt_ids` i det OID-bundne bundle — en eksplicit, committet id-liste (driver forfatter fra ankeret; reviewbar; scopet bekræftes ved krav OK). `filterSurface` håndhæver mekanisk: **filteret er PÅKRÆVET for recon-gaten — fravær = rød** (eksplicit deklaration > default; en glemt deklaration må aldrig tavst blive til fuld-flade-støj ELLER intet krav) · malformet/ukendt id = rød (typo-værn) · dæknings-kravet i recon-coverage-proofen gælder den FILTREREDE flade. Filteret kan ikke ændres uden nyt bundle-OID → aktør-attesterne brydes synligt. **Filterets dommer = omission-devil'ens `filter_angreb`-akse (2.C)** — et for snævert filter er en falsk-grøn-kanal og står aldrig udømt.

- **Hærdet-register (Mathias 2026-09-03 — u-hærdet gate-transport må aldrig gå i drift; `haerdet-register.json` + selftest):** **Kandidat-tilstand (fabrik-beslutning 2026-09-15, pkt. 36 + vejnings-skæring):** en entry kan være `status: kandidat` (blob-bundet, `pas_afventer`, `kandidat_siden`) — lokal drift og CI's selftest-job kører m. `STORK_V5_KANDIDAT_OK=1` (⚠, ikke rød); CI's separate register-autoritetsjob er rødt indtil Codex-pas; ændret indhold er altid rødt. »Drift« i Mathias' regel betyder AUTORITATIV drift — kandidat-drift er navngivet som sådan i hvert gate-resultat. HVERT modul gate-dommen afhænger af (transitiv import fra gate-entrypoints + producenter af gatede artefakter) SKAL bære en register-entry med Codex-pas-commit + **blob-OID af den pas'ede version** — et modul ændret efter sit pas ⇒ blob-mismatch ⇒ **CI rød** indtil nyt pas + register-opdatering. Byg af build-/chain-gate-transport (Fase 4/5) tilføjer sine moduler FØR deres dom tæller.

## 2.E Enforcement — autoritet ved artefakt-grænsen (hooks = lokal UX)
**Worktrees (driver-fund 2026-09-09):** husky-shims (`.husky/_/`, gitignored) findes kun hvor `pnpm install` har kørt — i en git-worktree kører pre-commit-zone ellers IKKE mekanisk. Regel: hver fabrik-/driver-session kører `pnpm exec husky` (eller lægger den minimale shim) i sin worktree FØR første commit, og CI (`v5:selftest` inkl. register + actors-lock) er autoriteten — hooks er lokal UX. **Prettier rører aldrig blob-bundne artefakter:** `plan-build/`, `docs/sandhed/`, `launch/`, `recon/` og analyse-arkiver står i `.prettierignore` — en reformattering ville ændre blob-OID og bryde gate-bindingen.

**Platform-håndhævet (autoritet):** gate-App `checks:write`, **aldrig `contents:write`** (gate-state muterer ikke træet) · build/test-jobs `contents:read` · **måle-lag** (`scripts/v5/**` · `test/v5/**` · `.claude/**` · `.github/workflows/**`) via **rulesets/push-path-restrictions** — Code kan ikke skrive; Codex/CI ejer (*der måler ≠ der bygger*; Code må læse + køre) · required check `v5/gate/<id>` **source-locked** til gate-App'en · CODEOWNERS + branch-protection = backstop.
**Lokale hooks (friktion/UX, exit 2):** default-deny produkt-skriv før `plan-laast` · måle-lag-skriv-deny · sandhed-protect (AI skriver aldrig `docs/sandhed/` — ENESTE undtagelse: driver-flytten af krav-udkast til `docs/sandhed/krav/<pakke>-krav.md` på Mathias' `krav upload`-ord, byte-identisk, eksakt sti; Fase 2 pkt. 3) · **attack-spec-gate som state-machine:** før `plan-laast` → kun read-only tool-kald; efter → kun **driver-routede** build/prove-kald for **aktuel bid MED committet angrebs-spec**; direkte skriveveje uden om driveren → deny. Auto-fix-/"issue→PR"-makroer er ALDRIG i allowlisten (springer den gatede kæde over).

## 2.F Driver + `actors.lock` (provenance + transport)
decide/udfoer-split (genbrug `dirigent.mjs`-arkitektur, ny logik). **`actors.lock`** = låst registry `{role, provider, model, reasoning, skill_oid, allowed_tools, output_schema}` — **eksakt skill-OID, aldrig "latest"**. CI's actor-runner injicerer skill-bytes + returnerer **signeret `actor_run.json`** (faktisk model/skill/input-OIDs **+ faktisk CLI-version**) → model+skill = **provenance**, ikke CLI-config. Driver pr. fase: invokér aktør → candidate → commit → trigger CI; **auto-dispatch** (kun krav/plan/slut OK afbryder — menneske); HALT → durabelt flag; lokal fejl → fix-loop (`/loop` + `/goal` turn-cap; **prover = eneste success**); uløst → `/rewind` + eskalér. Lokal = candidate; CI = autoritativ.
**Driver som postkontor + Mathias-vendt kvalitetslag (Mathias 2026-09-03):** driveren (1) fører **mathias-ord-ledgeren** (`plan-build/<pakke>/mathias-ord.md` — append-only, tidsstemplet, VERBATIM, committet ved hvert ord; krav/plan citerer ord-id; DEL IV) · (2) kører **spørgsmåls-devilen** FØR hver Mathias-vendt besked (spørgsmål, fremlæggelse, gate-bede): en uafhængig pass (Codex/frisk agent, blind for forfatteren) tjekker bord-test + form-krav + afled-tjek **+ TROSKABS-akse (P-3, M-35): fremlæggelsen holdes mod PRÆCIS doc-blob + beviserne — tabte negativer/forbehold/udskydelser, og "kan bygges" der lyder som "virker", er fund** — fejl → omformulér før afsendelse (KPI i luk-rapporten: Mathias-korrektioner pr. fase; mål pakke 2: 0 strukturelle) · (3) verificerer at fremlæggelsen OGSÅ ligger som durabel fil (DEL I) · (4) afviser `krav upload`/plan-låsning uden **fresh-eyes-audit** (B1, Fase 2 pkt. 3) · (5) arkiverer aktør-artefakter FØR workdir-oprydning (Fase 1) · (6) kører **rent-arbejdstræ-preflight** (`decideWorktreePreflight`) + committer kun i egen **commit-zone** (`commitZoneDecision`, pre-commit-hook, 2.E).
**P-1 (M-35) — transport-red-team:** Codex angriber HELE ruten aktør-kørsel→gate (forkert aktør · gammelt resultat · afbrudt kørsel · rigtigt verdikt til forkert gate). **ÆRLIGT: kørslen VENTER på gate-transporten/CI-actor-runneren** (findes ikke endnu) — spec + angrebs-katalog skrives sammen med transporten, angrebet køres FØR transporten får dom-autoritet (hærdet-register-reglen).
**Transport-regler for aktør-kørsler (M-39 pkt. 1/3/4, 2026-09-08; `codex-run.sh` + `verdikt-byg.mjs`):** hvert codex-kald kører m. TIMEOUT + præcis ét automatisk genforsøg (ny kørsel, egen provenance, SAMME model+effort — en retry må ALDRIG tavst sænke niveauet) · output til fil (-o) + .started/.done-markører · kø-/kørselstid i provenance · timeout efter retry = MANGLENDE VERDIKT = BLOKER (aldrig fail-open). Aktør-input pakkes AUTOMATISK (`verdikt-byg.mjs`: gate/commit/artefakt som args, bindinger fra registry+layout — aldrig hånd-redigerede konstanter) og gate-kernens evidens-kontrakt (citat-scope ⊆ gated input) kontrolleres STRAKS ved aktør-levering, FØR gate-eval. Faktisk effort LOGGES i verdikt-provenance (valgfrit `run.effort`-felt — ændrer ikke pinnen, gør afvigelser synlige).
**Transport v4 — rollen bestemmer kaldet + fabrik-frys (M-41 Trin A3/A4/A5/A7, 2026-09-09, hærdet gennem 3 Codex P2-runder: FAIL(9) → FAIL(9) → v4; `codex-run.sh <rolle> <dom|produktion> <workdir> <timeout≥1> <out uden for workdir> <prompt>`):** model + effort afledes af `actors.lock[rolle]` — ALDRIG af argumenter, prompt eller lokal `~/.codex/config` (validering V-F2: mindst 10 kald kørte `high` trods xhigh-pin 2026-09-08). **Fabrik-frys FØR kaldet:** `lock.skill_oid` SKAL == `HEAD:<skill_path>` og låsen i arbejdstræet SKAL == `HEAD:actors.lock.json`, ellers BLOKER — intet aktør-kald starter på en rolle eller lås der ikke er endelig. **Sandbox efter AKTIVITET** (V-8): `dom` (angreb-dom · P-4 · devil · buildability · P-9) → `read-only`; `produktion` (recon-output · måle-lag · kill-list) → `workspace-write`. Provenance pr. kørsel: rolle · model · effort · sandbox · service_tier · codex-version · regel-commit (HEAD) · lock-blob · skill-oid · pr. forsøg rc + varighed (monotont ur OG vægur — WSL2-anomali 2083 s vs 900 s). **PID-fil** (`$OUT.pid`) → målrettede drab, ALDRIG brede `pkill`-mønstre (dræbte fremmede kørsler 2026-09-08). Separat stderr-log. Env-overrides (`STORK_V5_LOCK/REPO`) kun under `STORK_V5_SELFTEST=1` og markeres `lock=OVERRIDE(selftest)` i provenance — `verdikt-byg.mjs` AFVISER en sådan provenance (og en BLOKER-provenance) som gate-evidens. **`verdikt-byg.mjs` tager transportens provenance som 5. arg:** `run_attempt` = antal kørte forsøg, `run.effort` = det FAKTISK kørte niveau; afviger aktørens erklærede effort → SCHEMA-RØD; uden provenance (Claude-Agent-aktører) mærkes `actor_server_id` "SELV-ERKLÆRET". **Rundevalg fra eksplicit leverancehenvisning** (V-F3): `krav-gate-run.mjs` vælger verdikter via `approval._provenance.verdikt_run_ids` (præcis én fil pr. run_id blandt `verdikt-*-krav*.json` @ evidenceRef; manglende liste/dublet/0 eller >1 match → fail-closed) — aldrig et hardkodet filnavn. **Hook (A4):** rolletekst staged uden `actors.lock.json` i SAMME commit → deny (atomisk rolletekst→lock; registret håndhæves i CI, fordi et Codex-pas kræver en commit at referere). **v4-hærdning (P2 runde 2, F-1..F-13):** struktureret KVITTERING (`$OUT.receipt.json`, schema 2: status · run_id · rolle · aktivitet · model/effort/sandbox · regel_commit · lock_mode · skill_oid · prompt_sha256 · **gate_input** (fra `STORK_V5_GATE_INPUT=<gate>:<commit>:<artefakt>` — hvilken gate/commit/artefakt dommen gælder) · **selftest** (ENHVER lempelse mærker kørslen uegnet som gate-evidens) · binaries{realpath,sha256} · attempts[{rc, output_sha256, output_bytes}]); `.done` + exit 0 KUN efter publiceret leverance og skrevet kvittering · systemværktøjer fra FAST system-PATH, codex/node kun under kendte prefixer, codex-entry pinnet i `binaries.lock.json` (version + sha256, opdateres bevidst i commit når preflight opdaterer CLI'en) · flock FØR nogen fælles fil, privat kørselsmappe pr. run, OUT hverken mappe/symlink/i workdir · `--no-replace-objects` på alle betroede git-kald (wrapper + `git.mjs`) og rolleteksten hash-verificeres mod skill_oid (`git replace` kunne ellers bytte indhold bag samme OID) · **`verdikt-byg.mjs` udtrækker draften FRA leverancen** — som dens SIDSTE ikke-tomme linje `VERDIKT-DRAFT: <base64(JSON)>`, præcis én sådan linje i hele filen (også citater tæller); INGEN Markdown-parsing (P2 runde 4-8 viste at en fence-/container-parser aldrig blev falsk-grøn-fri — vejnings-reglen: det simpleste der fuldt dækker) — dommen kan ikke byttes uafhængigt af den leverede tekst, binder kvittering ↔ gate/commit/artefakt ↔ rolle/lås @ regel_commit ↔ leverance-sha, og skriver `run.receipt_sha256` · **`transportReceipt` i gate-kernen**: Codex-verdikter kræver en committet transport-kvittering i `plan-build/<pakke>/provenance/*.receipt.json` bundet til netop gaten (`makeTransportVerifier`) — en Codex-dom kan ikke konstrueres uden et Codex-kald · hook: staged lås skal parse og pege på staged rolle-blobs. Selvtests: transport 78 · kvittering 74 · kerne +17. **Deklareret residual (fabrik-beslutning 2026-09-15, se DEL VIII pkt. 36):** kvitteringen er ikke signeret af en betroet runner og codex-binæren er hash-pinnet, ikke integritets-verificeret — lokal drift hviler på git-historik + fabrik-frys (DEL I: lokal = candidate, CI = autoritet). **Transport-hærdning efter CLI-opdateringen 0.153→0.154 (2026-09-10, Codex P2 runde 10-10h, PASS @ 10h):** shim-filen `bin/codex.js` er byte-identisk på tværs af versioner, så F-3-pinnen fangede ikke CLI-skift → **F-3b** den native binær pinnes (`codex_native` i `binaries.lock.json`: sha + `path_from_pkg_root`) og KØRES DIREKTE (shim'en vælger sin native via package-exports og er ude af eksekveringsstien); rækkefølge lås → shim-sha → native-sha → `--version` (én linje, rc 0, == lås) → exec; **F-3c** CLI'en kører i en privat, wrapper-ejet `CODEX_HOME` uden config.toml med kun en kopi af auth.json (token-refresh skrives tilbage) og et allowlist-miljø (`env -i`: HOME·PATH·LANG·CODEX_HOME·CODEX_MANAGED_*) — brugerens `~/.codex/config.toml` og OPENAI_BASE_URL/proxy/CA-variabler kan ikke pege udbyderen om; **F-3d** node autoriseres af `scripts/v5/node.pin` (én linje `sha256=<hex>`, streng grammatik) med systemværktøjer FØR første node-start; **F-3e** alle `NODE_*` fjernes fra wrapperens eget miljø; **F-3f** binær-kontrollen styres af den eksplicitte selvtest-tilstand, aldrig af låsens indhold (tom lås → BLOKER); **F-3g** native SKAL være en ELF i `/node_modules/@openai/codex-<platform>/vendor/<triple>/bin/codex`; **A5** sandbox-politik eksplicit pr. kald (`network_access=false`, `exclude_slash_tmp`, `exclude_tmpdir_env_var` — lokal config havde netværk TIL i produktion); **F-3h** selvtesten måler hver programstart med ptrace (`exec-tracer.py`), mutant-bevis arkiveret. Residualer navngivet af Codex: en anden ELF pinnet i en dishonest lås (låsen er betroet, versionsbundet kilde — review bærer identiteten) · TOCTOU/bind-mount mellem hash og exec · systemværktøjer fra /usr/bin (betroet runner). Selvtest 138 (kræver python3). **Kendt ur-anomali (driver-observation 2026-09-16, A'-kvittering run_id 20260910T201232-1717260-6470):** ved WSL2-værtsdvale talte kvitteringens »monotone« ur frysningen med (varighed_mono_s 54729 = vægur), mens GNU timeout ikke udløste — de to mål er IKKE sammenlignelige efter en frysning; kvitteringens ure er indikatorer for kørslens omfang, ikke bevis for varighed, og en timeout der ikke udløser under dvale er forventet adfærd, ikke et hul. **Vejnings-skæring 2026-09-15:** en Codex-CLI-opdatering (ny version → ny native-sha) er en MEKANISK lås-opdatering (preflight + `binaries.lock.json` + `node.pin` i én commit) med selvtest — ikke en red-team-runde; runde 10-10h var over-hærdning for kandidat-drift og gentages ikke pr. version.
**Pre-flight "altid nyeste" (Mathias 2026-09-03 — struktur, ikke huskeregel; `preflight.mjs`):** FØR hver aktør-kørsel tjekker driveren transporten mod frisk opslag (Codex: installeret CLI vs `npm view @openai/codex version` · Code: `claude update`-status). **Grænsen:** "nyeste CLI" = TRANSPORT — bagud → opdatér FØR spawn, fejler opdateringen → HALT med besked (`decideCliPreflight`; aldrig nedgradering, malformet opslag = halt). "Nyeste MODEL" = AKTØR-IDENTITET/provenance — pinned model SKAL være tilgængelig for kontoen (ellers HALT); nye u-vurderede modeller → **flag til Mathias** (`decideModelPreflight`; skift er HANS ord + actors.lock-opdatering m. `vurderede`-liste, ALDRIG automatik — gpt-5.6 var fx afvist af kontoen). Faktisk brugt CLI-version + model logges i `actor_run`-provenance, så et gate-spor altid viser hvad der kørte.

## 2.G Roller → verificerbare output-kontrakter
Hver rolle-skill kompilerer til en output-kontrakt (CI ignorerer fri prosa). **Alle:** forstå-før-producér · antag-aldrig → HALT · byg fra committet OID, ikke hukommelse. **Mandate-canary pr. rolle** i red-teamet (DEL VI). **Rolle-sessioner implementerer ALDRIG workflow-rettelser (Mathias 2026-09-03):** mekanik/plan/rolletekst-ændringer er driver/fabrik-byggerens bord; en rolle-session modtager ændringer UDELUKKENDE via sin opdaterede rolletekst (ny session = ny tekst; aldrig via chat-instrukser om at bygge mekanik). **P-6 (M-35): hver rolletekst-ÆNDRING får et Codex-pas for producent/aftager/timing/output-konsistens på tværs af rolleteksterne** (roller.selftest tjekker kun felter, ikke tekst-semantik — det er hullet; P-6 fandt+lukkede kill-list-timing-modstriden planner vs codex-angreb/code-reviewer).
- **code:** tre FRISKE sessioner — **recon-Code → planner-Code → builder-Code** (aldrig samme session to stadier). Builder: design fejlen UD (umulighed > korrekthed) · 1:1 · skriver aldrig måle-lag.
- **code-reviewer:** frisk ≠ byggeren; dybde-inspektion → `claim_graph_refs` (kun gyldig ved eksekveret + dræbt).
- **codex/ANGREB:** falsk-grøn-jagt, dybde intrinsisk (overfladisk test = falsk-grøn); ejer måle-laget; cross-vendor (**gpt-6-astra xhigh** — Mathias' model-ord 2026-09-08, M-31; pin i `actors.lock`, altid EKSPLICIT `-m`/`-c` i kald, aldrig implicit lokal config); **ingen web**.
- **codex/FORBEDRING:** bedre alternativer (test/forbyg/dybde); **web TILLADT**; rådgivende — ingen gate; separat agent.
- **claude-ai:** forretnings-mening mod **låste** docs; med-forfatter af krav; ingen kode; kanal = frisk Code-terminal-session pr. gate i claude-ai-rollen (Mathias 2026-09-02; før: app).
- **recon-roller:** friske, blinde, separat workdir, **web FORBUDT**, evidens-trace pr. fund.

## 2.H `test-led`-skill (rygradens motor, anvendt pr. ⊨)
Givet led X⊨Y → producér **falsificerbar test/probe + citeret evidens**, aldrig en bar "✓". Deterministisk led → skriv/kør den eksekverbare check (**kør'en dømmer**). Semantisk led → citerede drift-fund (**Mathias/Claude.ai dømmer**). Aktøren er **input, aldrig dommer** (P3).

---

# DEL III — KÆDEN, FASE FOR FASE

## Fase 0 — Åbning
`qwers <anker>` (Mathias, terminal; author-bundet = mgrubak) → driver committer `launch/launch.json {anker, anker_sha, pakke, author}` (`PAKKE_RE=/^[a-z][a-z0-9-]*$/` — anti-traversal). **Ingen gate** — mindste tvivl om anker/scope → spørg Mathias; ellers starter Fase 1 automatisk. Ikke-bestilt anker kan ikke passere kæden (scopet bekræftes ved krav OK).

## Fase 1 — Recon → "hvad pakken berører" (føder krav)
Driveren bygger + committer **pakke-kontekst-bundlet** (anker + refererede docs + låste vision/forretning **+ `flade_filter`** = eksplicit pakke-flade-deklaration, 2.D) + udleder pakke-fladen (`coverage --derive` → `filterSurface`, 2.D). **Recon dækker KUN pakkens flade** — dæknings-kravet er den filtrerede flade, ikke hele repoet (Mathias 2026-08-13; aktør-dybden var altid pakke-scoped — det er KRAVET der er pakke-scoped som håndhævet struktur). **Tre friske, blinde aktører** (recon-Code · Codex · Claude.ai — separat workdir · læseforbud mod hinandens output · **web FORBUDT**) → hver sit candidate → `consolidate-recon` → ÉN OID-bundet `recon.md` (3 bøtter; **konflikt-bevaring HÅRD:** divergenser bevares med aktør-mærke, kun ægte dubletter dedupes, kasseret uenighed → BLOKER) → **omission-devil** (Codex, én pass; loop kun ved fund) — **to eksplicitte akser** (2.C): (a) angrib `flade_filter` mod den fulde deriverede flade (pakke-relevant punkt udeladt af filteret?) · (b) find misset inden for pakke-fladen. **P-5 (M-35): + DOKUMENT-akse** — bundlets kilde-afsnit holdes mod claude-ai-aktørens flade-enumeration: oversprungne udsagn uden kode-aftryk = fund (proof-akse-kravet aktiveres fra næste recon-gate; kørt retroaktivt på denne pakke).
**P-2 (M-35) — divergens-efterprøvning efter konsolidering:** en frisk Codex efterprøver de TEKNISKE recon-divergenser mod de bundne kilder → bilag (evidens · probe · rest-usikkerhed); kun forretnings-forks går videre til Mathias-fladen. (Kørt retroaktivt på denne pakkes 77 bevarede divergenser.)
**Bundle-felter (byggevejen — næste pakkes driver bygger fra DENNE spec, kopierer aldrig forrige pakkes artefakt):** `{pakke, launch_commit, anker{path,oid,punkter}, laaste_docs[{path,oid,rolle}], kode_flade, regler, flade_filter{note,punkt_ids}}`. **INTET `tree_sha`-felt** (første pakkes bundle bar et — commit-OID under tree-navn = feltet løj; commit-pin hedder `launch_commit`, og deriveSurface pinnes altid ved gate-commit'en, aldrig ved et bundle-felt).
**Arkiverings-regel (provenance — lærdom fra første pakke):** aktør-kandidater + devil-output committes som arbejds-spor (`recon/spor/`) **FØR** workdirs/worktrees ryddes — oprydning uden arkivering = provenance-tab (kandidat-niveau-reproduktion umulig). Arbejds-spor slettes ved lukke-PR (Fase 6); git-historikken bevarer dem.
**Aktør-sandbox for ALLE tre (B4, Mathias 2026-09-03):** recon-Claude.ai køres som separat proces i **netværksløs worktree** som Codex — attesterne (workdir/web/læseforbud) bliver mekanisk sande, ikke erklærede. (Første pakke kørte claude-ai-aktøren som docs-only-dir med erklærede attester — ærligt navngivet residual, lukket fra pakke 2.)
**Pakke-ordbog (B3, Mathias 2026-09-03):** recon-Claude.ai høster Mathias' begreber fra de låste docs → `plan-build/<pakke>/ordbog.md` (Mathias' ord ↔ systemord, fx gruppe=leverandør · stand=placement). Al Mathias-flade bruger HANS ord; planen/koden ARVER navnene (eller mapper eksplicit i ordbogen — en afvigelse uden ordbogs-entry er en FAIL ved plan-gaten).
**Gate `recon`** = machineProof `recon-coverage` (2.C). Doc: `recon.md` (AI-intern).
**Aftager-kæden definerer leverance-formen (struktur, ikke huskeregel — Mathias 2026-08-13):** recon-1's aftager er **krav-dokken** (HVAD pakken skal kunne — Mathias' bord via 3-bøtte-præsentationen) → `recon.md` organiseres som KRAV-føde: 3 bøtter, forretnings-oversætbart, hvert fund klar til disposition (behandlet/udskudt/ikke-relevant); GØR/AFVISER + negativer er krav-føde (→ acceptkriteriernes slut-effekt inkl. negativer). Mutation-/test-dybde-materiale er IKKE krav-føde — det gemmes som bilag/spor og konsumeres af **recon-2, hvis aftager er plan-dokken** (HVORDAN — kill-lists/angrebs-spec dømmes ved plan-gaten, Fase 3/4). Intet kasseres; det ligger blot hos sin aftager.
*Forbygning:* (a) forstå HELE bundlet ved OID; (b) kortlæg hele scope (ikke første-fund) · evidens-trace pr. fund · spørg v. uklarhed. **Fundamentet — fejl her forplanter sig nedstrøms.**

## Fase 2 — Krav (vision/forretning ⊨ krav) — Mathias godkender SIDST
1. **Handover:** Mathias starter en NY, FRISK Code-terminal-session i **claude-ai-rollen** (rolle-ord + binding: recon-commit + `recon/recon.md`). Sessionen læser recon DIREKTE ved OID fra git (stærkere læsebevis end app-sync; Mathias 2026-09-02). **Handover-HALT uændret:** binding verificeres FØR krav — stale/forkert/overfladisk → HALT (recon er committet/durabel). Sessionen **konsumerer** recon-dybden, tilføjer den ikke. Kontekst = repo-docs + recon @ OID + Mathias live (app-chat-historik indgår ikke — ærligt tab, DEL I).
2. **3-bøtte-præsentation:** nuværende kode ("x bygget sådan — korrekt?") / ikke-bygget/dokument ("dok y siger — korrekt?") / intet-data ("hvad skal x kunne?"). **Hvert berørt bord-område SKAL præsenteres** (udækket → FAIL); kun pakke-relevant (ingen støj). **Afled-før-spørg (Mathias 2026-09-02):** bøtte 3-spørgsmål forudgås af opslag i hans nedskrevne sandhed (låste docs + masterplan-afgørelser + kode) → afledte svar præsenteres til bekræftelse m. citat ("din sandhed siger X → foreslået Y — korrekt?"); kun ægte ubesvarede punkter stilles åbent. Afledt svar = forslag; hans bekræftelse = sandheden; afledningskilder inkl. mønster-analogi. Mathias dømmer forretning, ikke kode — håndhævet af **bord-testen** pr. spørgsmål: (a) kan kun Mathias svare? (b) kan han svare uden teknik-viden? (c) **materialitet (M-33):** ændrer svaret hvad der bygges i DETTE trin og skal afgøres NU? — ellers nedstrøms-liste m. eksplicit default. Fejler ét → ikke et krav-spørgsmål; **ét-skridts-reglen (M-33):** ét-skridts-konsekvens af hans ordrette ord = batchet BEKRÆFTELSE, aldrig spørgsmål (intet loft/takt — værn 3 afvist af Mathias); teknik-/model-forks noteres eksplicit som plan-fase-afgørelser (planner afgør i kravets ramme · Codex angriber · plan OK dækker) — flytter bord synligt, forsvinder aldrig tavst. Spørgsmål der består, stilles i **skarp form** (rolletekstens form-krav): én beslutning pr. spørgsmål (aldrig parentes-haler) · scenarie-form m. navngivne klienter/steder (aldrig meta-sprog) · svarbart med ét ord (udfald givet + anbefaling) · værdi-spørgsmål (enhed/længde/sats) = UI-konfig → bortfald; "forstår ikke" → omformulér, aldrig videre uden svar. **Spørgsmåls-devil (A1, Mathias 2026-09-03):** FØR hver Mathias-vendt besked (spørgsmål · fremlæggelse · gate-bede) kører driveren en uafhængig pass (bord-test + form-krav + afled-tjek); fejl → omformulér før afsendelse (2.F). Mathias-flade-sproget følger **pakke-ordbogen** (`plan-build/<pakke>/ordbog.md`, Fase 1/B3).
3. **Krav skrives:** Mathias + claude-ai-sessionen → hvert **K-n: HVAD** (forretnings-sprog, ingen kode) + **acceptkriterie = slut-effekt INKL. negativer**; bundet til recon-OID **+ citeret mathias-ord-id pr. K** (ledgeren, DEL IV); **hvert recon-fund disponeret** (behandlet/udskudt/ikke-relevant). Sessionen skriver KUN **udkast**: `plan-build/<pakke>/krav-udkast.md` (AI-zone — sandhed-protect slækkes ALDRIG generelt). **Fresh-eyes-audit FØR upload (B1, Mathias 2026-09-03):** en FRISK claude-ai-instans (blind for forfatter-sessionen) auditerer udkastet for **ubekræftede afledninger** (antag-aldrig-audit) → committet audit-fil `plan-build/<pakke>/antag-aldrig-audit.md`; driveren AFVISER upload uden den (mandatet bærer audit-blob-OID — hook-håndhævet). **Audit-input (M-40 E17):** BÅDE fresh-eyes og P-4 modtager komplette versionsbundne kilder + diff GENERERET FRA DE FAKTISKE BLOBS (aldrig hånd-staged patch — to staging-fejl i pakke 1 var undgåelige). **Audit-fund klassificeres (M-33) FØR de når nogen:** bekræftelse (ét-skridts-afledning af M-ord → batchet i fremlæggelsen) vs ægte spørgsmål (→ spørgsmåls-devil → Mathias). **P-4 (M-35): auditen suppleres af et Codex-KILDETJEK** med komplet versionsbundet kildegrundlag — hver påstand klassificeret {direkte belæg · deklareret afledning · modstrid · mangler kilde} — før upload OG før plan-lås. **ÉT delta-batch pr. runde (M-39 pkt. 2):** kildetjek (P-4) og buildability kører PARALLELT på SAMME blob; ALLE fund foldes ind i ÉN ny version → ét audit-addendum → én upload → én re-bind — og der bedes ALDRIG om `krav ok` mens et tjek stadig er åbent (M-37-situationen må ikke gentages).
**Upload = driver-AUTOMATIK (M-34, 2026-09-08 — intet Mathias-ord):** når udkastet er committet OG fresh-eyes-auditen af PRÆCIS den blob er REN, flytter driveren udkastet **byte-identisk** til `docs/sandhed/krav/<pakke>-krav.md` + committer — committet krav-blob-OID SKAL == udkast-blob-OID (flyt, aldrig redigér; afvigelse → HALT). Hook-mandatet er uændret (kun denne sti · udkast-OID + audit-OID · byte-identisk). **Upload ≠ krav OK — intet er sandt før hans ok.** Rækkefølgen herfra: upload → buildability (pkt. 4) → **ÉN fuld fremlæggelse** (hele doc'en i hans sprog + afledninger-til-bekræftelse + ændret-siden-sidst + buildability-resultatet) → hans ENE `krav ok` (pkt. 5, orderedApproval uændret). Buildability-krævet nyt udkast → samme loop, stadig én fremlæggelse pr. runde. (Historik: 2026-09-02-modellen havde `krav upload` som Mathias-ord — afskaffet ved M-34.)
4. **Buildability:** Code + Codex afgiver verdikt (2.B) ad ÉN akse — *kan det kodes? er der huller?* — IKKE forretnings-merit. De **retter ALDRIG krav** — mangler → spørgsmål/forslag til Mathias → **nyt upload = ny runde**. **Citat-scope (M-40 E16):** verdikt-citater KUN fra gated input (krav-doc · recon · anker); kode-citater hører til analyse-sporet. Mathias-ord-citat-audit ejes af P-4 (kildetjekket), ikke buildability. Ét FULDT pas pr. substans-version; delta-kørsel ved ren ordlyds-diff (M-39 pkt. 2).
5. **krav OK = Mathias SIDST** (krav 5): `krav ok` i terminalen (mgrubak-autoritet). **Hændelseskæde (M-41 princip 9, validering V-F1):** endelige verdikter → fremlæggelse → REN devil på PRÆCIS teksten → frys + KVITTERING (verdikt-digests · fremlæggelses-blob · tidsstempel) → registrér afsendelse → bind `krav ok` til kvitteringen; nye verdikter kan ikke bagefter blive dens forudsætning (M-38 15:06 lå FØR r4b 15:10 — rækkefølgen var ikke bevist, substansen var uændret). **Præ-betingelse på menneskesiden (Mathias 2026-09-02):** ok må kun bedes om efter FULD fremlæggelse af den komplette krav-doc i chatten, overskueligt og i hans sprog (formål · pr. K én linje HVAD + vigtigste negativ · UI-styret vs. hardkodet · ikke-i-scope · dispositioner · buildability-resultat); fil-reference ≠ fremlæggelse; nyt upload → ny fremlæggelse. **Fremlæggelser/luk-rapport citerer KUN mekanisk verificerede tal (M-40 E19)** — `negative_cases`-lister og attest-booleans er aktør-ORD, ikke bevis; tallene der nævnes skal komme fra kørte verifikationer. orderedApproval-mekanikken uændret. **orderedApproval** binder til uændret krav-OID + refererer de forudgående verdikt-digests → rækkefølgen er bevist, ikke påstået. Krav **immutabelt** efter OK.

**Gate `krav`** = actors(code, codex) + approver(mgrubak, sidst). *Bevis:* test-led — Claude.ai (forretnings-mening mod låste docs; med-forfatterskabet ER pre-upload-trinnet). *Dommer:* Mathias.
**Krav-template** (`docs/sandhed/krav/<pakke>-krav.md` · Mathias-flade):
```
# <pakke> — krav-og-data   (Status: UDKAST|krav-laast · recon_oid · anker)
## Formål — hvad pakken leverer
## Krav — K-n: HVAD (forretnings-sprog, ingen kode) · Acceptkriterie (slut-effekt INKL. negativer) · pr. K markeret: UMULIGT (struktur, systemet afviser) vs. KONFIGURERBART (værdi, UI-styret)
## Recon-fund-dispositioner | flade_punkt | bøtte | disposition | krav-ref |
## IKKE i scope   ## Holdt mod låste docs
```
Regler: K-n uden acceptkriterie → FAIL · hvert recon-fund disponeret · kun HVAD.

## Fase 3 — Plan (krav ⊨ plan)
1. **recon-2:** frisk recon-Code + Codex mod krav-OID; genbruger recon-1 og **uddyber krav-drevet** (hvordan koden/opsætningen virker dér kravet skal bygges · mønstre · constraints · eksisterende tests). Web forbudt. Committet `recon2`-OID.
2. **planner-Code (FRISK — ≠ recon, ≠ builder)** skriver planen: **krav-ID-matrix** (bijektion) · **bid-opdeling** (afhængigheds-ordnet · prover-bevisbar størrelse · angrebs-spec-krav + risiko-flag pr. bid) · **design fejl-klasser UD** (constraints/types/RLS — umulighed > korrekthed) · repo-doc-tekst 1:1 · **de dybe tests specificeres HER** (effect-harness-form + kill-list pr. K).
3. **Codex angriber** — i M-41-formen (GRUNDPLAN-v2 Trin B, princip 2/4): **(B1) codex-angreb skriver sit EGET kill-list-udkast BLINDT** fra låst krav + recon-2 (aldrig plannerens — plan v1's kill-list var plannerens, plan.md:24), committet FØR planneren ser det (commit-orden = blindheds-bevis); recon-2's tjekliste er FÆLLES kontrolgrundlag, ikke katalog. **Første angrebs-pas = katalog-opslag + ét FRIT helheds-pas** (kan to lokalt rigtige dele tilsammen bryde et krav?). Angreb ∥ fresh-eyes ∥ P-4 på SAMME plan-blob → ÉN fold-ind (delta-batch, M-39) → **runde 2 = delta-verifikation på rettelserne OG de krav, negativer og afhængigheder de berører** — ikke kun diff-linjerne — plus code-reviewer som FRISK slutlæser af helteksten uden producentens lukningsliste. »Iteration til angrebet er tømt« er undtagelsen (logges), ikke default. Hvert fund ender som rettet m. bevis / inden for delegeret mandat / kræver Mathias (princip 6 — »deklareret«, »residual«, »planvalg« lukker intet). Krav-problem → retter IKKE krav → retur til Mathias. **For lokations-skabelon (plan v1 allerede skrevet og set):** katalog + samlet angreb + frisk slutlæser bruges i runde 2; det påstås ikke bagefter at forløbet var blindt fra start.
3a2note. **P-8-beskæring er PLAN-GATENS dom (M-40 E20):** samtidighed skæres til de K-navngivne races (fx sidste-stand · dublet-kobling) · permutationer én kontrolkopi · fuld-populations-import + bijektion + canaries + kæde-forløbet BEHOLDES — planner + codex-angreb får dette som input, gate-aktørerne dømmer snittet.
3a2. **P-8 (M-35) — slutprøve-spec FØR plan-lås:** Codex leverer spec for slutprøvens data-udvælgelse · anonymisering · negative kontroltilfælde · kæde-forløb på tværs af bids — og ANGRIBER udvælgelse der frasorterer svære rækker; CI vælger held-out data efter build (Fase 5) inden for spec'en.
3a3. **Forventningsliste FØR plan-lås (M-41 Trin B2, validering V-5/F-7):** én låst matrix pr. K · acceptkriterie · strukturforbud · negativ · **bevisform** (ulovlig tilladelse · forkert slutværdi · manglende offentlig handling · samtidighed) · effekt-bid · kildeankre — bruges til BÅDE at producere og kontrollere byggebeviset (K-sættet må aldrig komme fra beviset selv). P-8 afstemmes ind i samme matrix; Codex viser at måle-adapteren kan udtrykke de fire udfald; kodeinspektion erstatter aldrig en to-sessions-prøve.
3b. **Mathias-vendt kvalitet i plan-fasen (Mathias 2026-09-03):** al plan-gate-dialog med Mathias passerer **spørgsmåls-devilen** (2.F) og bruger **pakke-ordbogen** (planen arver Mathias' navne eller mapper eksplicit — umappet afvigelse = FAIL ved gaten); **plan-låsning kræver fresh-eyes-audit** (B1 — frisk instans lister ubekræftede afledninger i plan-udkastet FØR plan OK bedes om) **+ P-4-kildetjek m. komplet versionsbundet kildegrundlag; begge følger princip 6 (beskriv ≠ luk): et fund er lukket KUN som rettet m. bevis / inden for delegeret mandat (citeret) / kræver Mathias — og efter fold-ind dømmes HELTEKSTEN mod den oprindelige K-forpligtelse (en tydeligt mærket indsnævring er stadig en indsnævring).**
4. **Gate `plan`** = actors(code-reviewer, codex, claude-ai) + approver(mgrubak). **Bindinger (M-41 B5): krav · recon2 · P-8-spec · ordbog · kill-list-udkast; `orderedApproval: true`** (validering V-F1: M-38 15:06 lå FØR r4b-verdikterne 15:10 — rækkefølgen var ikke bevist). **Mathias sidst = HÆNDELSESKÆDE (princip 9):** endelige artefakter + verdikter → fremlæggelse → REN devil på PRÆCIS den tekst → frys tekst + KVITTERING (verdikt-digests · fremlæggelses-blob · P-8 · ordbog · tidsstempel) → registrér afsendelse → bind Mathias' `plan ok` til kvitteringen. Enhver ændring efter kvitteringen gør den ugyldig (ny kvittering, ny fremlæggelse). **Mekanik (`kvittering.mjs`, P2-gates F-1, 2026-09-09):** driveren committer `plan-build/<pakke>/<gate>-kvittering.json` {schema_version 1 · gate_id · pakke · artifact_oid · bindings_oids · scope_digest · prerequisite_digests (de endelige verdikters digests) · fremlaeggelse {path, blob_oid} · devil {path, blob_oid, dom:"PASS"} · frosset (ISO)} i commit A FØR fremlæggelsen sendes; efter Mathias' ord committes `<gate>-approval.json` med `approval.kvittering_digest` = sha256 af kvitteringens committede bytes i commit B. Kernen (`gates.mjs`, `approvalReceipt: true` på plan + slut) kræver frisk `verifyApproval` (`kvittering.mjs`): digest matcher · indhold == gatens faktiske artefakt/bindinger/scope/verdikt-digests · fremlæggelses- og devil-blobs er de committede @ evidens-ref · kvitteringens sidste berørings-commit er ÆGTE forfader til approvalens (aldrig samme commit, aldrig efter) — git-historik er beviset, som ved blindhed. Krav-gaten kræver OGSÅ kvittering — kun PRÆCIS den historiske M-38-godkendelse og r4b-Codex-verdiktet er undtaget, **digest-bundet** i `gates.mjs HISTORISKE_UNDTAGELSER` (P2 runde 2: en gate-bred undtagelse lod nye run-ID'er passere); listen udvides aldrig uden Mathias' ord + DEL VIII-punkt. **Devil-dommen læses fra blobben** (devil-filen er JSON m. `konklusion` + `fremlaeggelse_blob`), begge referencer skal være git-type blob, og evidens-ref opløses ÉN gang til commit-OID (`resolveEvidence`, rapporteret som `evidence_commit`). Signeret kvittering fra betroet runner = deklareret residual (CI-approver-flow). `plan-gate-run.mjs` er udfør-siden (spejl af krav-gate-run + verifyApproval). **REN devil = dom-tærskel, ikke nul noter (vejnings-skæring 2026-09-16, efter fire devil-kørsler på plan v3.5's fremlæggelse m. 12→7→7 rene form-fund):** en devil-kørsel FEJLER kun ved (a) skjult spørgsmål til Mathias · (b) usand/over-/underdreven gengivelse af plan, krav eller Mathias' ord · (c) manglende §10-post/S-3/overdragelse · (d) uforståeligt systemord; form-/»kunne tilføjes«-noter tæller ikke og genåbner aldrig en runde. Højst TO devil-kørsler pr. fremlæggelse; ét a-d-fund i den sidste rettes i teksten uden ny kørsel og noteres i kvitteringens provenance. Devil-prompten bærer tærsklen eksplicit (driverens v2-skabelon). **Højst to fulde review-runder på planen (vejnings-skæring 2026-09-16 — Mathias: »det virker meget voldsomt at det tager så lang tid«; fabrik-beslutning):** runde 1 (B4) + én dokumenteret ekstra runde (B6 m. tre aktører) er grænsen. BLOKER derefter → driveren retter det navngivne sted (mini-planner-instruks, kun det afsnit), og Codex dømmer KUN deltaet (henvisnings-delta uden frit pas) som gate-verdikt; code-reviewer og claude-ai genbruges ved byte-lighed på de citerede afsnit. RET-fund → deklarerede rettelser i §10/§11 uden ny dom, bæres ind i første bid. Kan planen ikke løse et fund uden at bøje et krav → HALT-flag → Mathias. Fase 4: én angrebs-spec pr. bid, ét review pr. bid, ingen genreview af uændrede bids. **B6 genbruger B4 (vejnings-skæring 2026-09-15, M-39 »genbrug ved byte-lighed«):** gate-verdikterne fra code-reviewer og codex er B4's SIDSTE pas bundet til den ENDELIGE plan-SHA (samme input · rolle · model · effort · instruks) — de køres ikke igen; kun claude-ai's verdikt er nyt. Ændrer planen sig efter B4's sidste pas (v3.2 plan-SHA-synk), gælder genbruget kun hvis synk-diffen er ren SHA-/henvisningsopdatering (verificeret mekanisk: ingen ændring i §1-§3-indhold), ellers ét delta-pas. **Ingen Mathias-spørgsmål fra plan-fasen (DEL VIII pkt. 37, Mathias 2026-09-15 »plan er kode og krav er forretningsgange«):** fremlæggelsen bærer plannerens deklarerede defaults som ORIENTERINGER under `plan ok`; kun det der ændrer et krav/sandhedsdokument er et spørgsmål. **Dybde-dommen ligger HER:** aktør-verdikterne dømmer at de planlagte tests udøver den faktiske logik (build dømmer ikke igen). Krav-matrix mekanisk: K-n uden step+test → FAIL · plan-løfte uden bid → FAIL · rogue-trin → FAIL. **Plan-SHA låses.** Uløselig modsigelse mod låste docs → terminal STOP. Efter plan OK ændres planen aldrig tavst → HALT → ny plan-SHA → ny plan OK (git-historik = versionering).

**Plan-template** (`plan-build/<pakke>/plan.md` · AI-intern):
```
# <pakke> — plan   (krav_oid · recon2_oid · plan_sha · status)
## Krav-ID-matrix (bijektion) | K-n | bid | step(s) | test→slut-effekt inkl. negativer + kill-list |
## Bid-opdeling (afhængigheds-ordnet)
   Bid N: leverer K-n · afhænger af <—|bid> · prover-bevisbar · angrebs-spec up-front · risiko-flag
## Repo-doc-tekst 1:1 (forud-godkendte ```doc:<sti>```-blokke)
## Bro-bindinger: plan⊨krav · plan⊨vision/forretning · build⊨plan
```

## Fase 4 — Build (plan ⊨ build 1:1) — gate MEKANISK

**HURTIGT SPOR gælder for pakke 1 (DEL VIII pkt. 42, Mathias 2026-09-21 »hele workflowet er overkill«):** angrebs-tests som kode + indeks (skema 2) i én Codex-produktion · byggeren i to omgange · ét review pr. omgang · CI læser rapporten · P-8 én gang. Punkterne nedenfor beskriver den fulde mekanik; hvor de strider mod pkt. 42, gælder pkt. 42 for pakke 1.
1. **Codex skriver angrebs-spec FØR byg** (hård gate — attack-spec-state-machine, 2.E): de hårde + negative tilfælde + kill-list pr. K. Holdets fælles done-kriterier, ikke en skjult fælde.
2. **builder-Code (FRISK — konsumerer den LÅSTE plan-SHA, skrev den ikke)** bygger bid-for-bid **1:1 mod plan-teksten** — intet nyt besluttes. Læser + kører tests, skriver ALDRIG måle-lag (2.E). **Rigtig parallelitet i Fase 4 (M-41, GRUNDPLAN-v2 §4):** Codex skriver angrebs-specs i pipeline (bid N+1 mens builder bygger N) og effect-harness + mutanter for bid N BLINDT fra den låste plans harness-form (indgang · rolle · slut-effekt · reject-klasse; alle navne fastlagt i planen), parallelt med byggeren og uden at se diffen — første prover-kørsel bliver et opslag, og afviger koden fra planens navne går harnessen rød = 1:1-brud → HALT (det er en feature, ikke en fejl). **Ét-bid-loft (M-41 beslutning 5, validering V-F9):** højst ÉT efterfølgende bid må være startet uden det forrige bids review; delt skema, rettighedskerne eller andet bærende grundlag afventer review — så et sent fund i bid 1 højst vælter ét startet bid, ikke fire. Cascade-STOP består.
3. **build-proof** (2.C): bijektion + effect-harness + config-mutant-kill + claim_graph + async_reviews + prover grøn — **produceret af sandheds-motoren, ikke af hånd-skrevne flag (M-40 C / M-41 Trin C1):** `build-proof.json` skrives af `runBuildProofEngine` + `runProver`-output; K/acceptkriterie/negativ-sættet kommer fra den LÅSTE forventningsliste (Fase 3 pkt. 3a3), aldrig fra beviset selv; harnessen observerer faktisk data (før/efter) og kontraktbundne reject-klasser (ikke kun én SQLSTATE); »afvisnings-sti-coverage« er ikke et selvstændigt bevis (M-40 D11) men »negativ afvist m. pinnet reject-klasse, målt i samme harness-kørsel«. D10 (én meningsfuld mutant pr. afvisnings-ac m. eget værn) og D12 (forudsætnings-bid binder til effekt-bid) håndhæves MEKANISK i `build-proof.mjs` før første bid bygges (Trin C2). **P-7 (M-35):** Codex leverer pr. bid et EGET resultat om FAKTISK diff ⊨ krav/plan (manglende kravdel · ekstra beslutning · test-tilpasning), bundet til aktør + base_oid + præcis kode-OID i review-kontrakten (async_reviews kræver dermed et BESTEMT reviewer-sæt, ikke blot PASS pr. bid).
**Fejl-håndtering:** rød/modsigelse → HALT + durabelt flag · lokal fejl → fix-loop (prover = eneste success) · uløst → `/rewind` + eskalér · sent Codex-fund i N-1 → cascade-STOP (invaliderer N) · større nyopdaget scope → **teknisk gæld** (logges, løses korrekt senere — aldrig undskyldning for at gøre et rødt bid grønt); mindre → bilag. **P4:** fanget falsk-grøn lukkes kun med failing-first regressions-test.
**Gate `build` = MEKANISK** i CI (ingen Mathias-dom — HVORDAN er ikke hans bord når build er tro mod den godkendte plan).

## Fase 5 — Sandhed (build ⊨ sandhed v. fuld dybde)
CI fetcher **held-out reel data EFTER build** (anti-tailoring, 2.C) → chain-proof + full-chain prover ved fuld dybde (P5, alle K-n; ingen fixtures — omdøbt/kurateret fixture afvises). **Chain-proof-harnessen BYGGES UNDER Fase 4 (M-41, Claude F-8):** CI-job · P-8 §1 data-udvælgelse · canaries N1-N9 · kæde C0-C10 · verifier skrives fra P-8-spec + låst plan i måle-laget mens bids bygges, så Fase 5's kritiske vej = kørselstid; kun held-out-fetch sker efter build. `proofs.mjs` fail-lukker korrekt indtil harnessen findes — det beskrives ALDRIG som afsluttet beskyttelse. **B4-sandbox** (recon-attester bliver mekaniske) planlagt fra pakke 2.
**Gate `slut`** = machineProof(chain-proof) + approver(mgrubak — krav-rammen fuldt leveret, alle K-n).

## Fase 6 — Luk (doc-spor)
Under build kun arbejdsdoks (fokus = build). Efter build: repo-docs + masterplan **1:1 fra plan-teksten** (forud-godkendt — anvendes, fabrikeres ikke) · teknisk-gæld-diff opdateret · lukke-PR sletter arbejdsdoks (main = fulde spor, rent repo). **Masterplan-diff = Mathias-godkendelse ved luk** (doc-godkendelse, ikke en 4. kæde-gate). sandhed-protect: AI skriver aldrig `docs/sandhed/`. **M-41 (beslutning 7, GRUNDPLAN-v2 §6):** doc-diffs fra plan §6 genereres ved plan-lås og appliceres byte-identisk ved luk · masterplan-diff + workflow-diff + nøgletal fremlægges SAMLET i ÉN luk-fremlæggelse (via devil, m. kvittering som ved plan-gaten) og godkendes med ÉT ord.
**P-9 (M-35) — luknings-efterprøvning:** en frisk Codex efterprøver pakkens lukninger (fejl → rettelsen der bærer lukningen → kørsel der viser fejlen fanges); »ingen forslag« accepteres som udfald. **Lukningsgrundlaget bygges ved MODTAGELSE, ikke ved luk (M-41 Trin A8, Codex F-10):** driverens arbejdslog registrerer hvert modtaget fund m. kilde · fund-id · berørt input-OID · status, og ved rettelse rettelses-OID + bevisreference; P-9 starter med et opslag mod den fulde liste og søger derefter frit efter manglende lukninger (pakke 1 mistede to recon-kandidater og devil-output).
**Retrospektiv = fast trin (L2, Mathias 2026-09-03):** alle korrektioner/fund fra pakken klassificeres **struktur · mekanik · residual** (DEL VII) og forankres (L1: anker-kolonnen i DEL VIII — læring uden commit+fil-anker = ikke lært). **Workflow-diffen = Mathias-godkendelse ved luk** (som masterplan-diffen). Luk-rapporten bærer TRE tal: (1) Mathias-korrektioner pr. fase · (2) fresh-eyes-fund · (3) Codex-fund på transport EFTER gate-åbning. Mål for pakke 2: 0 strukturelle Mathias-korrektioner. **Tallene tælles MEKANISK (`retro-kpi.mjs`, M-40 E19 / M-41 §6) fra ledger, audits, verdikter og provenance — aldrig aktør-ord — og suppleres med tids-KPI pr. pakke: aktørtid · ventetid på Mathias · fabrik-tid · Mathias-ture (validering B-4: ingen tidsbesparelse loves før den er målt; pakke 1 er baseline).**

---

# DEL IV — TVÆRGÅENDE

## Forløbs-troskab
Gate-rækkefølgen (registry-DAG + check-runs pr. commit) håndhæver fasernes orden + hver gates beviser; + **minimal append-only, actor-signeret worklog** (audit-spor) — **bygget som mathias-ord-ledgeren** (`plan-build/<pakke>/mathias-ord.md`, A3/Mathias 2026-09-03): append-only · tidsstemplet · VERBATIM (parafrase eksplicit deklareret) · committet af driveren ved hvert ord · krav/plan citerer ord-id (K-n → M-n); et ord uden ledger-entry kan ikke bære et krav. Separat manifest-validator droppet (redundant med gates).

## Pakke-2-tiltag (M-39 — besluttet nu, aktiveres fra næste pakke)
1. Spekulativ recon-2 + P-2 i ventetid på Mathias (startet efter fremlæggelse; delta-re-bind mod endelig krav-OID efter ok). 2. Audit-addendum ∥ buildability på udkast-blobben (byte-identisk upload; re-stamp deklareret i provenance). 3. P-5 dokument-aksen som tredje akse i omission-devil-passet (ikke separat kørsel). 4. Fabrik-bygger-session adskilles fra driveren (ny commit-zone driver ⊂ fabrik). 5. Genbrug af uændrede aktør-resultater KUN mekanisk (byte-lighed, ingen dom — og genbrug kræver identisk samlet input + rolle + model + effort + instruks, ikke kun dokument-bytes; M-41 V-3). **M-41 (2026-09-09, GRUNDPLAN-v2 §7):** 6. Fase 1: filter- og omission-/dokument-katalog bygges BLINDT før recon-aktørerne på frosset bundle; aktørerne binder én gang; frit komplethedspas efter katalog-opslag bevares. 7. Fase 2: K-skelet (9 linjer: K-n · HVAD · bærende M-ord · bord-flyt) vises i den eksisterende forretningsdialog FØR krav-teksten — stoptjek, ingen ekstra gate (ét struktur-ord omskrev 66+/41− linjer i pakke 1). 8. Fase 2: spørgsmål m. stabilt id, svarmuligheder og kilde; svar gemt med spørgsmål; ny betydning markerer hvad den afløser; ÉN samlet åben-punkt-liste pr. tur (kilde · eksisterende svar · materialitet). 9. Recon-præsentation: gentagne tekniske forklaringer vises én gang, ordlyds-divergenser grupperes, kun forretningsvalg til Mathias. 10. Fase 3 fuldt skelet-forløb (planner committer matrix · bid-liste · valg · ordbogs-entries som SKELET → struktur-pas ∥ krop → krop-commit m. deklareret skelet-diff → ét samlet angreb); variant ved stor struktur-risiko: stop ved skelettet, luk struktur-fund, frys skelet v2, skriv krop. 11. Frisk fresh-eyes-instans pr. artefakt (samme instans kun til delta-addenda). 12. Fase 5: parallelle effekt-prøver i isolerede databaser KUN ved målt behov. 13. Flere pakker samtidig vurderes først når pakke 2 er kørt rent OG pakke-afgrænsede stier/routing findes. **IKKE (afvist):** fresh-eyes+P-4 sammenlagt · devil skippet på ny tekst · flere pakker samtidig før pakke 2 er kørt rent · effort-sænkning (M-39: vent).

## Genbrug — vejet
| Eksisterende | Dom | Hvorfor |
|---|---|---|
| CI-scaffolding (Actions · required checks · CODEOWNERS · branch-protection · husky) | Genbrug + fix | CI-som-dommer kræver CI-infra; fix skipped=green · marker-match · per-gate. |
| Adapter-transport (`code.sh`/`codex.sh`/`claude-ai-rolle.sh`/`mathias.mjs`) | Genbrug mønster + fix | Fix `--dangerously-skip-permissions` → måle-lag-isolation. |
| `dirigent.mjs` decide/udfoer + transport | Genbrug arkitektur, ny logik | Pure-decide + atomisk transport er sund plumbing. |
| `tilstand.mjs` git-state-parsers | Delvis genbrug | Git-parsing ja; marker-match-i-markdown → JSON-verdikter. |
| `fitness.mjs` + `migration-gate.mjs` | Genbrug | Deterministiske invariant-dommere. |
| Selftest-framework | Genbrug framework, nye tests | Harness genbruges. |
| `/workflow/` 24 kontrakter · roller · gate-def | Rebuild (udvind sunde sub-regler) | Encoder gammel uvalideret mekanik. |
| Marker-verdikter · per-gate-bug · skip-permissions · skipped=green | Rebuild/fix | Præcis det der gjorde flowet falsk-grønt. |

## Vejet FRA (anti-over-test)
| Mekanisme | Hvorfor ud |
|---|---|
| Multi-round recon-devil-til-tør | Én omission-pass + coverage; loop kun ved fund. |
| Separate dybde-/integrations-/fidelity-meta-canaries | Slut-effekt-test + full-chain + menneske-troskab dækker. |
| Blanket mutation/PBT/fuzz | Kun høj-risiko-bids; targeted config-mutant-kill pr. opsætnings-K er billig og bliver. |
| Liveness-selvtjek (prover · full-chain · held-out) | "Kan testen fejle" garanteres af negativ-krav + red-team. |
| Elaborate kontrol-dok + forløbs-manifest-validator | Gates kræver deres beviser; minimal worklog nok. |
| Separat model-gate | Driver pinner model+skill by construction (`actors.lock`, måle-lag-beskyttet, CI re-kører). |
| Committede gate-state-filer som autoritet | Erstattet af check-runs (2.A) — en bot-skrivbar fil kan aldrig være gate-autoritet. |

---

# DEL V — FORUDSÆTNINGS-FIX (røde i dag — før workflowet kan håndhæve)
1. `gate_ord`↔`gate-def`-afstemning (`plan OK`/`build OK` i motoren) + divergens-vogter.
2. CI mod **reelle committede artefakter** (ikke selftests/fixtures).
3. `workflow:selftest`-rød på `main` fikset.
4. **GitHub-rettighedsmodel:** gate-App `checks:write` (aldrig `contents:write`) · jobs `contents:read` · rulesets på måle-lags-stier · required check source-locked · app-adgang ved privat repo (Actions-minutter + re-grant).
5. **Mappe-migration:** krav-doc fra `docs/coordination/*` → `docs/sandhed/krav/` + reference-rewrite + CODEOWNERS på både gamle og nye stier indtil cutover — **ÉN bevægelse**, FØR enforcement (2.E) kan påstås live.

---

# DEL VI — BYGGERÆKKEFØLGE + RED-TEAM (accept-kriteriet)

**Rækkefølge (når Mathias siger byg):**
1. Forudsætnings-fix (DEL V).
2. **Gate-kernen (2.A+2.B) + dens per-mekanisme-red-team STRAKS** — kernen bevises kun på maskine (bindende lektie, 2.A).
3. Resten af substratet (proofs · prover · coverage · enforcement · driver · rolle-skills) + selftests.
4. Fase 0–2-wiring. 5. Fase 3. 6. Fase 4. 7. Fase 5–6.
8. **Fuldt red-team end-to-end** — først her er workflowet skudsikkert (workflowets eget slut OK; P5 på sig selv).

**(a) Per-mekanisme-bevis** — hver load-bearing mekanisme demonstreres at gøre sit job:
`evaluateGate` nægter uden bundet bevis · generisk `{ok:true}` afvist (forkert gate/artifact-oid) · tomt/manglende felt = rød (fail-closed-schema-test) · check-run fra forkert app afvist (source-lock) · ruleset blokerer Code-skriv til måle-lag · **mutation-survival fanger en "findes"-test** (plantet mutant overlever → rød) · claim uden eksekvering+kill afvist · anti-tavshed blokerer manglende/forfalsket verdikt · path-binding afviser orphan-blob-citat · handover-HALT blokerer krav på stale recon · coverage flagger sprunget flade-punkt · prover rød på plantet dyb fejl.

**(b) Falsk-grøn red-team end-to-end + mandate-canary pr. rolle** — plant bevidst, SKAL blokeres ved rette gate:
shallow test der består men misser negativen (→ mutant-kill) · forfalsket verdikt/approval (→ server-provenance/orderedApproval) · stale recon-OID (→ handover-HALT) · Code ændrer måle-lag (→ ruleset) · reviewer citerer ikke-eksekveret kode (→ claim_graph) · recon taber et berørt område (→ scope-coverage) · byg uden committet angrebs-spec (→ attack-spec-gate) · krav OK før aktør-verdikter (→ orderedApproval).
Hver canary = seeded negativ fixture; gaten SKAL gå rød. Alle bestået → **skudsikker**. Testene er selv forståelses-baserede (udøver mekanismens faktiske logik, ikke dens beskrivelse).

**(c) Proces-canaries (L3, Mathias 2026-09-03 — første pakkes fejl-mønstre som plantede prøver; pakke 2 SKAL bestå):**
teknik-/model-fork stillet til Mathias (→ spørgsmåls-devil skal fange den) · ubekræftet afledning i et udkast (→ fresh-eyes-audit skal liste den) · commit uden for sessionens rolle-zone (→ pre-commit-hook skal afvise) · u-hærdet gate-transport i drift (→ hærdet-register skal gå rød) · Mathias-ord brugt i krav uden ledger-id (→ krav-review skal fange det) · fremlæggelse kun i chat uden durabel fil (→ driver-tjek). KPI'erne fra Fase 6-retrospektivet (L2) måler at canary-familien virker i praksis.

---

# DEL VII — ÆRLIGE RESIDUALER (ikke fake-fixet)
- **Semantisk forretnings-troskab** (krav⊨vision korrekt, ikke kun citeret) — Mathias + Claude.ai.
- **Ukendt-ukendt recon-komplethed** — omission-devil + Mathias krymper; residual.
- **Dybde-fuldstændighed** — effect-harness + mutant-kill mindsker mekanisk; fuld sti-dækning er ikke deterministisk; rest = plan-gatens dom + prover.
- **Internt-komplet vs. eksternt-forkert bijektion** (krav forkert splittet/udvandet) — Mathias + Claude.ai.
- **Claude-terminal-session som menneske-led** (før: app; Mathias 2026-09-02) + **GitHub-App/Actions som root-of-trust** — enforcement-selftest verificerer at config er live; org-admin (mgrubak) uden for workflowet, navngivet.
- **Aktør-dommens korrekthed** — kernen verificerer PASS + reelle citater, IKKE at dommen er rigtig (P3-residual; derfor cross-vendor + menneske-gates).

---

# DEL VIII — AFGJORT (beslutningslog)
1. **Genbrug vejet** (sund plumbing genbruges · falsk-grøn-mekanik bygges på ny).
2. **Recon i terminal** (`claude -p`/`codex exec`); **krav i NY frisk Code-terminal-session i claude-ai-rollen** (Mathias 2026-09-02 — før: Claude.ai-appen; ændret for cross-session-kommunikation. Udkast i AI-zone, upload = driver-flyt på Mathias' ord, byte-identisk).
3. **CI = autoritativ dommer**; lokal = candidate-only.
4. **test-led-skill** pr. ⊨; aktør = input, dommer = deterministisk/menneske.
5. Trace = committede OID-bundne artefakter.
6. **Gate-autoritet = check-runs fra gate-App'en** — aldrig committede filer (v4-kernen).
7. **Krav-rækkefølge: Mathias godkender SIDST** (krav 5) — upload ≠ krav OK; orderedApproval beviser rækkefølgen.
8. **build OK = mekanisk**, ikke Mathias-gate.
9. **Mathias rører aldrig GitHub-UI**; hans flade = Code-terminalen (app udgået 2026-09-02, se pkt. 2).
10. **Papir-runder på gate-kernen er stoppet** — dens bevis er byg + red-team (DEL VI pkt. 2).

**Anker-regel (L1, Mathias 2026-09-03): hver beslutning/læring peger på commit + fil — læring uden anker = ikke lært.** Dagens korrektioner, bagudret:

| # | Beslutning/læring | Anker (commit · fil) |
|---|---|---|
| 11 | Recon-dækning KUN pakkens flade; flade_filter påkrævet; devil dømmer filteret | 7c08e4c · coverage.mjs/proofs.mjs + 2.C/2.D/Fase 1 |
| 12 | Aftager-kæden: recon-1→krav-dok, recon-2→plan-dok; mutation-materiale = bilag | 7c08e4c · Fase 1 + consolidate-recon.mjs |
| 13 | Krav i terminal-session; udkast i AI-zone; upload = driver-flyt (byte-identisk) | a9403eb · DEL I/Fase 2 + hooks.mjs (kravUpload-rute) |
| 14 | Afled-før-spørg (+ mønster-analogi 1415b26) | 31b1d11 · claude-ai.md + Fase 2 pkt. 2 |
| 15 | Bord-testen: teknik-/model-forks = plan-fase-afgørelser, synligt flyttet | 5276c59 · claude-ai.md + Fase 2 pkt. 2 |
| 16 | Fremlæggelses-pligt: ok kun efter fuld fremlæggelse i chatten, hans sprog | 7f07068 · claude-ai.md + Fase 2 pkt. 5 |
| 17 | Form-krav: én beslutning · scenarie · ét-ords-svar · værdi=UI-konfig | 1415b26 · claude-ai.md + Fase 2 pkt. 2 |
| 18 | Altid nyeste: CLI=transport (auto) vs MODEL=identitet (Mathias' ord) | b7534fc · preflight.mjs + 2.F |
| 19 | Gate-transport: proof bærer egne bindinger (aldrig injiceret); fund-bøtte-konsistens; render-backstop | 58d2bac · recon-gate-run.mjs/consolidate-recon.mjs |
| 20 | Commit-zoner pr. rolle + rent-arbejdstræ-preflight (git add -A-fejlen) | aa6ba0b · hooks.mjs/pre-commit-zone.mjs + 2.E |
| 21 | Mathias-ord-ledger: verbatim, append-only, K-n citerer M-n | aa6ba0b · plan-build/<pakke>/mathias-ord.md + DEL IV |
| 22 | Hærdet-register: gate-moduler blob-bundet til Codex-pas; drift = CI rød | (dette commit) · haerdet-register.json + 2.D |
| 23 | Spørgsmåls-devil før hver Mathias-vendt besked; ét vindue; driver = postkontor | (dette commit) · DEL I + 2.F + Fase 2/3 |
| 24 | Fresh-eyes-audit før upload/plan-låsning (mandat bærer audit-OID) | (dette commit) · hooks.mjs + Fase 2 pkt. 3/Fase 3 |
| 25 | Divergens-klassificering: påstand→Mathias, ordlyd→AI-spor, uklassificeret=påstand — **default REVIDERET af M-40 D15 (se pkt. 34): bøtte-1-enighed (bøtte+negativer) = ai-spor-p2, kun substans-divergens løftes** | (dette commit; D15: M-40-commit) · consolidate-recon.mjs + 2.C |
| 26 | Pakke-ordbog: Mathias' ord ↔ systemord; plan/kode arver navnene | (dette commit) · plan-build/<pakke>/ordbog.md + Fase 1/3 |
| 27 | Rolle-sessioner implementerer aldrig workflow-mekanik (kun rolletekst-arv) | (dette commit) · 2.G + DEL I |
| 28 | Retrospektiv + workflow-diff-godkendelse + 3 KPI-tal i Fase 6; proces-canaries i DEL VI | (dette commit) · Fase 6 + DEL VI (c) |
| 29 | Codex-model-pin = gpt-6-astra xhigh (Mathias' model-ord M-31, 2026-09-08); actors.lock oprettet som pin-hjem; eksplicit -m/-c i alle kald; afsluttede passes genkøres ikke | 2026-09-08-commit · actors.lock + 2.G + preflight.mjs |
| 30 | Claude-rolle-pin = claude-fable-5-1 xhigh (M-33; pre-flight-smoke bestået, CLI 2.1.263) + materialitets-test som bord-testens 3. led + ét-skridts-regel (bekræftelse, ikke spørgsmål) + audit-fund klassificeres; værn 3 (loft/takt) AFVIST af Mathias | 2026-09-08-commit · actors.lock + claude-ai.md + Fase 2 pkt. 2/3 |
| 31 | `krav upload` som Mathias-ord AFSKAFFET (M-34): upload = driver-automatik (udkast + REN audit af præcis den blob); én fuld fremlæggelse efter buildability; hans ene ord = `krav ok` (orderedApproval uændret) | 2026-09-08-commit · DEL I + Fase 2 pkt. 3 + claude-ai.md |
| 34 | Over-test-integration (M-40): krav-gate-run.mjs (pinnet gate-dom) · 6 selvtest-fixes · sandheds-motoren = første Fase-4-forudsætning (build-proof fra reelle kørsler, K-sæt fra LÅST plan, kontraktbundne reject-klasser) · mutant-regel D10 · branch-coverage omdefineret D11 · forudsætnings-bids D12 · planner-undtagelse fjernet D13 · JS-runtime-klasse frosset D14 · consolidate-default D15 · proces-regler E16-E20. IKKE: suite-beskæring · 3-blind-ændring · devil-skip · register-undtagelser | 2026-09-08-commits · krav-gate-run.mjs + selvtests + plan 2.C/Fase 2-3 + rolletekster |
| 33 | Tidsoptimering (M-39): timeout+retry-samme-effort på al codex-transport · delta-batch pr. runde · auto-pakket verdikt-byg m. tidlig citat-scope-kontrol · effort i provenance (pin uændret: xhigh); pkt. 5-9 = pakke-2-tiltag (DEL IV) | 2026-09-08-commit · codex-run.sh + verdikt-byg.mjs + verdikt.mjs + 2.F/Fase 2/DEL IV |
| 32 | Codex-integration P-1..P-9 indført NU, testes på lokations-skabelon (M-35): troskabs-akse i devil (P-3) · kildetjek i audit (P-4) · divergens-efterprøvning (P-2) · dokument-akse i omission-devil (P-5) · rolletekst-konsistens-pas (P-6, kill-list-modstrid afgjort: udkast VED plan-gaten) · diff⊨krav-review (P-7) · slutprøve-spec før plan-lås (P-8) · luknings-efterprøvning (P-9) · transport-red-team (P-1, venter ærligt på gate-transport). IKKE ind: forretnings-dom · ekstra gate-aktør · medbygger · OID/schema-erstatning · obligatorisk ekstra runde | 2026-09-08-commit · DEL III/2.F/2.G + planner-code.md |
| 35 | **GRUNDPLAN-v2 (M-41, 2026-09-09):** 8 analyser (Codex-integration · tid ×2 · over-test ×2 · flow ×2 · Codex-opsætning 22 pkt) samlet og Codex-valideret (12 fund indarbejdet); Mathias' ene ok til 8 beslutninger: (1) Fase 3 runde 2 = batch + delta på berørte krav + frit helheds-pas + code-reviewer som frisk slutlæser · (2) kvitterings-kæde for »Mathias sidst« + orderedApproval på plan-gaten · (3) fabrik-frys pr. kørsel · (4) driver og fabrik-bygger i to sessioner NU (driver = eneste postkontor) · (5) Fase 4: højst ét efterfølgende bid uden forriges review · (6a) pakke 2: K-skelet før krav-tekst · (6b) pakke 2: katalog før recon, plan-skelet før krop, spørgsmål m. id · (7) én luk-fremlæggelse, ét ord. 11 principper (frosne input/blindt · skelet før krop · løkke = opslag + delta · delta-batch · beskriv ≠ luk · rollen bestemmer kaldet · fabrik frosset · Mathias sidst som hændelseskæde · sandhed = harness + mutant). Udførelse uden nyt ord: transport v2 (2.F) · rolletekst-konsistens (D10/D13-modstrid i planner/codex-angreb/builder fjernet) · beskriv ≠ luk i roller · forventningsliste før lås · fund-log · legacy-kæde (`scripts/kaede`, CI-refereret) = eget spor, ikke pensioneret her. Effort-pins UÆNDRET (M-39 VENT). Arkiv: `docs/workflow-faerdiggoerelse/analyser-2026-09-08/` | 2026-09-09-commit · 2.F + Fase 3 pkt. 3/3a3/3b/4 + DEL IV pakke-2 6-13 + roller |
| 36 | **Lokal gate-dom = candidate på git-historik + fabrik-frys (BESLUTTET af fabrik-armen 2026-09-15 — Mathias 2026-09-15 om punktet: »det er ikke mit bord«; rejst af Codex P2 runde 2/3):** **Beslutning (fabrik, bord-deling: mekanik er ikke Mathias' bord):** lokale gate-domme (plan · build · slut) er KANDIDATER — de bærer git-historik + fabrik-frys + transport-kvittering som bevis, mærkes `candidate` i gate-resultatet, og bliver autoritative først når CI-dommeren (C4) genkører dem mod rå input og emitterer check-run. B6/B7 kan køre på dette grundlag; Mathias' `plan ok` binder til kvitteringen som før. kvitterings-kæden (godkendelses- + transport-kvittering) beviser digest-lighed og commit-orden mekanisk, men IKKE en autentificeret menneske-hændelse eller en integritets-verificeret codex-binær — det kræver CI-approver-flow + betroet actor-runner (DEL V). Indtil de findes hviler lokal drift på: pinnede binærer (hash), lås fra pinned commit, `--no-replace-objects`, digest-bundne historiske undtagelser, kvitteringer i committet provenance. Codex kalder deklarationen »ærligt navngivet men utilstrækkelig som lokal lukning« og kræver Mathias' udtrykkelige mandat — ikke en residual-note. **Status: forelagt Mathias 2026-09-09; genforelagt 2026-09-10 efter Codex P2 runde 9 PASS (alle mekaniske fund lukket, G-F1 = ENESTE udestående, hærdet-register grønt @ regel-commit 8a5755f; leverancer i `docs/workflow-faerdiggoerelse/p2-haerdning-2026-09-09/`). Indtil ordet falder: Trin B1-B5 (produktion, ingen gate-dom) kan køre; B6/B7 (gate-domme som candidate) venter.** | 2026-09-09-commit · 2.F + Fase 3 pkt. 4 + kvittering.mjs |
| 37 | **Plan = kode, krav = forretningsgange (Mathias 2026-09-15, ordret til krav-vinduet mathias-df: »nu får jeg en masse spørgsmål? plan er kode og krav er forretningsgange - derudover s-2 spørgsmålet modsiger mine sandhedsdokumenter: svaret på spørgsmålet er at de styres i ui« — og til fabrik-armen samme dag om pkt. 36: »det er ikke mit bord« / »altså så længe det kun er kode logikker«):** i PLAN-fasen stilles Mathias ingen spørgsmål — planlæggeren afgør med DEKLAREREDE defaults, Codex angriber dem, `plan ok` dækker dem som orienteringer. Hans bord er krav (forretningsgange) og godkendelses-ordet; plan- og fabrik-mekanik (bid-opdeling, datomekanik, candidate/autoritet, hærdning, residual-håndtering) er ikke hans. Et spørgsmål der når ham fra plan-fasen er en bord-fejl: bord-testen for Fase 3-fremlæggelser bliver »ændrer det et krav eller sandhedsdokument?« — ellers er det plannerens valg. S-2 (persondata-undtagelse) blev besvaret ved afvisning af præmissen: om et felt er persondata »styres i ui« — markeres det, SKAL det kunne anonymiseres (K-7); ingen undtagelse. Spørgsmåls-devilen får »plan = kode« som eksplicit akse. Ordet ligger verbatim i `plan-build/lokations-skabelon/mathias-ord-afventer-ledger.md` (43baf9d) til driverens ledger-append (M-42/M-43). | 2026-09-15-commit · Fase 3 pkt. 3/4 + DEL I bord-deling |
| 38 | **C4 CI-dommer bygget som KANDIDAT (2026-09-15, `ci-gate-dom.mjs` + `.github/workflows/v5-gate-dom.yml`):** ved hvert push genkører CI gate-dommerne FRISK mod den pushede commit (evidens = commit'en selv) for de gates pakken har NÅET (nåethed = gate-specifik approval-/proof-fil findes i commit'en: recon-coverage-proof · krav-approval · plan-approval · build-proof · chain-proof) og emitterer `v5/gate/<id>` som check-run via workflow-tokenets `checks:write`; ikke-nået gate = intet check (aldrig skipped=green); runner der kaster = failure; emission der fejler = job rødt. build/slut-udfør-siderne er ikke bygget (C4b) → nået gate dømmes RØD m. grund, aldrig tavs. **Autoritet kræver repo-admin (Mathias' infrastruktur, DEL V): (a) branch protection der kræver `v5/gate/<id>` fra kilden »GitHub Actions«, (b) ruleset der beskytter `scripts/v5/**` + `.github/workflows/**` mod ikke-reviewede pushes; (c) valgfrit: gate-App m. egen identitet i stedet for GITHUB_TOKEN.** Indtil da er også CI's dom en navngiven kandidat — men én der ikke kan produceres lokalt. **Codex P-1 runde 1 (2026-09-15, run_id 20260915T142022-2377447-1654):** checkrun.mjs PASS (register) · CI-adskillelsen (v5.yml/v5-gate-dom.yml) PASS · ci-gate-dom.mjs FAIL m. to fund: **F-C4-1** (en gammel åben dom blev grøn for ÆNDRET indhold — forfader-kravet binder historik, ikke identitet) → LUKKET: gatens artefakt, alle bindinger og launch.pakke skal have samme blob-OID ved den dømte (pinned) og den pushede commit, ellers failure m. hvad der ændrede sig; nyere evidens-commits er tilladt · **F-C4-2** (kæden verificerer hashes, indhold og commit-orden — men ikke HVEM der skrev approval/verdikter; en producent m. contents:write kan fremstille en komplet ordnet kæde) = **R-CI-APPROVER-FLOW**, kendt fra pkt. 36: autoritet kræver at Mathias' godkendelses-ord kommer fra en kanal producenten ikke kan skrive i — konkret forslag: `plan ok <kvittering_digest>` som GitHub-kommentar/PR-review fra `mgrubak`, som CI-dommeren verificerer via GitHub-API (login + digest + tidsorden) i stedet for en committet fil; aktør-provenance tilsvarende fra CI's egen actor-runner (C4b). **Det ændrer Mathias' godkendelses-kanal (ikke hans ord) → hans valg ved plan-lås; indtil da er også CI's dom en navngiven kandidat.** Admin-forudsætninger (Codex): required checks låst til kilden GitHub Actions pr. fase (recon → +krav → +plan → +build → +slut) plus det strenge register-job og selftest-jobbet; ruleset på scripts/v5/** · .github/workflows/** · test/v5/** · .claude/** · CODEOWNERS, alle refs der kan producere checks; evt. gate-App m. egen afsenderidentitet. **Codex P-1 runde 2 (run_id 20260915T143657-2427309-31628): ci-gate-dom.mjs PASS som HÆRDET TRANSPORT (register) — identiteten dækker registryets faktiske input; identiteten er ikke rekursiv (ændret anker → rødt krav-check, plan-check kan stå grønt → derfor KUMULATIVE required checks pr. fase); `pinned == pushed` omgår ikke frisk dom.** C4c-krav (godkendelses-kanal, når Mathias vælger): kun den valgte hændelsestype (PR-kommentar/APPROVED-review) verificeret via API (repo · PR · hændelses-id · login mod betroet kontobinding — aldrig author_association alene) · ét entydigt body-format `plan ok <64-hex kvittering_digest>` m. genverifikation af gate/pakke/artefakt/bindinger/fremlæggelse/verdikter · serverens godkendelsestid EFTER betroet registrering af kvitteringscommit'en (ikke git-author-tid) · redigeret/slettet/dismissed → ny dom/invalidering; API-fejl er aldrig grønt; approval-filen bevares som krydsbundet spor, men `login_server_verified` bærer ikke autoriteten. C4b-krav (actor-runner-attestation): repo · workflow-/regelrevision · run-id/attempt · aktør/rolle · faktisk model/effort · binær-/skill-/låseidentitet · gate/pakke/pinned/OID'er/promptdigest · rå outputdigest · afslutning · isolation — verificeret ophav, kopieret provenance afvises. Selvtest 36. | 2026-09-15-commit · 2.A autoritet · 2.E · DEL V |
| 39 | **C4b — CI's produktionsrunner + build-dommer (2026-09-16, kandidat: `ci-build-dom.mjs` · `pg-runner.mjs` · `.github/workflows/v5-build-dom.yml`):** ved hvert push hvor build-gaten er NÅET (`plan-approval.json` + `angrebs-spec.json` findes i commit'en) bygger CI en frisk store (Postgres-service m. supabase-image; produktets `supabase/migrations/*.sql` @ commit anvendes i rækkefølge — fejl = STOP), dømmer plan-gaten frisk som forgænger (ci-gate-dom, inkl. identitetsbinding), måler m. sandheds-motoren mod manifest + angrebs-spec @ commit gennem `pg-runner` (samme runner som integrationen: struktureret diagnostik, overlap-vidne, exit-kanal), producerer build-beviset IN-MEMORY og dømmer build-gaten → `v5/gate/build`. **Artefakt-typen `ci-produced` (gates.mjs `artifactCiProduced`, kun maskinbevis-gates build/slut; bindinger er altid blobs):** beviset committes aldrig (jobs har contents:read); ref = {layout-sti, oid = git hash-object af bevis-bytes, type ci-produced}; filen uploades som workflow-artefakt (90 dage) så enhver kan genverificere mod check-run'ets oid. **Driver-/planner-kontrakt før første bid:** `plan-build/<pakke>/prover.json` = {cmd[], resultRelPath} (den committede testflades kørsel — cmd skal skrive {total, passed, failed, skipped} til resultRelPath; mangler den, er beviset ærligt ufuldstændigt → rød; **forfattes af Codex i Fase 4 pkt. 1 sammen m. angrebs-spec'en og fryses før byg (måle-lag — Codex A5-7, 2026-09-16): prover-run.mjs ligger i måle-lags-zonen, byggeren KØRER den frosne prover og leverer bids/reviews**) · `plan-build/<pakke>/bids/<bid_id>.json` = {bid_id, base_oid} (bid'ets basis-commit, skrevet når bid'et er bygget; forfader til den gatede commit) · `plan-build/<pakke>/reviews/<bid_id>.json` = {bid_id, conclusion: PASS, base_oid} (C3-minimum: P-7-reviewet pr. bid af netop basis-commit'en — ellers »stale review«; mangler → rød) · `angrebs-spec.json` (Fase 4 pkt. 1, Codex, skema = angrebs-spec.mjs). API-/UI-indgange måles via exit-kanalen (check.cmd) — ingen HTTP-adapter endnu. ci-gate-dom emitterer intet for build (én emitter pr. check-navn). **Codex C4b-runde 1 (2026-09-16, run_id 20260916T122454-2795331-10735): gates · gate-eval · build-proof · ci-gate-dom PASS (register); ci-build-dom + pg-runner FAIL m. to fund — lukket:** **F-C4b-1** (produktkode — prover-cmd · migrationer · exit-kontroller · psql-værtskommandoer — kørte i samme job/proces som emitteren og kunne nå `checks:write`-tokenet) → **TO JOBS, TO TILLIDSZONER:** `maaling` (contents:read, INGEN checks:write, checkout uden persist-credentials, intet token i miljøet; `ci-build-dom --produce` kører produktkode m. RENSET miljø (`producentMiljoe`: ingen GITHUB_TOKEN/ACTIONS_*/INPUT_*/tokens — pg-runner afviser et miljø m. credential) og skriver bevis-bytes + meta som workflow-artefakt) → `dom` (checks:write, INGEN produktkode, ingen Postgres; `ci-build-dom --judge` tager bytes → oid → forgænger frisk → snapshot → evaluateGate → emission; `--emit` i `--produce` og GITHUB_TOKEN i måle-jobbet afvises af scriptet). **F-C4b-2** (race anvendte ikke aktørens settings — den forkerte tenant kunne bevises) → settings (validerede identifier-nøgler) anvendes i BEGGE sessions før handlingerne og VERIFICERES m. current_setting; fejl lukker forløbet; sql() samme kontrakt. Check-run-summary bærer nu FULD artefakt-oid. Tredjeparts-genverifikation (Codex): betroet workflow-revision + run/attempt + head_sha → hent artefaktet fra netop kørslen → git hash-object over rå bytes (inkl. slut-newline) == oid i check-run → resolve plan/manifest/spec @ commit, genkør plan-forgænger, rekonstruér envelope/snapshot → evaluateGate m. verifieren. **Codex C4b-runde 2 (run_id 20260916T125157-2874685-17901): pg-runner PASS (register) · to-jobs-formen PASS · F-C4b-1/2 rettet m. bevis · ci-build-dom FAIL m. F-C4b-3 (bevis-filen blev læst som tekst før hashing — ugyldig UTF-8 erstattes, så check-run'ets oid kunne afvige fra artefaktets rå bytes) → LUKKET: dom-jobbet læser RÅ bytes, hasher dem, og afviser ugyldig UTF-8 (fatal decoder) før JSON-dom. Codex' afgrænsning af isolationen accepteres: renset underprocesmiljø er ikke fuld isolation (prover.mjs merger process.env — scrubbes for kaldets varighed; /proc/<ppid>/environ) — men job A har intet emissions-token, så ingen falsk-grøn vej; R-RUNNER-UDFØRELSE. Manglende meta → tavshed (intet check), aldrig success. **Codex C4b-runde 3 (run_id 20260916T130619-2902298-29361): ci-build-dom PASS som hærdet transport (register) — F-C4b-3 rettet m. bevis (3 mutanter dræbt: utf8-afkodning før hash · fatal-decoder fjernet · hash af tekst i stedet for rå bytes); BOM/CRLF/kompakt JSON binder korrekt, ugyldig UTF-8/NUL/ekstra objekt afvises; meta kan ikke reparere et brudt bevis.** C4b er dermed HÆRDET TRANSPORT hele vejen (22 moduler m. pas, 0 kandidater). Autoritet: C4c (godkendelses-kanal + frosset spec + prover-registrering) · C3 (reviewer-identitet) · admin (required checks · ruleset). | 2026-09-16-commit · 2.A · 2.C · 2.E · DEL V |
| 40 | **Forgænger-gaten dømmes ved SIN EGEN pin (driver-fund 2026-09-21 ved første plan-dom — latent fejl i `plan-gate-run.mjs`):** plan-gaten re-dømte krav-gaten ved planens commit (P5), men krav-gatens verdikter/approval er bundet til krav-gatens egen pin (efc85d5) → »evidens citerer commit efc85d5 ≠ gated commit« → falsk RØD plan-gate; CI (ci-gate-dom → runPlanGate) ville gøre det samme. Fix: `forgaengerPin()` læser forgængerens pin fra det committede kandidat-resultat `plan-build/<pakke>/<forgænger>-gate-resultat.json.commit_sha` @ evidens (samme kilde som CI's PINNED_KILDE), kræver fuld OID + forfader til/lig gatens commit + IDENTISKE forgænger-inputs (artefakt · bindinger · pakke) @ pin og @ gatens commit (F-C4-1), og dømmer forgængeren dér; `predecessor.artifact_oid` er forgængerens EGET artefakt @ pin (kernen binder det til gatens binding). Fail-closed på manglende/korrupt fil, mutable ref, fremmed historik, ændret indhold. Selvtest 13 (rigtig git-fixture, inkl. negativ »forgænger med anden pin« og »krav ændret efter pin«). Lærdom: første kørsel af en gate-kæde er selv en test — den fandt hvad selvtests uden forgænger ikke kunne. | 2026-09-21-commit · 2.A kæde · Fase 3 pkt. 4 |
| 41 | **API-bevisform + substitution i måle-laget (Codex' HALT i Fase 4 pkt. 1, 2026-09-21 — manifest og plan-lås urørt):** **H1** manifestet binder K-9/ac-1/neg-1 · K-9/ac-2/neg-1 (fase »(via API)«) og fire MH-delbeviser til PostgREST-API'et som R+, men motoren kunne kun udføre SQL → `entrypoint.kind: "api"` skifter HANDLINGERNES transport (UT positive/negative · FS/MH action = `{http:{method,path,body?,schema?}}`) til PostgREST i måle-jobbet (pg-runner.http: HS256-JWT fra actor.role + settings — `request.jwt.claim.<x>` → claim, så samme actor-definition er ctx-A for SQL og API); observationer (state · observe · vidner · checkpoints) er altid SQL; kald-udfaldet har samme kontrakt (code = body.code = Postgres' SQLSTATE · message = body.message · http_status) og dommen kræver http_status-konsistens; afvisningsstedet er ikke observerbart via API og springes over dér. **Binding uden manifest-ændring:** validatoren kræver api ⇔ (UT: fase matcher »(via API)«; MH/FS: delbevis-id matcher »via-api« ∨ forpligtelsen har et API-negativ); SA aldrig api — så API-negativer KAN kun måles via API, og SQL-negativer kan IKKE snige sig over på API (hvor sted-tjekket ville falde). CI: PostgREST-service i måle-jobbet (postgrest/postgrest mod den friske store, authenticator-password via supabase_admin, `NOTIFY pgrst` efter migrationer, `V5_PGRST_*`-env). **H2** plan:47 regel B (»motoren substituerer det ENE uuid-argument casen sendte«): case-felt `subst: {"<token>": "<uuid>"}` KUN hvor negativets grund bærer `{token}` (K-7/ac-2/neg-2 + fundament-beskeder); dommen substituerer før lighedstjekket, verifieren binder subst til spec'en, validatoren kræver nøgler == tokens og uuid-værdier. Bevist mod rigtig Postgres + rigtig PostgREST (94 grønne): JWT-rolle/sub-paritet SQL↔API, 42501/22023/PGRST202 via body, API-UT/MH-cases, API-mutant dræbt, {id}-substitution bestået og brudt ved anden uuid. Selvtests: harness 189 · spec 51 · verifier 97. Register: 6 kandidater (bundles). Lærdom: Codex' første PRODUKTION mod skemaet er selv en test af måle-laget — den fandt to huller plan-gaten ikke kunne se. | 2026-09-21-commit · 2.C · 2.E · Fase 4 pkt. 1 |
| 42 | **HURTIGT SPOR for pakke 1 (Mathias 2026-09-21, ordret: »Du skal løse at vi lige nu overkomplisere opgaven. det kan ikke være rigtigt at der bliver brugt så meget tid på en simpel pakke« og »Det er hele workflowet som er overkill. Vi skal kunne gennemføre en pakke langt hurtigere. det skal du løse« — ledger M-46/M-47; fabrik-beslutning på mandatet pkt. 36/37, plan-lås og krav urørt):** **(a) Angrebs-tests er KODE, ikke DSL:** Codex skriver `scripts/v5/<pakke>/tests/*.test.mjs` (`export const tests = [{id, covers, run(lib)}]`) mod et lille lib (`ejer.sql` · `som(actor).sql/.http` · `race` · `kontrakt` · `forvent.ok/afvist/lig/sandt`) — `angrebs-spec.json` er nu et INDEKS (skema 2: `tests[{id,file,oid,covers}]` + `mutants[{mutant_id,guard_ref,apply,restore,target_test_ids,control_test_ids}]` + bid-graf), valideret for komplethed mod manifestet (`angrebs-indeks.mjs`: hver forpligtelse×form · hvert negativ · hvert delbevis · D10 · K-gulv · D12); testfilerne er FROSNE via indeksets blob-oids (test-runner afviser en ændret fil). **(b) Beviset er rapporten:** CI's måle-job (`ci-build-dom --produce`, uden token) kører testene + mutant-loopet (apply · alle targets FEJLER · controls ok · restore · alle grønne igen = KILL) via `test-runner.mjs` og skriver rapport skema 3; dom-jobbet (`build-proof.mjs` v3) tjekker STRUKTUR + BINDING (præcis indeksets tests alle ok · præcis indeksets mutanter alle dræbt, genudledt fra protokollen · summary · index_oid == gate-bindingen · bids/reviews/prover · ci-produceret artefakt) — ikke semantik; testenes dybde er Codex' + reviewets ansvar, falsk-grøn-værnet er PROCESSEN (der måler ≠ der bygger · CI kører · mutant-kill · CI = autoritet). DSL-motoren (`build-harness.mjs`/`angrebs-spec.mjs` skema 1, F-20..F-40, H1/H2) forbliver som biblioteker/historik, ikke som gate-dommer. **(c) Slanket Fase 4–6:** ingen plan-runder mere (plan låst @ P5) · Codex skriver indeks + tests i ÉN produktion (max to) med kontraktbehovs-dommen som arbejdsliste · byggeren i TO omgange (A = bid 1+2 · B = bid 3–5), `bids/<bid>.json` pr. omgang · ÉT code-reviewer-review pr. omgang (`reviews/<bid>.json`) · CI `v5/gate/build` læser rapporten · P-8 slutprøve ÉN gang · kort rapport til Mathias → `slut ok` → luk m. ½ side. **SUSPENDERET for pakke 1 (genoptages kun ved behov, pakke-2-vejning i retro):** devil/fresh-eyes pr. Mathias-vendt besked i Fase 4–6 · tre aktørdomme pr. bid · Codex-pas pr. modul før drift (permanent kandidat-drift: `STORK_V5_KANDIDAT_OK=1`, autoritets-jobbet rødt er den ærlige status) · provenance-runder pr. leverance · FA-3 ur-/job-driver (kun hvis Codex' tests ikke kan udtrykke datoflows via SQL-opsætning) · claim_graph. **Residualer navngivet:** R-CI-AUTENTICITET · R-C3-REVIEW · R-SPEC-LEGITIMITET (indekset er Codex' og frosset før byg — hooks) · testdybde uden uafhængig dom. **Kæden bevares (Mathias 2026-09-21, ordret: »det er vigtigt at vi bevare kvaliteten i workflowet, så vi sikre korrekt byg. Din opgave er derfor at mathias sandhed = krav = plan = byg : dette skal sikres uden at overteste«):** det hurtige spor skærer RUNDER, aldrig bevis-LED — sandhed=krav (krav-gate) · krav=plan (plan-gate, manifest ↔ forventningsliste) · plan=byg (manifest → indeks-komplethed → Codex-tests uafhængige af byggeren → CI kører → mutant-kill → ét review pr. omgang → P-8). Billig binding i test-runneren: en test uden forventning er VAKUUM (rød); dækker en test et negativ, SKAL den have kaldt `forvent.afvist(…, netop det negativ)` (også SA-taberen) — ellers rød. Ærlige huller (kontraktbehov #6/#7 i manifestet · FA-3-ur) løses i planen/mekanikken (mini-planversion v3.6 + ét `plan ok`), omgås ikke i måle-laget. Selvtests: angrebs-indeks 41 · test-runner 65 · build-proof 56 · ci-build-dom 41. | 2026-09-21-commit · test-runner.mjs + angrebs-indeks.mjs + angrebs-indeks-validate.mjs + build-proof.mjs v3 + ci-build-dom.mjs + Fase 4 |
