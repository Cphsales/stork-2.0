# gov-2-vagt — Slut-rapport

**Dato:** 2026-06-05 · **Pakke:** gov-2-vagt (2/6 i governance-vagt) · **Type:** repo-side scanner + docs (0 migrations)
**Plan:** `docs/coordination/gov-2-vagt-plan.md` (V2, Codex APPROVAL — ingen nye fund)
**Build:** mekanisk lag-1 governance-spærhage + owns:-register + Codex-mandat + H-hjem

## Formål (genfremlagt)

Byg det mekaniske lag-1 der fanger governance-drift automatisk (Scope B: letvægts-owns:-register så "begreb defineret to steder" fanges mekanisk — root-cause-klassen der startede V5).

## Leverancer (mod plan)

| Leverance            | Status | Evidens                                                                                                                                 |
| -------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| A — mekanisk scanner | ✓      | `scripts/governance-check.mjs`, 7 checks grønne (16 docs, 6 scripts); `pnpm governance:check` + ci.yml-step før Supabase-checks         |
| owns:-register       | ✓      | `<!-- governance-owns: … -->` på 9 docs; owns-uniqueness grøn                                                                           |
| B — Codex-mandat     | ✓      | disciplin.md §8.1 (mekanisk + semantisk Codex-mandat + governance-som-review-artefakt)                                                  |
| C — H-hjem           | ✓      | ny `docs/teknisk/huskeliste.md`: 5 åbne `### [Hxxx]`-entries + historisk-registry (`<!-- gov-historical-codes: … -->`) + H/G-konvention |
| Negativ-test (§3.6)  | ✓      | `governance:selftest`: baseline grøn + 5 planted overtrædelser fanget                                                                   |

## Scanner-checks (alle grønne)

dead-doc-paths (docs+scripts, path-klasser+allowlist) · junk-files · laesefoelge-targets · pointer-validity · owns-uniqueness · number-home-uniqueness (kun kanoniske `### [Xxxx]`-entries) · H-ref-integrity (åben-entry ELLER historisk-kode; suffix→forælder). Build-krav opfyldt: fenced code blocks strippes før alle heading/ref-checks.

**Princip håndhævet:** owner = definitionshjem, ikke mention-hjem. **Ærlig grænse:** deklareret dobbelt-ejerskab + nummer-dubletter fanges mekanisk; udeklareret prosa-overlap → Codex-mandat (lag 2).

## Spærhagen fangede reel drift under build

- `data-grundlag.sh:76` pegede på `docs/coordination/cutover-checklist.md` (flyttet til `docs/teknisk/` i V4) og greppede `### [H...]` — rettet til `docs/teknisk/huskeliste.md` (det nye H-hjem). Reel bug fanget af scanneren = leveret værdi.
- 3 scripts havde stale Lag1-eksempel-refs → templated til `<pakke>`/`<dato>`.

## Stork-invariant-tjek (§7)

| Invariant                    | Status | Evidens                                                                 |
| ---------------------------- | ------ | ----------------------------------------------------------------------- |
| Vision-overholdelse          | ✓      | Tjener princip 1 (én sandhed/ét hjem pr. fakta) — nu mekanisk håndhævet |
| Permission-matrix-konsistens | N/A    | Ingen RPC/RLS                                                           |
| Audit-trigger-dækning        | N/A    | Ingen tabeller                                                          |
| Konfiguration-i-data         | N/A    | Ingen satser/lønarter                                                   |
| End-to-end-flow virker       | ✓      | selftest: baseline + 5 planted overtrædelser                            |
| Anonymisering-bevaring       | N/A    | Ingen data                                                              |

## Plan-afvigelser

1. **Scanner dækker `scripts/*.sh`-doc-refs** (Codex Q3-sanktioneret) — udvidet ud over docs/. Afslørede + rettede reel bug (data-grundlag cutover-sti).
2. **H-klassificering afveg fra pakke-åbningens "migrér de 10"** — kun 5 er åbne actions; 5 er historiske koder (registry, ikke entries). Bekræftet af Codex + flagget til Mathias i plan.

Ingen afvigelse fra godkendt plan-struktur.

## G-numre rejst

- Ingen nye. **Note (ikke G):** scanneren validerer doc-_stier_, ikke §-sektions-referencer (fx codex-notify's disciplin-§-refs). §-ref-validering er en mulig senere udvidelse (kandidat til gov-3 eller G hvis drift opstår).

## Konvergens-historie

| V             | Codex-fund                                                                      | Code-svar                | Outcome       |
| ------------- | ------------------------------------------------------------------------------- | ------------------------ | ------------- |
| V1            | 1 KRITISK (db diff-tvivl) + scope-spørgsmål                                     | scope B valgt af Mathias | V2-grundlag   |
| V2 (Step 2.1) | 2 KRITISK (H-ref selvmodsigende, dead-path for bredt) + 1 MELLEM (cutover-kant) | alle ACCEPT              | V2            |
| V2 (runde 2)  | APPROVAL — ingen nye fund                                                       | —                        | qwerg → build |

Normalt leje (§3.4).

## CODEOWNERS-note

owns:-markøren blev tilføjet til `vision-og-principper.md` (LÅST). Ændringen kræver Mathias-approval via CODEOWNERS ved merge — det er en bevidst, additiv én-linje-markør (ingen vision-/princip-ændring).

## Vision-tjek

- Rigtig løsning eller workaround? **Rigtig.** Root-cause-klassen (begreb defineret to steder) er nu mekanisk fangbar via owns:-register, ikke håbet fanget manuelt.
- Vision-styrkelser: princip 1 mekanisk håndhævet; governance-drift fanges i CI frem for af menneske-opmærksomhed.
- Vision-svækkelser: ingen. Ærlig grænse (prosa-overlap → Codex) er dokumenteret, ikke skjult.
- Konklusion: **forsvarligt.**

## Status efter gov-2

ci.yml-step kører `governance:check` (ikke required endnu — bliver required i gov-4 branch protection). Sekvensen holder: gov-1 (paritet) ✓ → gov-2 (spærhage) ✓ → gov-3 (CI-blockers) → gov-4 (branch protection gør gates bindende) → gov-5 (automation) → gov-6 (arkiv-fold).
