# Codex-review — H024 slut-rapport runde 1

## Status

FEEDBACK

## Review-kontekst

- **Review-type:** Slut-rapport PR-version
- **Branch:** `claude/H024-slut-rapport`
- **PR:** #28
- **Reviewed head:** `9d78d71`
- **Build-main reviewed:** `30fbdf4`
- **CI:** GitHub Actions `CI` for `9d78d71` completed success
- **Pull-verifikation:** `main` og `claude/H024-slut-rapport` var begge up to date før review
- **Lokale checks kørt:** `pnpm migration:check`, `pnpm fitness`, `git log` range-verifikation, `rg` dokument-verifikation

Note: Filnavnet bruger `runde-2`, fordi `docs/coordination/codex-reviews/2026-05-16-h024-runde-1.md` allerede eksisterer som H024 plan-review. Dette review er slut-rapportens runde 1.

SCOPE-KRAV niveau 1 er anvendt med runde 1 og formålet fra H024-krav-dokumentet: alle DB-tests er idempotente, eksisterende test-artefakter ryddet hvor muligt uden GDPR-vej, og Node-runtime opgraderet til 24 LTS.

## Fund

### [MELLEM] Slut-rapportens lag-boundary-tal og build-range er stale/forkerte

Konkret afvigelse: `docs/coordination/rapport-historik/2026-05-16-h024.md` angiver `build-range 8f46615..30fbdf4 = 6 fil-cluster-commits` og `Migration-gate: 75 migrations, 347 kolonner, 0 violations`.

Verifikation mod repo-state:

- `git log --oneline 8f46615..30fbdf4` returnerer kun 5 commits (`6016a68`..`30fbdf4`). Cluster 1 (`8f46615`) er ekskluderet af git range-syntaksen.
- Den range der faktisk indeholder alle 6 H024 cluster-commits er fx `8f46615^..30fbdf4` eller `a15caff..30fbdf4`.
- `pnpm migration:check` returnerer: `78 migration(s)` og `364` migrations-klassificerede kolonner, ikke 75/347.

Konsekvens: Slut-rapporten er ikke et præcist audit-spor for H024, og den kan få en senere reviewer til at overse cluster 1-cleanup-migrationen, som er en central leverance.

Anbefalet handling: V2-rettelse. Opdater slut-rapporten, `seneste-rapport.md` og `aktiv-plan.md` til en range-formulering der faktisk inkluderer alle 6 cluster-commits, og opdater migration-gate-tallene til den aktuelle `pnpm migration:check`-output.

### [KOSMETISK] Rapportens grep-verifikation for G044-reference er usand som skrevet

Konkret afvigelse: Rapporten siger at `grep -r "r4_salary_corrections_cleanup" docs/` returnerer 0 hits. Det gør den ikke. `rg -n "r4_salary_corrections_cleanup" docs/` finder hits i bl.a. afdæknings-filerne, arkiveret krav/plan og rapporten selv.

Det aktive G044-indhold i `docs/teknisk/teknisk-gaeld.md` er rettet nok til formålet, men verifikationspåstanden i slut-rapporten er faktuelt forkert og for bred.

Anbefalet handling: Kosmetisk V2-rettelse sammen med MELLEM-fundet. Skriv verifikationen som en scoped påstand, fx at den gamle aktive G044-fejlreference ikke længere står som berørt test, i stedet for at hævde 0 hits i hele `docs/`.

## Accepterede punkter

- `r3_commission_snapshots_immutability.sql` og `p1a_anonymization_strategies.sql` er tx-wrappet med `begin;`/`rollback;`.
- H022.1 random-offset er rullet tilbage i R3-testen til fixed dato `2199-01-01`.
- `db-test-tx-wrap-on-immutable-insert` findes i `scripts/fitness.mjs`, dækker de 9 planlagte tabeller og er grøn lokalt.
- `pnpm fitness` passer lokalt.
- PR #28 ændrer kun slut-rapport/pegefiler (`aktiv-plan.md`, `seneste-rapport.md`, rapport-fil) oven på merged build `30fbdf4`.

## Konvergens-status

ikke-enig
